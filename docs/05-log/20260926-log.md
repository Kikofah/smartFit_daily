# 2026-09-26

## รันเทสต์ทั้งหมดและสรุปผล

- รัน `npm run test` ทั้ง monorepo (commit `084e897`) เวลา 14:33 (+07): `apps/web` ผ่าน 55/55 ข้อ จาก 8 ไฟล์ (unit test ของ domain logic), `apps/mobile` ยังไม่มีเทสต์
- บันทึกผลรายเทสต์ (ทดสอบอะไร / ผล / เวลารัน) ไว้ที่ [[../03-testing/02-test-result/test-results|test-results.md]] รอบนี้ไม่มีข้อที่ไม่ผ่าน
- ข้อสังเกต: ONB-0, ONB-2, REC-1, REC-3, PLN-1, PLN-2, INT-2 และ flow UI ทั้งหมดยังไม่มี automated test

## Static checks

- `npm run typecheck` และ `npm run lint` ผ่านทุก workspace ไม่มี error บันทึกเพิ่มใน [[../03-testing/02-test-result/test-results|test-results.md]] § Static checks

## plan-task-builder: audit และอัปเดต Release Plan / Task Breakdown

- **Audit**: เทียบกับ `backlog.md`, `01-spec/`, HLA และโค้ดจริง พบ (1) INT-0 ไม่มีใน `release-plan.md` และ `03-task/` ตั้งแต่ 2026-08-30 (2) คำอธิบาย `TASK-INFRA-01` และ `TASK-ONB-3` ไม่ตรงกับปัจจุบัน (3) Status ทุก task ยังเป็น "ยังไม่เริ่ม" แม้มีโค้ดของทุกฟีเจอร์แล้ว
- **ผู้ใช้ยืนยัน Status**: `TASK-INFRA-01` = เสร็จแล้ว (มี Firebase project และ deploy บน Cloud Run + Firebase Hosting แล้ว), task ของ Feature ID ทั้ง 16 = กำลังทำ (มีโค้ดแล้ว แต่ยังไม่ผ่าน Exit Criteria ของ phase)
- **`release-plan.md`**: เพิ่ม INT-0 เข้า Future Phase (Could), dependency `ONB-0 → INT-0`, `INT-0 → INT-2/INT-3` (HLA §3.1/§4.5, REQ-18), นับ Feature ID เป็น 16
- **`03-task/`**: เพิ่ม `TASK-INT-0` ใน `phase-3-smart-integrations.md`, แก้คำอธิบาย `TASK-INFRA-01` (Express บน Cloud Run) และ `TASK-ONB-3` (เป้าหมาย 2 ค่า), อัปเดต Status ทั้ง 3 ไฟล์
- **ส่งต่อ**: `tech-stack.md` § 7 ยังบอกว่ายังไม่เลือก Firebase region ทั้งที่ provision แล้ว ควรให้ `tech-stack-builder` บันทึก region ที่ใช้จริง

## tech-stack-builder: บันทึก Firebase region ที่ provision จริง

- **ที่มา**: ต่อจากข้อส่งต่อของ `plan-task-builder` ข้างบน ผู้ใช้เลือก "record as official" (ตัวเลือกอื่นคือ บันทึกแต่ยังเปิดไว้ / ข้าม)
- **ข้อเท็จจริงที่ตรวจแล้ว**: Firestore `(default)` ของ project `smartfit-daily` อยู่ที่ `asia-southeast1` ชนิด `FIRESTORE_NATIVE` และ Cloud Run service `smartfit-daily-web` อยู่ที่ `asia-southeast1` (ตาม `smartfit_daily_app/firebase.json`)
- **`tech-stack.md`**: ปิด § 7 ข้อ 3 เป็น RESOLVED ระบุ `asia-southeast1` (สิงคโปร์) ทั้ง Firestore และ Cloud Run และหมายเหตุว่าเปลี่ยน location ของ Firestore ไม่ได้หลังสร้างแล้ว แก้ย่อหน้า PDPA ใน § 4 ให้ระบุ region จริง และอัปเดตหัวเอกสาร
- **ไม่รัน Discovery Questionnaire**: รอบนี้แค่บันทึกสิ่งที่ provision ไปแล้ว ไม่ได้เปลี่ยนตัวเลือก stack และ § 6 (Mapping) ไม่เปลี่ยน ภาคผนวก Stack Mapping ของ HLA/API Spec/Database Schema/Detailed Design จึงไม่ต้อง sync ใหม่
- **ที่ยังค้าง (รายงานเท่านั้น)**: `03-task/phase-1-mvp-core-loop.md` (บรรทัดประมาณ 55-56) ยังเขียนว่า `tech-stack.md` § 7 ยังไม่เลือก region ถ้าต้องการให้ตรงกันให้รัน `plan-task-builder`

## feature-list-journey: INT-1 ต้องมี log อย่างน้อย 7 วันก่อนพยากรณ์

- **การตัดสินใจ**: ผู้ใช้ยืนยันวันนี้ (ask-user protocol, ตัวเลือก 3 วัน = ค่าในโค้ดเดิม / 7 วัน / 14 วัน) ว่า INT-1 (REQ-11) ต้องมี log แคลอรี่สะสม**อย่างน้อย 7 วัน**จึงแสดงวันที่พยากรณ์ ถ้าน้อยกว่านั้นแสดง empty state "ประวัติยังไม่พอ" เหตุผลคือหนึ่งสัปดาห์เต็มตรงกับ Weekly Planner จันทร์–อาทิตย์ของ PLN-1 ค่าเฉลี่ยจึงรวมทั้งวันออกกำลังกายและ Rest/Cheat Day และผันผวนน้อยกว่า (โค้ดกำลังเปลี่ยน `MIN_LOG_DAYS_FOR_FORECAST` จาก 3 เป็น 7 แยกต่างหาก)
- **`01-spec/20260823-04-smart-integrations.md`**: เพิ่มการตัดสินใจนี้ใน "ข้อสมมติฐาน/การตัดสินใจที่ยืนยันแล้ว" (พร้อมเหตุผล) และขีดฆ่าข้อ INT-1 ใน "จุดที่ยังไม่ได้ระบุ" ว่า resolve แล้ว เลข REQ ไม่เปลี่ยน
- **`user-journeys.md`**: INT-1 ใส่เงื่อนไข "อย่างน้อย 7 วัน" ในจุดตัดสินใจของ diagram, ขั้นที่ 4-5, Preconditions และ Alt/Edge Cases (อ้างอิง decision ใน spec) และปิด Open Question ข้อ 3
- **`backlog.md`**: ไม่ได้แก้ เพราะคำอธิบาย INT-1 ไม่ได้บอกว่าข้อนี้ยังไม่ resolve และไม่ขัดกับการตัดสินใจ
- **ส่งต่อ (รายงานเท่านั้น)**: เอกสาร downstream ที่ยังบอกว่ายังไม่ resolve หรือบอกว่า 3 วัน ได้แก่ acceptance-criteria/test-plan/test-cases (`test-suite-builder`), HLA (`architecture-builder`), api-spec (`api-db-spec-builder`), detailed-design/04 (`detailed-design-builder`), tech-stack (`tech-stack-builder`), release-plan และ phase-3 task (`plan-task-builder`), prototype v1 หน้า 10 และ README (`prototype-builder`)

## ปิดงานค้าง: INT-1 ขั้นต่ำ 7 วัน, region, API route test

- **INT-1 / REQ-11**: ผู้ใช้เลือกให้ต้องมี log อย่างน้อย **7 วัน** ก่อนพยากรณ์ (ตัวเลือกที่เสนอคือ 3/7/14 วัน) บันทึกใน spec และ user-journeys ผ่าน `feature-list-journey` (ดูหัวข้อด้านบน) แล้ว sync เอกสาร downstream ทั้งหมด:
  - `test-suite-builder`: AC-INT-1-01/02 แก้, เพิ่ม AC-INT-1-08/09, แก้ TC-INT-1-001/002, เพิ่ม TC-INT-1-009/010, ปิด Risk R4 ใน test-plan.md
  - `architecture-builder` (HLA §4.5, §8 ข้อ 3), `api-db-spec-builder` (api-spec §3.7, §4 ข้อ 3), `detailed-design-builder` (04-smart-integrations INT-1), `tech-stack-builder` (§6.1, §7 ข้อ 10)
  - `plan-task-builder`: release-plan §5, phase-3 §3
  - โค้ด: `MIN_LOG_DAYS_FOR_FORECAST` 3 → 7 (`apps/web/server/domain/weightForecast.ts`) พร้อม unit test boundary 6/7 วัน
- **Region**: ผู้ใช้ยืนยันให้บันทึก `asia-southeast1` ที่ provision จริง (ตรวจจาก Firebase CLI และ `firebase.json`) ลง `tech-stack.md` §7 ข้อ 3 ผ่าน `tech-stack-builder` และ sync `phase-1-mvp-core-loop.md`
- **Stack Mapping appendices**: ตรวจแล้วว่า reconcile เป็น Express/Cloud Run ตั้งแต่ 2026-08-30 ที่ยังเห็นคำว่า Cloud Functions เป็นแค่บันทึกประวัติ ไม่ต้องแก้
- **API route test**: เพิ่ม 53 ข้อใน 7 ไฟล์ (`server/routes/**`, `server/middleware/`) ใช้ Firestore/Auth ปลอมในหน่วยความจำ (`server/test/fakeFirebase.ts`) และ stub YouTube/Gemini ครอบคลุม ONB-0/1/2/3, REC-1/2/3/4, PLN-1/2/3, INT-1 ผลรวม 110/110 ผ่าน, typecheck และ lint ผ่าน อัปเดต [[../03-testing/02-test-result/test-results|test-results.md]]
- **Prototype**: ผู้ใช้เลือกให้แก้ใน v1 (ไม่สร้าง v2) `prototype-builder` แก้ empty state ของ `v1/10-progress-insights.html` เป็น "ประวัติยังไม่พอสำหรับพยากรณ์ / ต้องบันทึกผลอย่างน้อย 7 วัน" (ตรงกับ AC-INT-1-02) และปิด Open Question ใน `v1/README.md`
- **README.md**: เพิ่ม URL เว็บออนไลน์ (https://smartfit-daily.web.app) และลิงก์ไปที่ test-results.md ไว้บนสุด
- **แอปจริง**: แก้ข้อความ empty state ของ INT-1 ใน `apps/web/client/src/pages/ProgressScreen.tsx` ให้ตรงกับ prototype v1 และ AC-INT-1-02 ("ประวัติยังไม่พอสำหรับพยากรณ์ — ต้องบันทึกผลอย่างน้อย 7 วัน…") ต้อง deploy ใหม่ก่อนหน้าเว็บจริงจะเปลี่ยน
- **Deploy**: build และ deploy `smartfit_daily_app` (commit `295590e`) ขึ้น Cloud Run service `smartfit-daily-web` (`asia-southeast1`) ด้วย `gcloud run deploy --source` ได้ revision `smartfit-daily-web-00004-rwl` รับ traffic 100% (ก่อนหน้าคือ `00003-tpx`) env vars เดิม 3 ตัวยังอยู่ครบ ตรวจ https://smartfit-daily.web.app แล้ว: หน้าเว็บโหลดได้, API ตอบ 401 เมื่อไม่ login, และ bundle ใหม่มีข้อความ empty state "ต้องบันทึกผลอย่างน้อย 7 วัน" แทนข้อความเดิม

## ติดตั้ง Playwright (e2e) ใน apps/web

- เพิ่ม `@playwright/test` 1.63.0 ใน `apps/web`, สคริปต์ `npm run test:e2e`, `playwright.config.ts` (ค่าเริ่มต้นทดสอบกับ https://smartfit-daily.web.app, เปลี่ยนได้ด้วย `E2E_BASE_URL`, รันทั้ง Desktop Chrome และ Pixel 7) และ smoke test แบบอ่านอย่างเดียว 3 ข้อใน `e2e/smoke.spec.ts` รันกับเว็บจริงผ่าน 6/6
- ไม่ให้ `playwright-report/`, `test-results/` เข้า git และ Docker image
