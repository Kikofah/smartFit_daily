# 2026-08-28 - Log

## สรุปสิ่งที่ทำ

### Prototype v1 (`prototype-builder`) — แก้ไข 3 ไฟล์ตาม fixed-calendar-week decision

- แก้ `v1/05-daily-dashboard.html`, `v1/08-weekly-planner.html`, `v1/12-device-pairing.html`: แก้วันในสัปดาห์
  ที่ผิด, แก้ streak badge ให้ตรง canonical dataset (streak = 3 ไม่ใช่ 5), แก้ caption ของ PLN-1 ให้สะท้อนว่า
  decision resolve แล้ว, แก้สถานะวันจันทร์ 24 ส.ค. ใน weekly planner ให้ตรงกับ log-history/test case ที่มี
  อยู่ (260 kcal ไม่ครบเป้าหมาย 400 kcal ไม่ใช่ครบเป้าหมาย), แก้ touch target ปุ่ม stepper ให้ครบ 44×44px

### Self-freshness audit ของ Test Suite (`test-suite-builder`) — ส่วนที่ 3/3 ของการตรวจสอบทั้งโปรเจกต์

ตรวจ `acceptance-criteria.md`, `test-plan.md`, `test-cases/*.md` ทั้ง 4 ไฟล์ ตามขั้นตอนที่ 0 ของ SKILL.md
ครบทุกหัวข้อ (Feature ID/REQ parity, coverage gap ใหม่, fact drift, scope drift ของ test-plan) — พบ fact
drift จริง 1 จุดที่ต้องแก้ และเปิดคำถามใหม่ 1 ข้อที่ยังไม่ resolve เอง:

**พบและแก้แล้ว** — PLN-1 fixed-calendar-week decision (resolve 2026-08-27) ทำให้ AC/test case เดิมล้าหลัง:

- `acceptance-criteria.md`: เพิ่ม `AC-PLN-1-03` (วันที่ผ่านมาแล้วซึ่งมี log อยู่ก่อน เปิดดูได้เฉพาะ read-only)
  แทนที่ note เดิมที่บอกว่าเรื่องนี้ "ยังไม่ระบุชัดเจน" (ซึ่งไม่จริงแล้วหลัง decision resolve) อัปเดตสรุปจำนวน
  scenario รวมจาก 38 เป็น 39
- `test-cases/planner-logging.md`: เพิ่ม `TC-PLN-1-004` ครอบคลุม AC-PLN-1-03 ใหม่ (ใช้ log ของวันจันทร์
  24 ส.ค. 260 kcal/ไม่ครบเป้าหมาย ตรวจสอบแล้วว่าตรงกับ dataset ล่าสุดที่ prototype v1 เพิ่งปรับให้ตรงกัน)
  ลบ note "Gap ที่ข้ามไปโดยตั้งใจ" เดิมที่อ้าง Open Question ซึ่งตอนนี้ resolve แล้ว อัปเดตสรุปจำนวน test
  case ของ PLN-1 จาก 3 เป็น 4 (รวมทั้งไฟล์จาก 17 เป็น 18)
- `test-plan.md`: mark ความเสี่ยง R3 (นิยามการแก้แผนวันที่ผ่านไปแล้ว) เป็น **RESOLVED** พร้อมอ้างอิง decision
  ที่ resolve แล้ว แทนที่จะปล่อยไว้เป็นความเสี่ยงเปิดค้าง อัปเดตจำนวน scenario ใน Entry Criteria ข้อ 2 จาก
  38 เป็น 39

**พบแต่ยังไม่แก้ — รอผู้ใช้ตัดสินใจ**: การ resolve PLN-1 ให้วันที่ผ่านมาแล้ว+มี log เป็น read-only ทำให้เกิด
คำถามใหม่กับ `TC-PLN-2-003`/`TC-PLN-2-004` (test case เดิมที่ทดสอบ "ตั้ง Cheat/Rest Day ย้อนหลัง" ผ่าน
ปฏิทินบนวันอังคาร 25 ส.ค./จันทร์ 24 ส.ค. ซึ่งเป็นวันที่ผ่านมาแล้วและมี log อยู่ก่อน) — ไม่ชัดเจนว่า flow นี้
ยังทำได้อยู่หรือไม่ภายใต้ PLN-1 ใหม่ ยังไม่แก้ไข test case ทั้งสองในรอบนี้ บันทึกไว้เป็น note ในเอกสารและส่ง
คำถามกลับให้ผู้ใช้แทนการเดา (ดูรายละเอียด 3 แนวทางในรายงานของ agent รอบนี้)

**ตรวจแล้วไม่พบปัญหา**: Feature ID/REQ parity ของทั้ง 13 Feature ID ครบถ้วนตรงกับ `backlog.md`/`01-spec/`
ปัจจุบัน, ไม่มี Feature ID ใหม่ที่ขาด AC/test case, NFR-01–08 ที่เพิ่ง trace เข้า `backlog.md` ใหม่ไม่กระทบ
เนื้อหา NFR section ของ `test-plan.md` (derive ถูกต้องอยู่แล้วตั้งแต่สร้างพร้อมกัน), ONB-2 multi-select
(reverse-drift ที่ backfill เข้า spec แล้ว) ตรงกับ AC-ONB-2-01/TC-ONB-2-001/002 อยู่ก่อนแล้ว ไม่ต้องแก้,
scope/priority ใน `test-plan.md` (Must/Should/Could) ยังตรงกับ MoSCoW ปัจจุบันใน `backlog.md` ทุกจุด

## Open items ที่ยังค้างอยู่

- Open Questions เดิม 4 ข้อที่เหลือ (REC-1 tolerance, REC-4 warmup/cooldown นับ target, INT-1 min log days,
  INT-2/INT-3 conflict resolution) และ gap อื่นที่บันทึกไว้ใน log วันที่ 2026-08-27 ยังไม่ resolve เพิ่มในรอบนี้

### Resolve คำถาม TC-PLN-2-003/004 vs. PLN-1 read-only decision (`feature-list-journey`)

- ผู้ใช้ตอบคำถามที่ `test-suite-builder` เปิดไว้ข้างต้นแล้ว: เลือก **ตัวเลือก (A) จำกัดเฉพาะวันนี้** — การ
  ตั้ง/เปลี่ยน Cheat/Rest Day ที่ทับ log ซึ่งมีอยู่แล้ว (PLN-2) ทำได้เฉพาะ **"วันนี้"** เท่านั้น ไม่ใช่วันใน
  อดีตของสัปดาห์ แม้จะอยู่ใน fixed calendar week เดียวกันก็ตาม — สอดคล้องกับ PLN-1's mermaid diagram ที่
  วันในอดีต+มี log ไปที่ node read-only แบบไม่มีข้อยกเว้น
- บันทึก decision นี้ไว้ใน `01-spec/20260823-03-planner-logging.md` § ข้อสมมติฐาน/การตัดสินใจที่ยืนยันแล้ว
  (ผูกกับ PLN-2/REQ-09) ก่อน แล้วอัปเดตตามให้ตรงกันใน `user-journeys.md` (diagram ของ PLN-2 เพิ่ม guard
  node ปิดกั้นวันในอดีต+มี log ก่อนถึงกิ่งเดิม, Trigger/Steps/Preconditions/Alt-Edge Case ปรับให้สะท้อนว่า
  ทับ log ได้เฉพาะวันนี้) และ `backlog.md` (คำอธิบาย PLN-2 เพิ่มข้อจำกัดเดียวกัน)
- ส่งต่อให้ `test-suite-builder` แก้ `TC-PLN-2-003`/`004` ให้ตรงกับ decision ใหม่นี้ต่อ (ไม่ได้แก้เองในรอบนี้
  เพราะเป็นไฟล์ที่ `test-suite-builder` เป็นเจ้าของ)

### แก้ไข TC-PLN-2-003/004 ตาม decision ที่ resolve แล้ว (`test-suite-builder`)

- `docs/01-requirements/acceptance-criteria.md`: แก้ `AC-PLN-2-02` ให้ระบุชัดว่าเป็น "วันนี้เท่านั้น" และเพิ่ม
  `AC-PLN-2-04` ใหม่ (พยายามตั้ง/เปลี่ยน Cheat/Rest Day ทับ log ของวันในอดีตของสัปดาห์ → ถูกปิดกั้น
  read-only ตาม PLN-1 ไม่มีข้อยกเว้น) อัปเดตสรุปจำนวน scenario ของ PLN-2 จาก 3 เป็น 4 และรวมทั้งไฟล์จาก
  39 เป็น 40
- `docs/03-testing/01-test-plan/test-cases/planner-logging.md`: แก้ `TC-PLN-2-003`/`TC-PLN-2-004` ให้ใช้
  "วันนี้" แทนวันอังคาร 25 ส.ค./วันจันทร์ 24 ส.ค. เดิม พร้อมเปลี่ยนไปใช้ test data อิสระ (เป้าหมาย 500 kcal)
  แทนการยืม dataset สัปดาห์ 24–30 ส.ค. เพราะ "วันนี้" ของ dataset นั้น (27 ส.ค.) ถูกกำหนดไว้แล้วว่าครบเป้าหมาย
  ด้วย log จริง 410 kcal (TC-PLN-4-001) การใช้ตัวเลขเดิมซ้ำจะขัดกันเอง — เพิ่ม `TC-PLN-2-006` ใหม่ครอบคลุม
  `AC-PLN-2-04` (ใช้วันจันทร์ 24 ส.ค. เดิม/260 kcal/ไม่ครบเป้าหมาย ยืนยันว่าถูกปิดกั้นจริง เชื่อมกับ
  `TC-PLN-1-004`) ลบ note "จุดที่ยังไม่ยืนยัน" ที่ตั้งไว้ก่อนหน้า แทนที่ด้วย note สรุปว่า resolve แล้ว
  อัปเดตหมายเหตุกลุ่ม test data ต้นไฟล์ให้ระบุว่า PLN-2-003/004 ย้ายไปอยู่กลุ่ม "independent test data"
  อัปเดตสรุปจำนวน test case ของ PLN-2 จาก 5 เป็น 6 (รวมทั้งไฟล์จาก 18 เป็น 19)
- `docs/03-testing/01-test-plan/test-plan.md`: อัปเดตความเสี่ยง R3 ให้ระบุว่าคำถามต่อเนื่องเรื่อง PLN-2 ก็
  resolve แล้วเช่นกัน (ไม่ใช่ "รอผู้ใช้ตัดสินใจ" อีกต่อไป) และอัปเดตจำนวน scenario ใน Entry Criteria ข้อ 2
  จาก 39 เป็น 40
- ไม่ได้แตะ `01-spec/*.md`, `backlog.md`, `user-journeys.md` เพิ่มเติม (เป็นไฟล์ที่ `feature-list-journey`
  แก้ไว้ให้แล้วตามที่รายงานไว้ด้านบน)
- หมายเหตุเสริม (ไม่ได้แก้ในรอบนี้ ไม่กระทบ scope ของ fix ครั้งนี้): สังเกตว่า `TC-PLN-2-005` (ยกเลิก Cheat/
  Rest Day ก่อนสิ้นวัน) ใช้ "วันพุธ 26 ส.ค." เป็นวันที่ยังไม่สิ้นวัน ซึ่งเป็นวันในอดีตของสัปดาห์เทียบกับ "วันนี้
  = พฤหัสบดี 27 ส.ค." ของ dataset กลุ่มเดียวกัน — เป็นความคลาดเคลื่อนเล็กน้อยที่มีมาตั้งแต่สร้างเอกสารครั้งแรก
  (2026-08-27) ไม่เกี่ยวกับ decision ใหม่รอบนี้โดยตรง บันทึกไว้เป็น note สำหรับการตรวจรอบถัดไป

### แก้ไข TC-PLN-2-005 ตามที่ผู้ใช้ขอเพิ่ม (`test-suite-builder`)

- `docs/03-testing/01-test-plan/test-cases/planner-logging.md`: แก้ `TC-PLN-2-005` ให้ใช้ "วันนี้" แทน
  "วันพุธ 26 ส.ค." เดิม (วันในอดีตของสัปดาห์ที่ขัดกับ pre-condition "ยังไม่สิ้นวัน") ย้ายไปใช้ test data
  อิสระเช่นเดียวกับ TC-PLN-2-003/004 เพราะ "วันนี้" ของ dataset สัปดาห์ 24–30 ส.ค. มี log จริงครบเป้าหมาย
  410 kcal อยู่แล้ว (TC-PLN-4-001) ซึ่งขัดกับสถานะ "ตั้งเป็น Rest Day ไว้ก่อน ยังไม่มี log จริง" ที่ test
  case นี้ต้องการ — อัปเดตหมายเหตุกลุ่ม test data ต้นไฟล์ให้ PLN-2-005 ย้ายไปกลุ่ม "independent test data"
  ด้วย (กลุ่ม 1 เหลือ PLN-2-001/002/006 เท่านั้น) ไม่กระทบจำนวน scenario/test case รวม (ยังคง PLN-2 = 4
  AC / 6 TC, รวมทั้งไฟล์ 40 scenario/19 test case เท่าเดิม เพราะเป็นการแก้ไขเนื้อหาไม่ใช่เพิ่ม/ลบรายการ)
- ตรวจ `acceptance-criteria.md` (`AC-PLN-2-03`) และ `test-plan.md` แล้ว ไม่มี reference ถึงวันที่เฉพาะเจาะจง
  ของ TC-PLN-2-005 ที่ต้องแก้ตาม — ทั้งสองไฟล์ไม่มีการเปลี่ยนแปลงเพิ่มเติมจากจุดนี้

### เพิ่ม skill/agent คู่ใหม่: `architecture-builder`/`architecture-writer`

- สร้าง `.claude/skills/architecture-builder/SKILL.md` และ `.claude/agents/architecture-writer.md`
  ตาม pattern เดียวกับ `prototype-builder`/`prototype-writer` — สำหรับสร้าง/อัปเดต/audit เอกสาร **High
  Level Architecture เชิง conceptual** (`docs/02-design/02-technical/high-level-architecture.md`,
  ยังไม่สร้างจริง แค่เตรียม skill/agent ไว้) กติกาที่เข้มงวดที่สุดของ skill นี้คือห้ามผูก technical stack
  ใดๆ (ไม่ระบุชื่อ framework/database/cloud/ภาษาโปรแกรม) ยกเว้นชื่อระบบภายนอกที่ requirement กำหนดไว้แล้ว
  จริง (YouTube, Health API/wearable)
- ผู้ใช้ยืนยัน 3 จุดก่อนสร้าง (เลือกตัวเลือกที่แนะนำทั้งหมด): (1) เอกสารเป็นไฟล์เดียวใน
  `docs/02-design/02-technical/` ไม่ versioned เหมือน prototype (2) มี full audit capability เทียบกับ
  Requirement/Backlog/User Journey/Prototype เหมือน skill อื่น (3) ไม่รวมเข้า `pipeline-orchestrator`
  รันอัตโนมัติ — แยกเรียกเองเหมือน `prototype-builder`
- อัปเดต `CLAUDE.md`: เพิ่มแถวในตาราง "Skills & agents at a glance" (4→5 คู่), เพิ่ม section "Building the
  High Level Architecture doc" ใหม่, ปรับคำอธิบาย `02-technical/` ใน Documentation structure, ปรับ
  "Current focus" ให้ `02-technical` ไม่ถูกจัดเป็น scaffold เฉยๆ อีกต่อไป (มี skill/agent พร้อมใช้แล้วเหมือน
  `03-testing`)
- ปรับ `feature-list-journey`'s SKILL.md และ `feature-journey-writer` agent ให้ขั้นตอนที่ 0.5
  (ตรวจดาวน์สตรีมว่า fresh หรือไม่) ครอบคลุม `high-level-architecture.md` ด้วย (เดิมตรวจแค่ AC/Test
  Plan/Test Case/Prototype) เพื่อให้คำอธิบายใน `CLAUDE.md` ตรงกับพฤติกรรมจริงของ skill
- ยังไม่ได้รัน `architecture-builder` จริง — สร้างแค่ตัว skill/agent ตามที่ผู้ใช้ขอ ยังไม่มีเอกสาร
  `high-level-architecture.md` เกิดขึ้นในรอบนี้

### สร้างเอกสาร High Level Architecture ครั้งแรก (`architecture-builder`)

- รันครั้งแรก ไม่มีเอกสารเดิมให้ audit — เก็บข้อมูลจาก `backlog.md`, `01-spec/*.md` ทั้ง 5 ไฟล์ (รวม NFR),
  และ `user-journeys.md` ครบทั้ง 13 Feature ID ก่อนเสนอโครงเนื้อหาให้ผู้ใช้ยืนยัน
- **พบ gap ใหม่ระหว่างเก็บข้อมูล** (ไม่ใช่ audit ของเอกสารนี้เอง เพราะยังไม่มีมาก่อน แต่เป็นความไม่สอดคล้อง
  ในเอกสารต้นทาง): `user-journeys.md`'s INT-1 ระบุ precondition "มีเป้าหมายน้ำหนัก (จาก ONB-3)" แต่ ONB-3/
  REQ-02 เดิมไม่เคยเก็บตัวเลขน้ำหนักเป้าหมายจริง มีแค่ประเภทเป้าหมาย — ถามผู้ใช้ 3 ทางเลือก ผู้ใช้เลือก
  **(A) เพิ่มช่องกรอกน้ำหนักเป้าหมายใน ONB-3** → ส่งต่อให้ `feature-list-journey` บันทึกเป็น confirmed
  decision: บังคับกรอกเมื่อเลือก "ลดน้ำหนัก" ไม่บังคับสำหรับเป้าหมายอื่น (แก้ `01-spec/20260823-01-...md`,
  `backlog.md`, `user-journeys.md`'s ONB-3 และ INT-1 ให้ตรงกัน)
- ผู้ใช้ยืนยันแผนที่เสนอ: **7 conceptual component** (Personalization & Profile, Content Recommendation,
  Exertion & Calorie Calculation, Planner & Day-Status, Logging & Streak, Insights & Forecast,
  Integration Gateway), **5 data flow** (Onboarding, Daily Recommendation & Exercise Session, Weekly
  Planning & Day-Status, Daily Logging & Streak, Smart Integration & Insights), **3 external
  integration boundary** (YouTube, Health API/Wearable, Bluetooth สมาร์ตสเกล), **14 conceptual data
  entity**
- สร้าง `docs/02-design/02-technical/high-level-architecture.md` (ไฟล์เดียว ไม่ versioned) ครบ 10 หัวข้อ
  ตามโครงสร้างบังคับของ skill — ทุก Mermaid diagram อ้างอิง step number จริงจาก `user-journeys.md`
  (ตรวจนับ step ปัจจุบันของทุก feature ก่อนอ้างอิง เพราะบางไฟล์เพิ่งถูกแก้จาก audit รอบก่อนหน้า) ตรวจ
  self-check แล้วว่าไม่มีคำศัพท์ผูก technical stack หลุดเข้ามา
- อัปเดต `docs/02-design/02-technical/index.md` ให้กล่าวถึงไฟล์ใหม่นี้
- คงจุดที่ยังไม่ได้ระบุ (Open Questions) ไว้ 5 ข้อในเอกสาร ไม่ฟันธงแทน upstream: REC-1 tolerance, REC-4
  warmup/cooldown นับ target, INT-1 min log days, INT-2/INT-3 conflict resolution, และ (ใหม่) ช่องทาง
  แจ้งเตือนผู้ใช้ที่ข้ามช่องน้ำหนักเป้าหมายแบบไม่บังคับ

### Resolve gap ONB-3 ไม่มีช่องกรอกน้ำหนักเป้าหมาย ที่ INT-1 ต้องใช้ (`feature-list-journey`)

- `architecture-builder` พบระหว่างเตรียม High-Level Architecture ว่า `user-journeys.md`'s INT-1 ระบุ
  precondition "มีเป้าหมายน้ำหนัก (จาก ONB-3)" แต่ ONB-3/REQ-02 จริงมีแค่ขั้นตอนเลือก **ประเภทเป้าหมาย**
  แล้วแปลงเป็น kcal offset เท่านั้น ไม่มีขั้นตอนกรอกตัวเลขน้ำหนักเป้าหมายเลย — ถามผู้ใช้ผ่าน
  AskUserQuestion แล้วเลือก **ตัวเลือก (A) เพิ่มช่องกรอกน้ำหนักเป้าหมายใน ONB-3**
- บันทึก decision ไว้ใน `01-spec/20260823-01-onboarding-personalization.md` § ข้อสมมติฐาน/การตัดสินใจที่
  ยืนยันแล้ว (ผูกกับ ONB-3/REQ-02) ก่อน: เพิ่มช่องกรอกน้ำหนักเป้าหมาย (target weight, kg) — **บังคับ**
  เมื่อเลือก "ลดน้ำหนัก" (กรณีที่ INT-1 ต้องใช้จริง) และ **ไม่บังคับ** เมื่อเลือก "กระชับสัดส่วน"/
  "เพิ่มความอึด" (ดุลยพินิจของ `feature-list-journey` เพราะสองเป้าหมายนี้ไม่ได้นิยามด้วยตัวเลขน้ำหนัก
  โดยตรง) — เพิ่ม open point ใหม่ใน § จุดที่ยังไม่ได้ระบุ เรื่องช่องทางเตือนให้กรอกภายหลังถ้าข้าม
- อัปเดตตามให้ตรงกันใน `backlog.md` (คำอธิบาย ONB-3 เพิ่มเรื่องน้ำหนักเป้าหมาย + เงื่อนไขบังคับ/ไม่บังคับ)
  และ `user-journeys.md` (ONB-3: diagram/Steps/Success State/Alt-Edge Case เพิ่มขั้นตอนกรอกน้ำหนักเป้าหมาย;
  INT-1: diagram เพิ่ม guard node ตรวจว่ามีน้ำหนักเป้าหมายหรือไม่ก่อนเช็ค log, Steps/Preconditions/
  Alt-Edge Case ปรับให้ตรงกับแหล่งที่มาจริงของค่านี้)
- ไม่กระทบ AC/test-plan/test-case/prototype ที่มีอยู่แล้วในรอบนี้ (เป็นการเพิ่ม field ใหม่ใน ONB-3 ที่ยังไม่มี
  scenario ทดสอบมาก่อน) — แนะนำให้รัน `test-suite-builder` ต่อเพื่อเพิ่ม AC/test case ของช่องกรอกน้ำหนัก
  เป้าหมายใหม่ (ONB-3) และปรับ precondition ของ INT-1 ที่เกี่ยวข้อง กับรัน `prototype-builder` เพื่อเพิ่ม UI
  ช่องกรอกนี้ในหน้าจอ ONB-3 ที่มีอยู่แล้วใน `v1/`

### เพิ่ม skill/agent คู่ใหม่: `api-db-spec-builder`/`api-db-spec-writer`

- สร้าง `.claude/skills/api-db-spec-builder/SKILL.md` และ `.claude/agents/api-db-spec-writer.md` —
  สำหรับสร้าง/อัปเดต/audit เอกสาร **API Spec** (`docs/02-design/02-technical/api-spec.md`) และ
  **Database Schema** (`docs/02-design/02-technical/database-schema.md`) เชิง conceptual ที่เจาะจงกว่า
  High Level Architecture หนึ่งระดับ (ยังไม่สร้างจริง แค่เตรียม skill/agent ไว้)
- ผู้ใช้ยืนยัน 2 จุดที่เป็นความไม่ชัดเจนจริงก่อนสร้าง (เลือกตัวเลือกที่แนะนำทั้งคู่):
  1. **API interface style**: ใช้ REST-style convention (HTTP verb + resource path + status code)
     เป็นภาษากลาง — เป็นข้อยกเว้นเพิ่มจากกติกา "ห้ามผูก stack" ของ HLA เพราะ REST/HTTP เป็นมาตรฐานสากล
     ไม่ใช่ทางเลือก framework ของทีม
  2. **HLA เป็นเงื่อนไขบังคับ**: ต้องมี `high-level-architecture.md` อยู่ก่อนเสมอ ถ้ายังไม่มี skill นี้
     หยุดทันทีและบอกให้รัน `architecture-builder` ก่อน — **ไม่อนุญาตให้สร้าง HLA เองแบบที่
     `test-suite-builder` ได้รับอนุญาตพิเศษให้สร้าง NFR doc** เพราะ HLA มีเจ้าของ (`architecture-builder`)
     อยู่แล้ว
  - Database Schema อนุญาตให้ใช้ logical/abstract data type เท่านั้น (string/integer/decimal/boolean/
    date/datetime/enum/identifier) ไม่ใช่ DBMS-specific type — ตัดสินใจเองตาม pattern เดียวกับ HLA
    ไม่ต้องถามเพิ่ม เพราะเป็นแนวปฏิบัติมาตรฐานของ logical data model อยู่แล้ว
- อัปเดต `CLAUDE.md`: เพิ่มแถวในตาราง "Skills & agents at a glance" (5→6 คู่), เพิ่ม section "Building
  the API Spec & Database Schema" ใหม่, แก้ข้อความเดิมใน "Building the High Level Architecture doc" ที่
  เคยเรียก database schema/API design ว่า "stack-specific documents" ให้ถูกต้อง (ตอนนี้เป็นชั้น
  conceptual แยกที่ `api-db-spec-builder` ดูแล ไม่ใช่ stack-specific), ปรับ Documentation structure และ
  Current focus ให้ตรงกับสถานะปัจจุบัน (02-technical มีเอกสารแรกแล้ว)
- ปรับ `feature-list-journey`'s SKILL.md และ `feature-journey-writer` agent ให้ขั้นตอนที่ 0.5 (ตรวจ
  ดาวน์สตรีมว่า fresh หรือไม่) ครอบคลุม `api-spec.md`/`database-schema.md` ด้วย (เดิมตรวจ AC/Test
  Plan/Test Case/Prototype/Architecture)
- ยังไม่ได้รัน `api-db-spec-builder` จริง — สร้างแค่ตัว skill/agent ตามที่ผู้ใช้ขอ ยังไม่มีเอกสาร
  `api-spec.md`/`database-schema.md` เกิดขึ้นในรอบนี้

### สร้างเอกสาร API Spec + Database Schema ครั้งแรก (`api-db-spec-builder`)

- ผ่านขั้นตอนที่ -1 (HLA มีอยู่แล้ว) — รันครั้งแรก ไม่มีเอกสารเดิมให้ audit เก็บข้อมูลจาก
  `high-level-architecture.md` ทั้ง 7 component + 14 data entity ก่อนเสนอโครงเนื้อหา
- ผู้ใช้ปรับแผนที่เสนอไป 2 จุด (เลือกตัวเลือกที่แนะนำทั้งคู่ในรอบถามเพิ่ม):
  1. **Streak (PLN-4) และ Weight Goal/Forecast (INT-1)**: มีตาราง cache/snapshot แยก
     (`streak_snapshot`, `weight_forecast_snapshot`) แทนการคำนวณ on-demand ที่เสนอไปตอนแรก — เพื่อให้
     ตรงกับที่ HLA อธิบายทั้งสอง entity เป็น "ค่าปัจจุบัน 1 ค่าต่อ User Profile" มากกว่า
  2. **session_rejected_video** (REC-3): persist เป็นตารางแยกแทนการเป็น ephemeral request-scoped —
     เพื่อ normalization มาตรฐานและเก็บ audit trail ของการ swap
- สร้าง `docs/02-design/02-technical/api-spec.md` — 22 operation จัดกลุ่มตาม 7 component ของ HLA
  (REST-style convention: HTTP verb + resource path เชิงแนวคิด, ไม่มี actual domain/auth mechanism)
- สร้าง `docs/02-design/02-technical/database-schema.md` — 15 ตาราง (ครอบคลุมทั้ง 14 conceptual data
  entity ของ HLA แบบ 1:1 ยกเว้น `session_rejected_video` ที่เป็นส่วนขยายของ Workout Session) พร้อม ER
  Diagram (Mermaid `erDiagram`) และระบุชัดว่ากติกาธุรกิจ 6 ข้อ (equipment mutual exclusion, safety
  floor, all-or-nothing, PLN-1 read-only, PLN-2 วันนี้เท่านั้น, streak/forecast sync) enforce ที่ schema
  ไม่ได้ ต้องเป็นหน้าที่ของ component ใดใน HLA
- อัปเดต `docs/02-design/02-technical/index.md` ให้กล่าวถึงทั้งสองไฟล์ใหม่
- self-check แล้วไม่มีคำศัพท์ผูก technical stack หลุดเข้ามานอกเหนือจาก 2 ข้อยกเว้นที่อนุญาต
- คงจุดที่ยังไม่ได้ระบุไว้ในทั้งสองเอกสาร ไม่ฟันธงแทน upstream: REC-1 tolerance, REC-4 warmup/cooldown
  นับ target, INT-1 min log days, INT-2/INT-3 conflict resolution, MET lookup table ค่าจริง, ความถี่
  recompute ของ snapshot ทั้งสอง, data retention

### เพิ่ม skill/agent คู่ใหม่: `detailed-design-builder`/`detailed-design-writer`

- สร้าง `.claude/skills/detailed-design-builder/SKILL.md` และ `.claude/agents/detailed-design-writer.md`
  — ชั้นสุดท้ายก่อนเขียนโค้ดจริง สำหรับสร้าง/อัปเดต/audit เอกสาร **Detailed Design** เชิง conceptual
  (`docs/02-design/02-technical/detailed-design/{epic-slug}.md`, ยังไม่สร้างจริง แค่เตรียม skill/agent
  ไว้) — Sequence Diagram บังคับขั้นต่ำ, State Diagram และอัลกอริทึมหลักเพิ่มตามความเหมาะสม
- ผู้ใช้ยืนยัน 2 จุดก่อนสร้าง (เลือกตัวเลือกที่แนะนำทั้งคู่):
  1. **โครงสร้างไฟล์**: หนึ่งไฟล์ต่อหนึ่ง epic (4 ไฟล์ ตาม slug เดียวกับ `01-spec/`/`test-cases/`) จัดกลุ่ม
     sequence diagram ตาม Feature ID ภายในไฟล์ — เหมือน pattern ของ `test-cases/`
  2. **ขอบเขตเนื้อหา**: นอกจาก sequence flow (บังคับ) ให้มี State Diagram (สำหรับ entity ที่มี state
     transition มีความหมาย) และอัลกอริทึมหลัก (สำหรับ feature ที่มีการคำนวณ เช่น TDEE, MET, safety
     floor, all-or-nothing, streak, forecast) เพิ่มด้วย
- **ไม่ต้องขอข้อยกเว้นใหม่ทาง technical stack** (ต่างจาก `api-db-spec-builder`) เพราะ sequence/state
  diagram และคำอธิบายอัลกอริทึมเป็น notation ที่เป็นกลางทางเทคโนโลยีอยู่แล้วโดยธรรมชาติ — ยึดกติกา
  conceptual เดิมของ HLA เข้มงวดเหมือนเดิม
- **Hard prerequisite ใหม่**: ต้องมีครบทั้ง 3 ไฟล์ (`high-level-architecture.md`, `api-spec.md`,
  `database-schema.md`) ก่อนเสมอ — ถ้าขาดไฟล์ใดไฟล์หนึ่ง หยุดทันทีและบอกให้รัน `architecture-builder`
  แล้วค่อย `api-db-spec-builder` ตามลำดับ (ไม่ได้รับอนุญาตสร้างเองเหมือนเดิม)
- อัปเดต `CLAUDE.md`: เพิ่มแถวในตาราง "Skills & agents at a glance" (6→7 คู่), เพิ่ม section "Building
  the Detailed Design docs" ใหม่, ปรับ Documentation structure ให้กล่าวถึงโฟลเดอร์ `detailed-design/`
- ปรับ `feature-list-journey`'s SKILL.md และ `feature-journey-writer` agent ให้ขั้นตอนที่ 0.5 (ตรวจ
  ดาวน์สตรีมว่า fresh หรือไม่) ครอบคลุม `detailed-design/*.md` ด้วย (เดิมตรวจ AC/Test Plan/Test
  Case/Prototype/Architecture/API-DB Spec)
- ยังไม่ได้รัน `detailed-design-builder` จริง — สร้างแค่ตัว skill/agent ตามที่ผู้ใช้ขอ ยังไม่มีเอกสาร
  `detailed-design/*.md` เกิดขึ้นในรอบนี้

### สร้างเอกสาร Detailed Design ครั้งแรก (`detailed-design-builder`)

- ผ่านขั้นตอนที่ -1 (HLA/API Spec/Database Schema มีครบทั้ง 3 ไฟล์แล้ว) — รันครั้งแรก ไม่มีเอกสารเดิมให้
  audit เก็บข้อมูลจากทั้ง 3 เอกสารแล้วเสนอโครงเนื้อหา ผู้ใช้ยืนยันแผนตามที่เสนอไม่มีการปรับ
- สร้าง `docs/02-design/02-technical/detailed-design/` (โฟลเดอร์ใหม่) พร้อม 4 ไฟล์ (1 ต่อ epic):
  `onboarding-personalization.md`, `daily-youtube-recommendation.md`, `planner-logging.md`,
  `smart-integrations.md`
- **Sequence Diagram**: ครบทั้ง 13 Feature ID (Mermaid `sequenceDiagram`) — ทุกอันอ้าง operation จริงจาก
  `api-spec.md` (verb+path) และตารางจริงจาก `database-schema.md` เมื่อมีการอ่าน/เขียนข้อมูล มี alt/opt
  block แสดง error/edge case อย่างน้อย 1 กรณีต่อ feature
- **State Diagram**: 2 ตัว — Workout Session (`workout_session.status`, ใน
  `daily-youtube-recommendation.md`) และ Integration Connection (`integration_connection.connection_status`,
  ใน `smart-integrations.md`, ใช้ร่วมกันระหว่าง INT-2/INT-3)
- **อัลกอริทึมหลัก**: 8 feature — ONB-1 (BMR/TDEE), ONB-3 (target kcal + safety floor), REC-1 (จับคู่
  วิดีโอ + ขยายเกณฑ์ค้นหา), REC-2 (MET + wearable override), PLN-2 (nested read-only/completed-wins
  check), PLN-3 (all-or-nothing evaluation), PLN-4 (streak walk-back), INT-1 (forecast calculation)
- อัปเดต `docs/02-design/02-technical/index.md` ให้กล่าวถึงโฟลเดอร์ `detailed-design/` ใหม่
- self-check แล้วไม่มีคำศัพท์ผูก technical stack หลุดเข้ามา (ไม่ต้องมีข้อยกเว้นใหม่ เพราะ sequence/state
  diagram และ pseudocode เป็นกลางทางเทคโนโลยีอยู่แล้วตามที่ skill ระบุไว้)
- คงจุดที่ยังไม่ได้ระบุไว้ในทุกไฟล์ ไม่ฟันธงแทน upstream: Activity Factor ต่อระดับ, safety floor ตัวเลข
  แน่นอน, REC-1 tolerance, REC-4 warmup/cooldown นับ target หรือไม่, MET lookup table ค่าจริง, INT-1 min
  log days, INT-2 multiple weigh-in priority, INT-3 wearable vs MET reconciliation

### เพิ่ม skill/agent คู่ใหม่: `tech-stack-builder`/`tech-stack-writer`

- สร้าง `.claude/skills/tech-stack-builder/SKILL.md` และ `.claude/agents/tech-stack-writer.md` —
  **skill แรกในทั้ง pipeline ที่เลือกเทคโนโลยีจริง** แทนที่จะ conceptual (`docs/02-design/02-technical/tech-stack.md`,
  ยังไม่สร้างจริง แค่เตรียม skill/agent ไว้) — ช่วยเลือก tech stack ที่เหมาะสมผ่านการถามผู้ใช้แบบเข้มข้น
  (Discovery Questionnaire) ก่อนแนะนำเสมอ ตามที่ผู้ใช้ขอ
- ผู้ใช้ยืนยัน 3 จุดก่อนสร้าง (เลือกตัวเลือกที่แนะนำทั้งหมด):
  1. **เอกสารบังคับก่อน**: ต้องมีครบทั้ง 4 เอกสาร (HLA + API Spec + Database Schema + Detailed Design)
     ก่อนจึงจะเริ่มเลือก stack ได้ — เข้มกว่า skill ก่อนหน้าที่บังคับแค่ 1-3 เอกสาร
  2. **Audit behavior ที่ต่างจาก skill อื่นทั้งหมด**: ถ้า audit เจอว่า upstream เปลี่ยนจนอาจกระทบตัวเลือก
     stack จริง **ห้าม auto-update เอง** ต้องถามผู้ใช้ก่อนเสมอ (skill อื่นในโปรเจกต์นี้ auto-update ส่วน
     ที่ "ล้าหลังแต่ไม่ขัดแย้ง" ได้เอง แต่ที่นี่การเปลี่ยน stack จริงมีต้นทุน migrate จริง)
  3. **Discovery Questionnaire**: เต็มชุด 8 หมวด (platform, ทีม, hosting, backend approach, budget/
     scale, timeline, compliance/residency, offline) เฉพาะครั้งแรกที่รัน ครั้งถัดไปถามเฉพาะหมวดที่
     เกี่ยวข้องกับสิ่งที่เปลี่ยน ไม่ถามเต็มชุดซ้ำ
- อัปเดต `CLAUDE.md`: เพิ่มแถวในตาราง "Skills & agents at a glance" (7→8 คู่), เพิ่ม section "Building
  the Tech Stack doc" ใหม่ (อธิบายว่าเป็นข้อยกเว้นเดียวที่ stack-specific จริง และไม่ auto-fix drift),
  ปรับ Documentation pipeline status ให้กล่าวถึง `tech-stack.md` ว่ามี skill พร้อมแล้วแต่ยังไม่สร้างจริง
- ปรับ `feature-list-journey`'s SKILL.md และ `feature-journey-writer` agent ให้ขั้นตอนที่ 0.5 (ตรวจ
  ดาวน์สตรีมว่า fresh หรือไม่) ครอบคลุม `tech-stack.md` ด้วย รวมทั้งเพิ่มหมายเหตุว่าถ้า `tech-stack.md`
  หลุด fresh ต้องบอกผู้ใช้ว่า `tech-stack-builder` จะไม่แก้ตัวเลือก stack เองแม้จะรู้คำตอบ (พฤติกรรมที่
  ตั้งใจ ไม่ใช่ข้อจำกัด)
- อัปเดต `docs/02-design/02-technical/index.md` ให้กล่าวถึง `tech-stack.md`
- ยังไม่ได้รัน `tech-stack-builder` จริง — สร้างแค่ตัว skill/agent ตามที่ผู้ใช้ขอ ยังไม่มีเอกสาร
  `tech-stack.md` หรือ Discovery Questionnaire เกิดขึ้นในรอบนี้

### สร้างเอกสาร Tech Stack ครั้งแรก (`tech-stack-builder`)

- ผ่านขั้นตอนที่ -1 (HLA/API Spec/Database Schema/Detailed Design มีครบทั้ง 4 แล้ว) — รันครั้งแรก ไม่มี
  เอกสารเดิมให้ audit
- รัน **Discovery Questionnaire เต็มชุด 8 หมวด** ผ่าน AskUserQuestion 2 รอบ: Platform targets (iOS +
  Android + Web), Team background (JavaScript/TypeScript), Hosting/Infra preference (ไม่มี preference
  ให้แนะนำ), Backend approach (Backend-as-a-Service), Budget/Scale (งบจำกัดมาก/MVP), Timeline (ปานกลาง
  3-6 เดือน), Data compliance/residency (ไม่มี residency เฉพาะ แต่ทำตาม PDPA มาตรฐาน), Offline support
  (ไม่จำเป็น)
- ถามเพิ่ม 2 จุดตัดสินใจที่มีทางเลือกสำคัญมากกว่า 1 ทาง (ตามกติกา skill — ไม่เดาเอง): **Mobile/Client
  framework** → ผู้ใช้เลือก React Native + Expo (เทียบกับ Flutter, Native แยก 3 codebase) **Backend &
  Database (BaaS)** → ผู้ใช้เลือก Supabase (เทียบกับ Firebase, AWS Amplify)
- สร้าง `docs/02-design/02-technical/tech-stack.md` ครบ 8 หัวข้อตามโครงสร้างบังคับ: Recommended Tech
  Stack ต่อองค์ประกอบ (Mobile/Client, Backend, Database, Auth, Hosting, Third-party Integration,
  CI/CD), Rationale ผูกกับคำตอบ Discovery + NFR-01/03 (client-side calculation), Alternatives
  Considered (≥3 ทางต่อ 2 จุดตัดสินใจหลัก), และ **Mapping จาก Conceptual Docs → Concrete Stack** (HLA
  component → Supabase implementation, logical type → PostgreSQL type, REST convention →
  PostgREST/Edge Function)
- อัปเดต `docs/02-design/02-technical/index.md` และ `CLAUDE.md`'s Documentation pipeline status ให้ย้าย
  `tech-stack.md` จาก "scaffolded" เป็น "populated" พร้อมระบุ stack ที่เลือก
- self-check แล้วไม่มี corruption/ตัวอักษรแปลกปลอมในเอกสาร
- คงจุดที่ยังไม่ได้ตัดสินใจไว้ 4 ข้อในเอกสาร: Supabase region ที่แน่นอน, HealthKit/Health Connect
  library ที่แน่นอน, ขอบเขต feature ของ web app (Bluetooth/HealthKit ใช้ไม่ได้บนเว็บ), และ Open Points
  เดิมของ conceptual docs (Activity Factor, MET lookup table, REC-1 tolerance, INT-1 min log days)

### ปรับปรุง 4 skill/agent คู่ (architecture/api-db-spec/detailed-design/tech-stack) ให้ลงรายละเอียดจาก tech-stack.md — เพิ่ม "ภาคผนวก: Stack Mapping"

- ผู้ใช้ขอให้ปรับปรุง `architecture-builder`, `api-db-spec-builder`, `detailed-design-builder` ให้
  "เหมาะสมและลงรายละเอียดยิ่งขึ้น" จากข้อมูล `tech-stack.md` ที่มีแล้ว — เกิดความขัดแย้งกับกติกาเดิมของทั้ง 3
  skill ที่ห้ามผูก technical stack เข้มงวดมาตลอดทั้งเซสชัน จึงถามผู้ใช้ 3 แนวทางก่อนแก้ไฟล์ใดๆ: (A) เพิ่ม
  "ภาคผนวก Stack Mapping" ท้ายเอกสาร เนื้อหาหลักยัง conceptual (B) แทรกชื่อ stack เข้าเนื้อหาหลักโดยตรง (ละทิ้ง
  หลักการเดิม) (C) ตรวจ audit อย่างเดียวไม่เพิ่มเนื้อหาใหม่ — ผู้ใช้เลือก **(A) เพิ่มภาคผนวก (แนะนำ)**
- แก้ทั้ง 8 ไฟล์ skill/agent ให้รองรับ "ภาคผนวก: Stack Mapping" เป็น section สุดท้ายของแต่ละเอกสารเมื่อ
  `tech-stack.md` มีอยู่แล้ว (skip ไม่ใช่ gap ถ้ายังไม่มี): ข้อยกเว้นเดียว/เพิ่มของกติกา no-stack-names เดิม,
  audit item ใหม่ "Stack Mapping Appendix freshness" (auto-fix ได้เองไม่ต้องถามผู้ใช้ เพราะเป็นการมิเรอร์
  ข้อเท็จจริงที่ตัดสินใจแล้วที่อื่น ไม่ใช่การตัดสินใจเนื้อหาใหม่ — ต่างจาก "เอกสารมีข้อมูลใหม่"/"ขัดแย้ง" ที่ยัง
  ต้องถามเหมือนเดิม), item ใหม่ในโครงสร้างเอกสารบังคับ, และ write-rule ตรวจก่อนเขียน/ข้ามภาคผนวก:
  - `architecture-builder`/`architecture-writer`: หัวข้อ 11 มิเรอร์ `tech-stack.md` § 6.1 (Component →
    concrete implementation) ทั้งหมด
  - `api-db-spec-builder`/`api-db-spec-writer`: `api-spec.md` มิเรอร์ § 6.3 (REST convention → Supabase
    routing), `database-schema.md` มิเรอร์ § 6.2 (logical type → PostgreSQL type) — คนละภาคผนวกต่อไฟล์
  - `detailed-design-builder`/`detailed-design-writer`: ภาคผนวกแรกที่ skill นี้ต้องมี (เดิมไม่ต้องมีข้อ
    ยกเว้นเลยเพราะ notation เป็นกลางทางเทคโนโลยีอยู่แล้ว) — ต่อไฟล์ epic มิเรอร์เฉพาะ Component ที่ปรากฏใน
    ไฟล์นั้นจาก § 6.1 บวกระบุว่า algorithm section แต่ละตัว execution จริงอยู่ฝั่งไหน (client-side/Edge
    Function) อ้างจาก § 4's NFR-01/03 rationale
  - `tech-stack-builder`/`tech-stack-writer`: เพิ่ม "การรับรู้แบบสองทิศทาง" — ถ้าการแก้ § 6 เปลี่ยน
    mapping จริง ต้องรายงานว่าภาคผนวกของเอกสารไหนอาจ stale และแนะนำให้รัน skill เจ้าของเอกสารนั้นต่อ (ห้าม
    แก้เอง)
- อัปเดต `CLAUDE.md`: เพิ่มย่อหน้า "Stack Mapping Appendix" ใน 3 section ("Building the High Level
  Architecture doc", "Building the API Spec & Database Schema", "Building the Detailed Design docs")
  และย่อหน้า "Bidirectional awareness" ใน "Building the Tech Stack doc"
- **ลงมือเขียนภาคผนวกจริงลงในเอกสารทั้ง 7 ไฟล์ที่มีอยู่แล้ว** (ไม่ใช่แค่เตรียม skill ไว้เฉยๆ เหมือนรอบก่อนๆ):
  `high-level-architecture.md` (หัวข้อ 10, มิเรอร์ § 6.1 เต็ม), `api-spec.md` (หัวข้อ 6, มิเรอร์ § 6.3),
  `database-schema.md` (หัวข้อ 8, มิเรอร์ § 6.2), และทั้ง 4 ไฟล์ใน `detailed-design/` (มิเรอร์ Component ที่
  เกี่ยวข้องต่อไฟล์ + execution ของ algorithm — สังเกตว่า `tech-stack.md` § 6.1 ใช้คำว่า "Postgres function
  หรือ Edge Function" สำหรับ Logging & Streak โดยไม่ฟันธง จึงระบุความไม่แน่นอนนี้ไว้ตรงๆ ใน
  `planner-logging.md` แทนการเดาแทน)
- อัปเดต `docs/02-design/02-technical/index.md` ให้กล่าวถึง convention ของภาคผนวกใหม่นี้
- ยังไม่ได้ commit/push — รอผู้ใช้ยืนยันรอบนี้

### NFR Gap Analysis + เพิ่ม NFR-09/10/11 (`test-suite-builder`)

- ผู้ใช้ขอให้วิเคราะห์ว่าควรมี Non-Functional Requirement อะไรเพิ่มเติมบ้าง โดยเทียบกับทุกเอกสารในไปป์ไลน์
  (requirement, backlog, feature list, test case, test report, HLA, detailed design) — **ยังไม่สร้าง
  เอกสารในขั้นแรก** นำเสนอผลวิเคราะห์เป็นตารางในแชทให้รีวิวก่อนตามที่ขอ พบว่า test report
  (`docs/03-testing/02-test-result/`) ยังเป็น scaffold ล้วน ไม่มีข้อมูลจริงให้ใช้
- ผลวิเคราะห์แบ่งเป็น 4 กลุ่ม: (ก) แนะนำเพิ่มทันทีเพราะมีหลักฐานรองรับอยู่แล้วใน DESIGN.md (Accessibility
  §4.3, Localization §4.5 — pattern เดียวกับที่ NFR-02 เคย mirror §4.6) (ข) มีทางเลือกจริงต้องถาม (Web app
  feature parity ของ Epic 4, PDPA compliance formalization) (ค) ต้องการข้อมูลเพิ่มห้ามเดา (Scalability/
  Capacity target — ไม่มีตัวเลขธุรกิจให้อ้างอิง) (ง) พิจารณาแล้วไม่ต้องเพิ่มตอนนี้ (Dark Mode, Observability,
  Maintainability — มีเหตุผลรองรับจากสถานะโปรเจกต์ปัจจุบัน)
- ถาม AskUserQuestion 4 ข้อ ผู้ใช้เลือกตัวเลือกที่แนะนำทั้งหมด: (1) เพิ่มทั้ง Accessibility+Localization (2)
  Web parity = core loop parity เต็ม + Epic 4 mobile-only โดยตั้งใจ (3) แยก PDPA เป็น NFR เดี่ยว (4) ยังไม่
  กำหนด Scalability target ตอนนี้
- ผู้ใช้ยืนยันให้เริ่มเขียนจริง — เรียก `test-suite-builder` เพื่ออัปเดต NFR doc ที่มีอยู่แล้ว (ไม่ใช่สร้าง
  ใหม่) และ cascade ไปยัง `test-plan.md`:
  - `docs/01-requirements/01-spec/20260827-05-non-functional-requirements.md`: เพิ่ม 2 หมวดใหม่ (Usability,
    Legal/Regulatory Compliance) รวม 3 ข้อ — **NFR-09 Accessibility** (mirror DESIGN.md §4.3: touch
    target 44×44px, WCAG AA contrast, ห้ามสื่อสถานะด้วยสีอย่างเดียว, font scaling 150%), **NFR-10
    Localization** (mirror §4.5: ภาษาไทยเป็นหลัก ทับศัพท์เทคนิคได้ — คงรูปแบบวันที่ ค.ศ./พ.ศ. เป็น open
    point เดิม) และ **NFR-11 PDPA Compliance** (แยกจาก NFR-04/06 แบบ implicit เดิม — ครอบคลุม consent
    record-keeping, สิทธิ์เจ้าของข้อมูล, breach notification, อ้างอิง Discovery Questionnaire ใน
    tech-stack.md §2) — ขยาย Scope section จาก 3 เป็น 5 หมวด, เพิ่ม 3 ข้อใน Acceptance Criteria checklist,
    บันทึก decision การขยาย scope และ decision เรื่อง web app parity (cross-reference เท่านั้น ไม่ใช่ NFR
    ใหม่ในเอกสารนี้ — ส่งต่อให้ `architecture-builder` บันทึกจริงใน HLA) ไว้ใน § ข้อสมมติฐาน/การตัดสินใจ,
    เพิ่ม open point ของ NFR-10/NFR-11 และหมายเหตุถึง `feature-list-journey` ว่าต้องอัปเดตตาราง "NFR
    Traceability" ใน `backlog.md` ต่อ (skill นี้ไม่มีสิทธิ์แก้ `backlog.md` เอง)
  - `docs/03-testing/01-test-plan/test-plan.md`: ปรับทุกจุดที่อ้าง "NFR-01–NFR-08"/"8 หมวด" เป็น
    "NFR-01–NFR-11" (header, §2 NFR-driven Testing, เอกสารอ้างอิงท้ายไฟล์), เพิ่ม NFR-11 เข้าแถว
    Backend/ระบบบัญชีผู้ใช้ใน §3 Mock/Stub table, เพิ่มความเสี่ยงใหม่ **R10** (NFR-10 open point เรื่อง
    รูปแบบวันที่) และ **R11** (NFR-11 พึ่ง backend จริง เหมือน pattern ของ R7) ใน §4, ปรับ §5 Exit
    Criteria ข้อ 4 ให้ระบุว่า NFR-09/NFR-10 ตรวจสอบได้จริงจาก prototype ส่วน NFR-11 อยู่กลุ่ม "not testable
    in this round" เหมือน NFR-04/06/08
  - ไม่แตะ `acceptance-criteria.md`/`test-cases/*.md` เพราะ NFR ไม่เคยมี AC-xxx/TC-xxx แยกเป็นรายการของ
    ตัวเอง (ตรวจสอบแล้วด้วย grep — ไม่มี reference ถึง "NFR" ในทั้งสองที่เลย) จึงไม่มี fact drift ให้แก้
- ยังไม่ได้ commit/push รอบนี้

### ปิดงานคั่งค้าง 2 จุดจาก NFR gap analysis (`feature-list-journey` + `architecture-builder`)

- **`feature-list-journey`** (ขอบเขตแคบ: เฉพาะตาราง NFR Traceability): เพิ่ม 3 แถวใหม่ (NFR-09 Usability/
  Accessibility, NFR-10 Usability/Localization — ทั้งคู่ผูกกับทุก Feature ID เพราะเป็น UI-level
  cross-cutting, NFR-11 Legal/Regulatory Compliance — ผูกกับ ONB-1/2/3, INT-2/3 เหมือน pattern ของ
  NFR-04/06) เข้าตาราง "NFR Traceability" ใน `docs/01-requirements/backlog.md` พร้อมอัปเดตข้อความอ้างอิง
  จำนวน NFR จาก 8 เป็น 11 — ไม่พบ drift อื่นในขอบเขตที่จำกัดนี้ (ไม่ได้รัน full audit ทั้ง 3 ชั้น)
- **`architecture-builder`** (ขอบเขตแคบ: เฉพาะการบันทึก decision เดียว): เพิ่มหมายเหตุ "แพลตฟอร์มที่รองรับ"
  ในหัวข้อ 6.2 (Apple Health/Google Health Connect) และ 6.3 (ตาชั่งอัจฉริยะ Bluetooth) ของ
  `high-level-architecture.md` — ระบุชัดว่าทั้งสอง external integration boundary ใช้ได้เฉพาะบน mobile
  client (iOS/Android) เท่านั้น เว็บแสดง "ไม่รองรับ" อย่างสงบ อ้างอิง NFR-07 (fallback) เป็นหลักการรองรับ —
  resolve open point §7 ข้อ 3 ของ `tech-stack.md` เรื่อง web app scope ให้เสร็จสมบูรณ์
- ทั้งสองงานเป็นการ apply decision ที่ผู้ใช้ยืนยันแล้วผ่าน AskUserQuestion ก่อนหน้านี้ ไม่มีคำถามใหม่เกิดขึ้น
  ระหว่างทำ
- ยังไม่ได้ commit/push รอบนี้
