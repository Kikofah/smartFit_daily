# Log — 2026-08-30

## `architecture-builder` — Architecture Consistency Audit + update `high-level-architecture.md`

**บริบท**: `feature-journey-writer` เพิ่งอัปเดต `backlog.md`, `user-journeys.md`, และ 2 ไฟล์ `01-spec/*.md`
(Onboarding & Personalization, Smart Integrations) ตามการตรวจสอบ codebase จริงหลัง re-architecture เมื่อ
2026-08-29 (Express + web-first, drop Firebase Cloud Functions) พบ 2 การเปลี่ยนแปลงที่กระทบ
`high-level-architecture.md` และส่งต่อให้ `architecture-builder` แก้ต่อ

**ผล Architecture Consistency Audit**: เทียบกับ Requirement (`01-spec/` 2 ไฟล์ที่เพิ่งแก้ + NFR doc),
Backlog/Feature List, และ User Journey — พบ 2 จุด **เอกสารล้าหลัง (ไม่ขัดแย้ง แค่เก่า)** แก้ผ่าน flow ปกติ
ได้เองไม่ต้องถามผู้ใช้:

1. **ONB-0 เป็น web-only** — component "Account & Session Management" (§3.1) เดิมไม่ได้ระบุข้อจำกัดนี้เลย
   ทั้งที่ codebase จริงมีเฉพาะเว็บแอปที่มีหน้าจอ auth (`apps/mobile/` ตัดเหลือแค่ device-pairing)
2. **กลไก pairing-code (identity handoff) ใหม่** — precondition ทางเทคนิค implicit ของ INT-2/INT-3 (map
   เป็น implicit precondition ของ REQ-12/REQ-13 เท่านั้น ยังไม่มี REQ number formal ของตัวเอง ตามที่
   product owner ยังไม่ตัดสินใจ) ที่ HLA เดิมไม่มี component interaction/data flow ใดครอบคลุมเลย

audit หัวข้ออื่นทั้งหมด (§2, §6 boundaries, §9 relationships) แล้วไม่พบข้อขัดแย้งอื่นเพิ่มเติม — ไม่มีจุดใด
เข้าเกณฑ์ "ขัดแย้งตรงๆ" หรือ "แนวคิดใหม่ที่ไม่มีใน upstream" จึงไม่ต้องใช้ ask-user protocol

**ไฟล์ที่แก้ไข**: `docs/02-design/02-technical/high-level-architecture.md`

- §3.1 (Account & Session Management): เพิ่มว่าเว็บไคลเอนต์เป็นทางเข้าเดียว (sole entry point) ของ
  credential-based auth ทุกวิธี; เพิ่มหน้าที่ mint/รับแลกรหัสจับคู่อุปกรณ์ชั่วคราวแบบใช้ครั้งเดียว
  (short-lived, single-use pairing credential) เพื่อทำ identity handoff ให้ไคลเอนต์ที่ไม่มีหน้าจอ auth
  ของตัวเอง; เพิ่มความสัมพันธ์กับ Integration Gateway ใน "คุยกับ"
- §3.8 (Integration Gateway): เพิ่มความสัมพันธ์กับ Account & Session Management (รับ identity handoff
  ก่อนเริ่มกระบวนการจับคู่อุปกรณ์จริงของ INT-2/INT-3)
- §4.1 (Flow 1 diagram + คำอธิบาย): ระบุชัดว่า ONB-0 เป็น web client เท่านั้น
- §4.5 (Flow 5 diagram + คำอธิบาย): เพิ่ม subgraph "Identity Handoff — Pairing-Code Mechanism" นำหน้า
  INT-2/INT-3 พร้อม mapping กลับ Step 1-6 ของ INT-2/INT-3 ใน `user-journeys.md` (ทั้งสอง feature มี Step
  1-6 เดียวกันเพราะเป็นกลไกร่วม)
- §5 (Conceptual Data Entities): เพิ่ม entity ใหม่ **Pairing Credential** (รหัสจับคู่อุปกรณ์, เวลาหมดอายุ,
  สถานะใช้แล้ว/ยังไม่ใช้, อ้างอิง User Account) — ไม่มี data type/key ตามกติกา conceptual
- §7 (Cross-cutting Concerns — Security/Privacy): เพิ่มหมายเหตุว่ารหัสจับคู่อุปกรณ์ต้อง short-lived/
  single-use และ NFR-05 ปัจจุบันยังไม่ได้ระบุครอบคลุมกลไกนี้ตรงๆ
- §8 (จุดที่ยังไม่ได้ระบุ): เพิ่มข้อ 7 บันทึกว่ากลไก pairing-code ยังไม่มี REQ number formal และ NFR ที่
  ครอบคลุมความปลอดภัยของมันโดยตรง — รอ product owner/`test-suite-builder` ตัดสินใจต่อ
- Header: อัปเดต "อัปเดตล่าสุด" สรุปการเปลี่ยนแปลงรอบนี้

**ไม่แตะ**: หัวข้อ 10 (ภาคผนวก: Stack Mapping) — `tech-stack.md` เองยังไม่ได้อัปเดตจาก Firebase Cloud
Functions + React Native/Expo เดิมมาเป็น Express + web-first ตามโค้ดจริง (ตาม CLAUDE.md § Docs/code drift)
รอ `tech-stack-builder` เดินกระบวนการถามผู้ใช้ (Discovery Questionnaire แบบ targeted) ก่อน แล้ว
`architecture-builder` จะ sync ภาคผนวกในรอบถัดไป

**ผลกระทบต่อเอกสารอื่น (สำหรับรอบถัดไป)**:

- `api-spec.md`/`database-schema.md` (`api-db-spec-builder`): ต้อง audit ว่าต้องเพิ่ม operation/ตารางใหม่
  รองรับ Pairing Credential entity และ 2 operation ใหม่ (ขอรหัสจับคู่, แลกรหัสเป็น session) หรือไม่ — HLA
  §3.1/§3.8/§4.5/§5 เปลี่ยนแล้ว จึงเป็นเอกสารล้าหลังที่ต้องตามให้ทัน
- `detailed-design/04-smart-integrations.md` (`detailed-design-builder`): ควรมี sequence diagram ใหม่
  สำหรับกลไก pairing-code (หรือเพิ่ม Note ในไดอะแกรมเดิมของ INT-2/INT-3) เมื่อ `api-spec.md` มี operation
  รองรับแล้ว
- Stack Mapping appendix ทั้ง 4 จุด (HLA §10, `api-spec.md` §6, `database-schema.md` §8, แต่ละไฟล์ใน
  `detailed-design/`) ยังอ้างอิง Firebase Cloud Functions/React Native-Expo เดิม — ค้างรอ `tech-stack-builder`
  เดินกระบวนการก่อน (pre-existing drift ที่ `feature-list-journey` พบเช่นกัน ไม่ใช่สิ่งใหม่จากรอบนี้)

**คำถามที่ยังค้างอยู่**: ไม่มี — ทั้ง 2 จุดที่พบเป็นเอกสารล้าหลัง แก้ได้ผ่าน flow ปกติ ไม่เข้าเกณฑ์ ask-user
protocol

## `api-db-spec-builder` — Reconcile pairing-code redeem behavior กับโค้ดจริง + Stack Mapping re-sync

**บริบท**: `tech-stack-writer` เพิ่ง reconcile `tech-stack.md` (บรรทัดด้านบน) และระหว่างนั้นได้อ่านโค้ดจริง
`apps/web/server/routes/pairing/index.ts` ตรง พบว่าพฤติกรรมจริงของ `POST /auth/pairing-codes/redeem` ไม่
ตรงกับที่ `api-spec.md`/`database-schema.md` เคยออกแบบไว้ในรอบก่อนหน้าของวันเดียวกัน (ตอนบรีฟกลไกนี้ครั้งแรก
ระบุ error case เป็น `404`/`409`/`422` และให้ตาราง `pairing_credential` มี `is_used` boolean) — ส่งต่องานนี้
ให้ `api-db-spec-builder` reconcile

**ลักษณะงาน**: แบ่งเป็น 2 ส่วนตามกติกาต่างกัน — (1) แก้เนื้อหาหลักให้ตรงกับโค้ดจริงที่ shipped ไปแล้ว
(ยึดหลัก CLAUDE.md § "Docs/code drift": ปกติ code ต้องตรง docs แต่กลไกนี้เพิ่งถูกคิดขึ้นในเอกสารช่วง session
เดียวกัน โค้ดจริงจึงเป็นสัญญาณที่น่าเชื่อถือกว่าว่าทีมตัดสินใจอะไรจริง — เป็นการแก้ข้อเท็จจริง ไม่ใช่ ask-user
protocol) (2) mechanical re-sync ภาคผนวก Stack Mapping ตามกติกาปกติ (auto-fixable ไม่ต้องถามผู้ใช้)

**สิ่งที่ยืนยันจากโค้ดจริง** (`apps/web/server/routes/pairing/index.ts`): `POST /api/pairing/redeem` อ่าน
`pairingCodes/{code}`, ถ้าไม่มี document หรือ `data.expiresAt < now` คืน **`410 Gone` เดียว** ("This code is
invalid or has expired.") ไม่แยก 404/409/422 — ถ้าสำเร็จเรียก `ref.delete()` (ลบ document ทิ้งถาวร) **ก่อน**
เรียก `auth.createCustomToken(data.uid)` — ไม่มี field ใดเทียบเท่า `is_used` เลยในโค้ด (ลบแทนการตั้ง flag)

**ไฟล์ที่แก้ไข**:

- `docs/02-design/02-technical/api-spec.md`
  - §2 (Conventions, Status code): เพิ่ม `410` เข้ารายการ พร้อมคำอธิบายว่าใช้เฉพาะ
    `POST /auth/pairing-codes/redeem`
  - §3.1: แก้แถว `POST /auth/pairing-codes/redeem` — เปลี่ยน error/edge case จาก 3 กรณีแยก (`404`/`409`/
    `422`) เป็น **`410` กรณีเดียว** ครอบคลุมทั้งไม่พบ/หมดอายุ/ถูกใช้แล้ว พร้อมอธิบายเหตุผล (delete-on-redeem
    ทำให้ 3 กรณีแยกไม่ออก)
  - §6/§6.3/§6.3.1 (ภาคผนวก: Stack Mapping): เขียนใหม่ทั้งหมดให้มิเรอร์ `tech-stack.md` §6.3/§6.3.1 ฉบับ
    Express.js บน Google Cloud Run — 10 operation ครบ (7 client SDK ตรง + 3 Express route: forgot-password,
    pairing-codes, pairing-codes/redeem) พร้อมหมายเหตุ `410` ตรงกับพฤติกรรมจริงในแถว redeem
  - Header: อัปเดต "อัปเดตล่าสุด" (รอบ 5) สรุปการเปลี่ยนแปลงทั้งสองส่วน
- `docs/02-design/02-technical/database-schema.md`
  - §2 (ER Diagram): ลบ `boolean is_used` ออกจาก entity `PAIRING_CREDENTIAL`
  - §3.17: ลบ column `is_used` ออกจากตาราง พร้อมเขียนหมายเหตุใหม่อธิบาย delete-on-redeem แทน (คงชื่อ field
    อื่นเป็น snake_case ตามธรรมเนียม logical model เดิมของทั้งไฟล์ — ไม่เปลี่ยนเป็น camelCase เพราะนั่นเป็น
    ธรรมเนียมของภาคผนวก §8 Firestore mapping เท่านั้น ไม่ใช่ของหัวข้อ 1-7)
  - §4 ข้อ 8: แก้กติกาธุรกิจให้อธิบาย delete-on-redeem แทนการอ้าง `is_used` flag
  - §8.1/§8.2/§8.3 (ภาคผนวก: Stack Mapping): mechanical re-sync ทั้งหมดให้มิเรอร์ `tech-stack.md`
    §6.1/§6.2/§6.3 ฉบับ Express.js บน Google Cloud Run — ทุก "Cloud Function" เปลี่ยนเป็น "Express route",
    เพิ่มแถว `pairing_credential` ใหม่ในทั้ง §8.2 (top-level collection `pairingCodes/{code}`, ไม่มี field
    เทียบเท่า `is_used`) และ §8.3 (enforcement ผ่าน Express route `POST /api/pairing/redeem`'s
    `ref.delete()`)
  - Header: อัปเดต "อัปเดตล่าสุด" (รอบ 5) สรุปการเปลี่ยนแปลงทั้งสองส่วน

**ไม่แตะ**: `high-level-architecture.md` (ไม่กระทบ — entity/component เดิมยังถูกต้องเชิงแนวคิด ไม่มี field
ระดับ schema ให้แก้), `tech-stack.md` (เจ้าของคือ `tech-stack-writer` เอง — เพียงอ่านเป็น read-only upstream)

**ผลกระทบต่อเอกสารอื่น (สำหรับรอบถัดไป)**: `detailed-design/04-smart-integrations.md` ยังมี alt block
`404`/`409`/`422` เดิมของ sequence diagram กลไก pairing-code ที่ไม่ตรงกับ `api-spec.md` ฉบับนี้อีกต่อไป (ควร
เป็น `410` เดียว) และภาคผนวก Stack Mapping ของทุกไฟล์ใน `detailed-design/` ยังอ้าง Cloud Function เดิม/นับ 8
operation — ควรรัน `detailed-design-builder` ต่อ

**คำถามที่ยังค้างอยู่**: ไม่มี — งานนี้เป็นการ reconcile ข้อเท็จจริงจากโค้ดที่ shipped แล้ว บวก mechanical
re-sync ล้วน ไม่เข้าเกณฑ์ ask-user protocol

## `detailed-design-builder` — Detailed Design Consistency Audit + update `detailed-design/*.md` (ทั้ง 4 ไฟล์)

**บริบท**: `architecture-builder`/`api-db-spec-builder` เพิ่งอัปเดต `high-level-architecture.md` (§3.1,
§3.8, §4.5, §5) และ `api-spec.md`/`database-schema.md` (2 operations ใหม่ `POST /auth/pairing-codes`,
`POST /auth/pairing-codes/redeem`; ตารางใหม่ `pairing_credential` §3.17) รองรับกลไก **Identity Handoff —
Pairing-Code Mechanism** ข้างบน — ส่งต่อให้ `detailed-design-builder` เพิ่ม sequence diagram รองรับ

**ผล Detailed Design Consistency Audit**: เทียบทั้ง 4 ไฟล์ epic กับ HLA/API Spec/Database Schema ฉบับ
ล่าสุด, `01-spec/` (รวม NFR), `backlog.md`, `user-journeys.md` — พบ 2 จุด **เอกสารล้าหลัง (ไม่ขัดแย้ง แค่
เก่า)** แก้ผ่าน flow ปกติได้เองไม่ต้องถามผู้ใช้ ทั้งสองจุดอยู่ใน `04-smart-integrations.md` และ
`01-onboarding-personalization.md`:

1. **`04-smart-integrations.md` ไม่มี sequence diagram รองรับกลไก Identity Handoff — Pairing-Code
   Mechanism เลย** ทั้งที่เป็น precondition ทางเทคนิคของ INT-2/INT-3 ที่เพิ่งเพิ่มใน HLA/API Spec/Database
   Schema ครบแล้ว
2. **`01-onboarding-personalization.md`'s ONB-0 sequence diagrams ใช้ `actor U as ผู้ใช้` แบบ generic**
   ไม่ได้ระบุชัดว่าเป็นเว็บไคลเอนต์เท่านั้น — อ่านกำกวมได้ว่าเป็นไคลเอนต์ใดก็ได้ ทั้งที่ HLA §3.1 ยืนยันชัดว่า
   เว็บไคลเอนต์เป็น sole entry point ของ credential-based auth ทุกวิธี (โดยเฉพาะตอนนี้ที่มีไคลเอนต์อื่น —
   companion app ของ INT-2/INT-3 — อยู่ในระบบแล้วซึ่งไม่มีหน้าจอ auth ของตัวเองเลย ทำให้ความกำกวมนี้สำคัญ
   ขึ้น)

audit หัวข้ออื่นทั้งหมดของทั้ง 4 ไฟล์ (State Diagram, algorithm sections, ความสัมพันธ์กับเอกสารอื่น) แล้ว
ไม่พบข้อขัดแย้งหรือแนวคิดใหม่ที่ upstream ไม่รองรับ — ไม่เข้าเกณฑ์ ask-user protocol

**ตรวจ main-body stack-name violation ตามที่ผู้ใช้ระบุ (grep พบ "Firebase Cloud Function"/"Cloud Function"
ในทั้ง 4 ไฟล์)**: อ่านตำแหน่งจริงของทุกจุดที่ grep เจอในทั้ง 4 ไฟล์แล้วยืนยันว่า**ทุกจุดอยู่ภายใน "##
ภาคผนวก: Stack Mapping" เท่านั้น** (หลัง header ของ section นั้นเสมอ) — **ไม่พบ main-body violation ในไฟล์
ใดเลย** ไม่ต้องแก้อะไรเพิ่มในส่วนนี้

**ไฟล์ที่แก้ไข**:

- `docs/02-design/02-technical/detailed-design/04-smart-integrations.md`
  - เพิ่ม section ใหม่ **"Identity Handoff — Pairing-Code Mechanism (precondition ของ INT-2, INT-3)"**
    ก่อน INT-2 — 1 sequence diagram (participant: ผู้ใช้, เว็บไคลเอนต์, Account & Session Management,
    ไคลเอนต์ที่ไม่มีหน้าจอ auth ของตัวเอง/companion app, Integration Gateway) ครอบคลุม mint
    (`POST /auth/pairing-codes`) → แสดงรหัสบนเว็บ → กรอกบนมือถือ → redeem
    (`POST /auth/pairing-codes/redeem`) พร้อม alt block ครบ 3 edge case (`404`/`409`/`422` ตาม
    `api-spec.md` §3.1) → ส่งต่อเข้า Integration Gateway — ไม่มี state diagram แยก (`is_used` เป็น
    `boolean` ไม่ใช่ `enum` หลายค่า ไม่เข้าเกณฑ์) ไม่มี algorithm แยก (เป็น lookup ตรงไปตรงมา)
  - เพิ่ม `Note` precondition ชี้กลับมาที่ diagram ใหม่นี้ ในทั้ง sequence diagram ของ INT-2 และ INT-3
  - อัปเดตหัวข้อ "ความสัมพันธ์กับเอกสารอื่น" ให้รวม HLA §3.1/§4.5/entity Pairing Credential, `api-spec.md`
    §3.1 (2 operation ใหม่), `database-schema.md` §3.17 `pairing_credential` — พร้อมแก้เลขหัวข้อ HLA/API
    Spec ที่ค้างจากการ renumber Account & Session Management เข้ามาเป็น §3.1 (เดิมไฟล์นี้ยังอ้าง "3.6
    Insights & Forecast, 3.7 Integration Gateway" ที่ล้าหลัง — แก้เป็น §3.7/§3.8 ที่ถูกต้อง)
  - Header: อัปเดต "อัปเดตล่าสุด" สรุปการเปลี่ยนแปลงรอบนี้
- `docs/02-design/02-technical/detailed-design/01-onboarding-personalization.md`
  - แก้ actor declaration ในทั้ง 3 sequence diagram ของ ONB-0 จาก `actor U as ผู้ใช้` เป็น
    `actor U as ผู้ใช้ (เว็บไคลเอนต์เท่านั้น — sole entry point ตาม HLA §3.1)`
  - เพิ่มหมายเหตุ cross-reference ในส่วนนำของ ONB-0 ชี้ไปยัง sequence diagram ของกลไก pairing-code ใน
    `04-smart-integrations.md` แทน (เพราะเป็น precondition ของ Epic 4 ไม่ใช่ของ 4 REQ ของ ONB-0 เอง)
  - อัปเดตหัวข้อ "ความสัมพันธ์กับเอกสารอื่น" ให้กล่าวถึง operation ใหม่ 2 ตัวใน `api-spec.md` §3.1 พร้อม
    cross-reference ไปยัง `04-smart-integrations.md`
  - Header: อัปเดต "อัปเดตล่าสุด" สรุปการเปลี่ยนแปลงรอบนี้
- `docs/02-design/02-technical/detailed-design/02-daily-youtube-recommendation.md`,
  `docs/02-design/02-technical/detailed-design/03-planner-logging.md`
  - ไม่มีการแก้เนื้อหาหลัก (กลไก pairing-code ไม่กระทบ REC-*/PLN-* เลย) — อัปเดตเฉพาะ header บันทึกผล
    audit รอบนี้ (ไม่พบ drift, ไม่พบ main-body stack-name violation)

**ไม่แตะ**: ภาคผนวก "Stack Mapping" ของทั้ง 4 ไฟล์ — `tech-stack.md` เองยังไม่ได้อัปเดตจาก Firebase Cloud
Functions/React Native-Expo เดิมมาเป็น Express + web-first ตามโค้ดจริง (ตาม CLAUDE.md § Docs/code drift)
รอ `tech-stack-builder` เดินกระบวนการก่อน — การ map sequence diagram ใหม่ของกลไก pairing-code เข้า stack
จริงตอนนี้จะต้องแก้ซ้ำอีกรอบหลัง `tech-stack-builder` reconcile จึงเลื่อนไปรวมกับงาน reconcile อื่นทั้งหมด

**ผลกระทบต่อเอกสารอื่น**: ไม่มี — chain HLA → API Spec/Database Schema → Detailed Design สำหรับกลไก
pairing-code ปิดครบแล้วในรอบนี้ เหลือเพียง `tech-stack.md`/Stack Mapping appendices ทั้ง 4+3 จุดที่รอ
`tech-stack-builder` เดินกระบวนการ (pre-existing drift ไม่ใช่สิ่งใหม่จากรอบนี้)

**คำถามที่ยังค้างอยู่**: ไม่มี — ทั้ง 2 จุดที่พบเป็นเอกสารล้าหลัง แก้ได้ผ่าน flow ปกติ ไม่เข้าเกณฑ์ ask-user
protocol

## `api-db-spec-builder` — API & Database Consistency Audit + update `api-spec.md`/`database-schema.md`

**บริบท**: `architecture-builder` เพิ่งอัปเดต `high-level-architecture.md` (บรรทัดด้านบน) 2 จุด — (1) §3.1
ระบุชัดว่าเว็บไคลเอนต์เป็นทางเข้าเดียวของ credential-based auth (2) เพิ่มกลไกใหม่ **Identity Handoff —
Pairing-Code Mechanism** (§4.5, component interaction ระหว่าง Account & Session Management ↔ Integration
Gateway) พร้อม conceptual data entity ใหม่ **Pairing Credential** (§5) — ส่งต่อให้ `api-db-spec-builder`
propagate เข้า `api-spec.md`/`database-schema.md`

**ผล API & Database Consistency Audit**: เทียบทั้งสองไฟล์กับ HLA ฉบับล่าสุด, `01-spec/` (รวม NFR),
`backlog.md`, `user-journeys.md` — พบ 2 จุด **เอกสารล้าหลัง (ไม่ขัดแย้ง แค่เก่า)** แก้ผ่าน flow ปกติได้เอง
ไม่ต้องถามผู้ใช้:

1. **§2 Conventions ของ `api-spec.md`** เคยเขียนไว้ว่า "ไม่มี endpoint ใดในเอกสารนี้ที่ยกเว้น" การยืนยัน
   ตัวตนก่อนเรียก — ไม่จริงอีกต่อไปหลังเพิ่ม operation ใหม่ตามข้อ 2
2. **ยังไม่มี operation/ตารางรองรับกลไก pairing-code/entity Pairing Credential** ที่ HLA เพิ่งเพิ่ม — ทั้งสอง
   ไฟล์ต้องเพิ่มตามให้ทัน

audit หัวข้ออื่นทั้งหมด (§1, §3.2-3.8 ของ `api-spec.md`, §1/§3.1-3.16/§7 ของ `database-schema.md`) แล้วไม่
พบข้อขัดแย้งหรือแนวคิดใหม่ที่ HLA ไม่รองรับ — ไม่เข้าเกณฑ์ ask-user protocol

**ไฟล์ที่แก้ไข**:

- `docs/02-design/02-technical/api-spec.md`
  - §2 (Conventions, Authentication): แก้ประโยคปิดท้ายให้ระบุข้อยกเว้นเดียวคือ
    `POST /auth/pairing-codes/redeem`
  - §3.1 (Account & Session Management): เพิ่ม 2 operation ท้ายตาราง — `POST /auth/pairing-codes` (ขอรหัส
    จับคู่อุปกรณ์, ต้องยืนยันตัวตนตามปกติ, response = Pairing Credential ใหม่) และ
    `POST /auth/pairing-codes/redeem` (แลกรหัสเป็น session, ไม่ต้องยืนยันตัวตนก่อนเรียก — ข้อยกเว้นเดียวของ
    เอกสารนี้, error case ครบ 3 กรณี: `404` ไม่พบรหัส, `409` รหัสถูกใช้แล้ว, `422` รหัสหมดอายุ)
  - §4 (จุดที่ยังไม่ได้ระบุ): เพิ่มข้อ 12 (ยังไม่มี REQ number formal) และข้อ 13 (concurrent unused code +
    rate limiting ของการขอรหัสซ้ำ)
  - Header: อัปเดต "อัปเดตล่าสุด" สรุปการเปลี่ยนแปลงรอบนี้
- `docs/02-design/02-technical/database-schema.md`
  - §2 (ER Diagram): เพิ่ม entity `PAIRING_CREDENTIAL` และความสัมพันธ์
    `USER_ACCOUNT ||--o{ PAIRING_CREDENTIAL : "mints"`
  - §3.17 (ตารางใหม่ท้ายสุด): `pairing_credential` ← Pairing Credential — column `id`/`code`/
    `user_account_id` (FK)/`expires_at`/`is_used`/`created_at`
  - §4 (Relationships & Constraints): เพิ่ม cardinality bullet (1 user_account → หลาย pairing_credential),
    หมายเหตุว่าตารางนี้ไม่ได้อยู่ใต้ per-user isolation path เหมือนตารางอื่น (query หลักคือ `code` ไม่ใช่
    user id), และกติกา app-layer ใหม่ข้อ 8 (single-use + short-lived enforcement ที่ Account & Session
    Management เป็นเจ้าของ)
  - §5 (Query/Access Pattern): เพิ่มหมายเหตุ pattern ค้นหาด้วย `code`
  - §6 (จุดที่ยังไม่ได้ระบุ): เพิ่มข้อ 9-11 (REQ number formal, รูปแบบเก็บ `code` plaintext/hash,
    concurrent unused code ต่อ user)
  - Header: อัปเดต "อัปเดตล่าสุด" สรุปการเปลี่ยนแปลงรอบนี้

**ไม่แตะ**: ภาคผนวก Stack Mapping ของทั้งสองไฟล์ (`api-spec.md` §6, `database-schema.md` §8) —
`tech-stack.md` เองยังไม่ได้อัปเดตจาก Firebase Cloud Functions/Cloud Firestore เดิมมาเป็น Express +
web-first ตามโค้ดจริง (ตาม CLAUDE.md § Docs/code drift) — `api-spec.md` §6.3.1 ที่เคยระบุ "8 operation" และ
`database-schema.md` §8.2/§8.3 ที่ยังไม่มีแถว `pairing_credential` เลย จึงกลายเป็นข้อมูลล้าหลังเพิ่มอีกชั้น
รอรวมแก้พร้อมกันในรอบ `tech-stack-builder` ถัดไป (mechanical re-sync ไม่ใช่ ask-user)

**ผลกระทบต่อเอกสารอื่น (สำหรับรอบถัดไป)**:

- `detailed-design/04-smart-integrations.md` (`detailed-design-builder`): ต้องมี sequence diagram ใหม่
  (หรือ subgraph/Note เพิ่มในไดอะแกรมเดิมของ INT-2/INT-3) รองรับ 2 operation ใหม่นี้ — `api-spec.md`/
  `database-schema.md` มี operation/ตารางรองรับครบแล้ว
- Stack Mapping appendix ทั้ง 4 จุด (HLA §10, `api-spec.md` §6, `database-schema.md` §8, แต่ละไฟล์ใน
  `detailed-design/`) ยังค้างรอ `tech-stack-builder` เดินกระบวนการก่อนเหมือนเดิม (pre-existing drift) —
  ตอนนี้เพิ่ม `pairing_credential`/2 operation ใหม่เข้าไปในรายการที่ต้อง map ด้วย

**คำถามที่ยังค้างอยู่**: ไม่มี — ทั้ง 2 จุดที่พบเป็นเอกสารล้าหลัง แก้ได้ผ่าน flow ปกติ ไม่เข้าเกณฑ์ ask-user
protocol

## `tech-stack-builder` — Tech Stack Consistency Audit + Reconciliation (`tech-stack.md`)

**บริบท**: `architecture-builder`/`api-db-spec-builder`/`detailed-design-builder` ทั้ง 3 รอบข้างบนแก้
เนื้อหาหลักของ HLA/API Spec/Database Schema/Detailed Design ตามโค้ดจริง (Express + web-first, กลไก
pairing-code) ครบแล้ว แต่ทั้งหมดจงใจไม่แตะ "ภาคผนวก: Stack Mapping" ของตัวเอง เพราะรอ `tech-stack.md`
reconcile จาก Firebase Cloud Functions/React Native-Expo เดิมก่อน (ตาม CLAUDE.md § "Docs/code drift") —
งานนี้คือการ reconcile นั้น

**ผล Tech Stack Consistency Audit**: เทียบ `tech-stack.md` เดิมกับ HLA/API Spec/Database Schema/Detailed
Design ฉบับล่าสุด และกับโค้ดจริงใน `smartfit_daily_app/` — พบว่าโค้ดเปลี่ยน**ตัวเลือก stack จริง 3 จุด**
พร้อมกัน: (1) Backend compute engine (Firebase Cloud Functions → Express.js) (2) Hosting/Infra (Firebase
Hosting ผูกกับ Cloud Functions ใช้กับ Express ไม่ได้ตรงไปตรงมา) (3) Client/codebase split (1 codebase
React Native+Expo คุม 3 แพลตฟอร์ม → แยกเป็น React+Vite สำหรับเว็บ (`apps/web`) กับ React Native+Expo ตัด
ขอบเขตเหลือ INT-2/INT-3 (`apps/mobile`)) — ตามกติกาบังคับของ skill นี้ (ห้ามแก้ตัวเลือก stack เองแม้จะรู้
คำตอบที่ควรจะเป็น) จุดที่ (1) และ (3) เป็นข้อเท็จจริงที่ทีมตัดสินใจไปแล้วในโค้ดจริง (ไม่ใช่ทางเลือกที่ต้อง
ถามซ้ำ) แต่จุด (2) — **"Express server ที่ตอนนี้ serve ทั้ง REST API และ built web client ควร run การผลิต
จริงที่ไหน"** — ยังไม่มีคำตอบจากที่ไหนเลย จึงหยุดถาม mini Discovery Questionnaire ก่อนแก้ไฟล์

**Mini Discovery Questionnaire**:

| หมวด | ตัวเลือกที่เสนอ | คำตอบที่ยืนยัน |
|---|---|---|
| Hosting/Infra สำหรับ Express server ในการผลิตจริง | (1) Google Cloud Run (2) Render (3) Fly.io (4) VM แบบดั้งเดิม | **Google Cloud Run** |

**เหตุผล** (ยืนยันจากผู้ใช้): อยู่ GCP project เดียวกับ Firestore/Firebase Auth ที่เลือกไว้แล้ว —
Application Default Credentials ใช้งานได้ทันทีไม่ต้องจัดการ service account key file แยก,
autoscale-to-zero เข้ากับงบ MVP จำกัดมาก, deploy จาก container image เดียว (Dockerfile) ครอบคลุมทั้ง
Express + built client ในคราวเดียว ตรงกับโครงสร้างจริงที่ `apps/web/server/index.ts` serve ทั้งคู่อยู่แล้ว

**ไฟล์ที่แก้ไข**:

- `docs/02-design/02-technical/tech-stack.md`
  - Header: เพิ่ม "อัปเดตล่าสุด" 2026-08-30 สรุปการ reconcile ทั้งหมด, เลื่อนรายการเดิมลงเป็น
    "อัปเดตก่อนหน้า"/"อัปเดตก่อนหน้านั้น"
  - §2: เพิ่มหัวข้อย่อย "การ Reconcile เพิ่มเติม (2026-08-30)" อธิบายบริบท/การประเมิน/mini Discovery
    Questionnaire/ผลลัพธ์ตามข้างต้น
  - §3 (Recommended Tech Stack): เพิ่มแถว **Web/Client** ใหม่ (React + Vite), แก้แถว **Mobile/Client**
    ให้ระบุขอบเขตที่ตัดแล้ว (INT-2/INT-3 เท่านั้น), แก้ **Backend/API** เป็น Express.js, แก้
    **Hosting/Infra** เป็น Google Cloud Run, แก้ **CI/CD & Dev Tooling** ให้ตรงกับ
    `.github/workflows/ci.yml` จริง + เพิ่มหมายเหตุยังไม่มี deploy step
  - §4 (เหตุผลการเลือก): เขียนใหม่ 4 bullet (React Native+Expo ตัดขอบเขต, React+Vite ใหม่, Express.js
    แทนที่ Cloud Functions, Google Cloud Run แทนที่ Firebase Hosting) + แก้ bullet client-side
    calculation และ PDPA ให้อ้าง Express/Cloud Run แทน Cloud Functions/Firebase Hosting
  - §5: เพิ่มหัวข้อย่อยใหม่ "Hosting/Infra สำหรับ Express.js Server" (ตาราง 4 ทางเลือกพร้อมข้อดี/ข้อเสีย,
    ผลลัพธ์ Google Cloud Run)
  - §6.1: rewrite ตาราง Conceptual Component → Concrete Implementation ทั้งตาราง — ทุกแถว remap "Cloud
    Function `{ชื่อ}`" → Express route จริง (พร้อม path/ไฟล์จริงที่ตรวจสอบจากโค้ด) รวมแถว Account &
    Session Management ที่เพิ่ม mapping กลไก pairing-code ใหม่ (`pairingCodes/{code}` top-level
    collection, `create-code`/`redeem` route, `createCustomToken`, หมายเหตุ delete-on-redeem แทน
    `is_used` flag), แถว Logging & Streak เพิ่มหมายเหตุ `recomputeStreak()` เรียกตรงแทน Firestore
    `onWrite` trigger เดิม
  - §6.2: เพิ่มหมายเหตุไม่เปลี่ยนแปลง (Database ไม่กระทบ) + แก้คำ "Cloud Function" → "Express route"
  - §6.3: rewrite เป็น "Express.js Routing" (โฟลเดอร์ต่อ component, mount ผ่าน `apps/web/server/index.ts`,
    `authenticate` middleware แทน `onCall()`)
  - §6.3.1: rewrite ตาราง 10 operation (เดิม 8 + `pairing-codes`/`pairing-codes/redeem` ใหม่) พร้อมแก้
    ทุก reference เป็นไฟล์จริง (`authService.ts`, `forgotPassword.ts`, `pairing/index.ts`) และหมายเหตุ
    status code ของ redeem ไม่ตรงกับ `api-spec.md` (`410` เดียว vs. `404`/`409`/`422`)
  - §7: แก้ข้อ 5 (Web app scope) ให้ตรงกับโค้ดจริง (INT-1 อยู่ apps/web แล้ว ไม่ใช่ Epic 4 ทั้งหมด
    mobile-only), เพิ่มข้อใหม่ 3 ข้อ (Cloud Run cold start กับ NFR-02, Dockerfile/CI deploy pipeline
    ยังไม่มี, pairing-code redeem status code ไม่ตรงกับ `api-spec.md`), renumber ข้อที่เหลือ (เดิม 6-10
    → 9-13) พร้อมแก้คำ "Cloud Function" เป็น "Express route" ในข้อ 12/13
  - §8: rewrite ทั้งหมด — เพิ่มหมายเหตุสรุปว่าภาคผนวกทั้ง 4 จุดเคย sync สมบูรณ์แล้วครั้งหนึ่ง (รอบ ONB-0)
    แต่ stale อีกครั้งหลังรอบนี้ ด้วยเหตุผลเดียวกันทั้งหมด (Cloud Function เดิม + ไม่มี pairing-code
    mapping + จำนวน operation 8 vs. 10)
- `docs/02-design/02-technical/index.md`
  - แก้ย่อหน้าอธิบาย `tech-stack.md` ให้ระบุ stack ปัจจุบัน (React+Vite/RN+Expo ตัดขอบเขต, Express.js บน
    Google Cloud Run, Firestore/Firebase Auth ไม่เปลี่ยน)
  - ต่อท้ายย่อหน้า "ภาคผนวก: Stack Mapping" ด้วยอัปเดต 2026-08-30 สรุปการ reconcile รอบนี้และผลกระทบต่อ
    ภาคผนวกทั้ง 4 จุด

**ไม่แตะ**: `high-level-architecture.md`, `api-spec.md`, `database-schema.md`,
`detailed-design/*.md` เอง — ตามกติกาบังคับของ skill นี้ (ห้ามแก้ไฟล์เหล่านั้น เรียก
`architecture-writer`/`api-db-spec-writer`/`detailed-design-writer` แทน)

**ผลกระทบต่อเอกสารอื่น (สำหรับรอบถัดไป)**: ภาคผนวก Stack Mapping ทั้ง 4 จุด — **HLA §10**, **`api-spec.md`
§6** (รวม §6.3.1 ที่ยังนับ 8 operation), **`database-schema.md` §8.2/§8.3** (ยังไม่มีแถว
`pairing_credential`), และ**ทุกไฟล์ใน `detailed-design/`** (โดยเฉพาะ `04-smart-integrations.md` ที่ต้อง
เพิ่ม mapping ของ sequence diagram กลไก pairing-code ที่เพิ่งเพิ่มเข้าไป และ
`01-onboarding-personalization.md` ที่ยังนับ 8 operation) — **stale ทั้งหมด** พร้อมให้
`architecture-builder` → `api-db-spec-builder` → `detailed-design-builder` รัน mechanical re-sync ต่อ
ตามลำดับ (ไม่ใช่ ask-user เพราะเป็นการมิเรอร์ข้อเท็จจริงที่ตัดสินใจแล้วในไฟล์นี้)

**คำถามที่ยังค้างอยู่**: ไม่มี — มินิ Discovery Questionnaire ได้คำตอบครบแล้ว (Google Cloud Run) จุดอื่นที่
เหลือ (Cloud Run cold start ผลจริงต่อ NFR-02, pairing-code status code alignment, Dockerfile/CI deploy
step) เป็น "จุดที่ยังไม่ได้ตัดสินใจ" ที่บันทึกไว้ในหัวข้อ 7 ของ `tech-stack.md` แล้ว ไม่ใช่คำถามที่ต้องรอ
ผู้ใช้ตอบก่อนดำเนินการต่อ

## `architecture-builder` — Stack Mapping Appendix re-sync (`high-level-architecture.md` §10)

**บริบท**: `tech-stack-builder` เพิ่ง reconcile `tech-stack.md` ให้สะท้อนสถาปัตยกรรมจริง (Express.js บน
Google Cloud Run แทน Firebase Cloud Functions/Firebase Hosting, กลไก pairing-code identity handoff ใหม่
ใน §6.1/§6.3.1) ข้างบน — ทิ้งไว้ว่าภาคผนวก Stack Mapping ทั้ง 4 จุด (HLA §10, `api-spec.md` §6,
`database-schema.md` §8, `detailed-design/*.md`) stale พร้อมกัน รอบนี้คือการ re-sync ส่วนของ
`high-level-architecture.md` §10 เท่านั้น

**ลักษณะงาน**: mechanical re-sync ล้วนๆ ตามกติกา CLAUDE.md "Drift between the appendix and
`tech-stack.md` is auto-fixable without the ask-user protocol" — ไม่ต้องใช้ ask-user protocol, ไม่แตะ
เนื้อหาหลักหัวข้อ 1-9 (บรรยาย pairing-code เชิงแนวคิดถูกต้องอยู่แล้วจากรอบ audit ก่อนหน้าในวันเดียวกัน)

**ไฟล์ที่แก้ไข**: `docs/02-design/02-technical/high-level-architecture.md`

- Header: เพิ่ม "อัปเดตล่าสุด (รอบ Stack Mapping re-sync)" 2026-08-30 สรุปการเปลี่ยนแปลง, เลื่อนรายการ
  "อัปเดตล่าสุด" เดิม (Architecture Consistency Audit รอบ pairing-code) ลงเป็น "อัปเดตก่อนหน้า"
- §10 (ภาคผนวก: Stack Mapping) — เขียนใหม่ทั้งหมดให้มิเรอร์ `tech-stack.md` §6.1/§6.2/§6.3/§6.3.1 ฉบับ
  ล่าสุด:
  - แทนที่ทุกจุดที่เคยเขียน "Cloud Function `{ชื่อ}`" ด้วย **Express route** จริง (เช่น
    `profileUpdate` → `PUT /api/profile/personal-info` ฯลฯ, `recommendation` →
    `GET /api/workouts/today/recommendation` ฯลฯ, `sessionComplete` →
    `POST /api/workouts/sessions/:sessionId/complete`, `cheatRest` →
    `POST /api/planner/days/:date/cheat-rest`, `forecast` → `GET /api/insights/forecast`,
    `integrations` → 6 endpoint ย่อยใต้ `/api/integrations/*`)
  - แถว **Account & Session Management**: แก้จำนวน operation จาก 8 เป็น **10**, เพิ่ม mapping ของ**กลไก
    pairing-code** ที่ยังไม่เคยมี stack mapping มาก่อน (top-level collection `pairingCodes/{code}`, TTL 5
    นาทีผ่าน `expiresAt`, mint ผ่าน `POST /api/pairing/create-code`, redeem ผ่าน `POST /api/pairing/redeem`
    ที่ `delete()` document แทนการตั้ง `is_used` flag แล้วเรียก `auth.createCustomToken(uid)`)
  - แถว **Logging & Streak**: เพิ่มหมายเหตุเปลี่ยนจาก Firestore `onWrite` trigger อัตโนมัติเดิมเป็นฟังก์ชัน
    `recomputeStreak()` ที่ต้องเรียกตรงจากทุก route ที่เขียน `dailyLogs`/`dayStatus`
  - แถว **Exertion & Calorie Calculation**: อัปเดต referential existence validation ให้อ้าง helper กลาง
    `assertDocExists()` แทนการ `get()` แยกกันของแต่ละ Cloud Function เดิม
  - หมายเหตุ ⚠️ ท้ายตาราง และย่อหน้าปิดท้าย: อัปเดตให้อ้าง Express route/Google Cloud Run แทน Cloud
    Function/Firebase Hosting ทั้งหมด

**ไม่แตะ**: หัวข้อ 1-9 ของ `high-level-architecture.md` — ยังคง conceptual ถูกต้องอยู่แล้ว ไม่มีอะไรต้องแก้

**ไฟล์อื่นที่แก้ไข**: `docs/02-design/02-technical/index.md` (เพิ่มบรรทัดสรุปการ re-sync นี้ต่อท้ายย่อหน้า
Stack Mapping)

**ผลกระทบต่อเอกสารอื่น (สำหรับรอบถัดไป)**: ภาคผนวก Stack Mapping ที่เหลืออีก 3 จุดยังค้างอ้าง Cloud
Function เดิม/นับ 8 operation — `api-spec.md` §6 (รวม §6.3.1), `database-schema.md` §8.2/§8.3 (ยังไม่มีแถว
`pairing_credential`), และทุกไฟล์ใน `detailed-design/` (โดยเฉพาะ `04-smart-integrations.md` ที่ต้องเพิ่ม
mapping ของ sequence diagram กลไก pairing-code) — ควรรัน `api-db-spec-builder` ต่อด้วย `detailed-design-builder`
ตามลำดับเพื่อ mechanical re-sync ส่วนที่เหลือ (ไม่ใช่ ask-user)

**คำถามที่ยังค้างอยู่**: ไม่มี

## `detailed-design-builder` — Factual correction (pairing-code error case) + Stack Mapping Appendix re-sync ปิดท้ายเชน (ทั้ง 4 ไฟล์ `detailed-design/`)

**บริบท**: `api-db-spec-builder` (บรรทัดด้านบน) เพิ่ง reconcile `api-spec.md`/`database-schema.md` ให้ตรงกับ
โค้ดจริง (`apps/web/server/routes/pairing/index.ts`) และ `tech-stack-writer`/`architecture-writer`/
`api-db-spec-writer` reconcile `tech-stack.md`/HLA §10/`api-spec.md` §6/`database-schema.md` §8 ให้เป็น
Express.js บน Google Cloud Run ครบแล้ว — เหลือเพียงไฟล์ใน `detailed-design/` ที่ยังไม่ sync ทั้ง 2 มิติ:
alt block ของ pairing-code sequence diagram ที่เคยแยก `404`/`409`/`422` (ไม่ตรง `api-spec.md` แล้ว) และ
ภาคผนวก Stack Mapping ทั้ง 4 ไฟล์ที่ยังอ้าง Firebase Cloud Functions เดิม/นับ 8 operation

**ลักษณะงาน**: pure factual correction + mechanical appendix re-sync ทั้งคู่ ไม่เข้าเกณฑ์ ask-user protocol
(1) แก้ sequence diagram ให้ตรงกับพฤติกรรมที่ shipped จริงตามที่ `api-spec.md`/`database-schema.md` เพิ่ง
ยืนยัน (2) มิเรอร์ข้อเท็จจริงที่ตัดสินใจแล้วใน `tech-stack.md`

**ไฟล์ที่แก้ไข**:

- `docs/02-design/02-technical/detailed-design/04-smart-integrations.md`
  - แก้ `alt` block ของ sequence diagram "Identity Handoff — Pairing-Code Mechanism" จากเดิม 3 กรณีแยก
    (`404` ไม่พบ/`409` ถูกใช้แล้ว/`422` หมดอายุ) เป็น **`410 Gone` กรณีเดียว** ครอบคลุมทั้ง 3 สถานการณ์
    (ไม่พบ/หมดอายุ/ถูกใช้แล้ว) ให้ตรงกับ `api-spec.md` §3.1 เป๊ะ พร้อมแก้ข้อความอธิบาย edge case ท้าย
    diagram ให้ตรงกัน
  - แก้ step มินต์ที่เคยอ้าง `is_used = false` (ลบออก — field นี้ไม่มีใน `database-schema.md` §3.17 อีก
    ต่อไป) และแก้ step redeem สำเร็จจาก "ตั้ง `is_used = true`" เป็น "ลบ `pairing_credential` ทิ้งถาวรทันที"
    ให้ตรงกับ delete-on-redeem ที่ยืนยันจากโค้ดจริง
  - mechanical re-sync ภาคผนวก Stack Mapping: เพิ่มแถวใหม่ **Account & Session Management** (เฉพาะกลไก
    pairing-code) ที่ไม่เคยมีมาก่อน, แถว Insights & Forecast/Integration Gateway remap เป็น Express route
    จริง, แก้ execution note ของ Forecast (INT-1) จาก React Native client เป็น React+Vite web client (ตาม
    `tech-stack.md` §7 ข้อ 5 ที่ INT-1 ย้ายเข้า `apps/web` แล้ว)
  - Header: อัปเดต "อัปเดตล่าสุด" (รอบ 2) สรุปทั้งสองส่วน
- `docs/02-design/02-technical/detailed-design/01-onboarding-personalization.md`
  - mechanical re-sync ภาคผนวก Stack Mapping: แถว Account & Session Management แก้จำนวน operation จาก 8
    เป็น 10 (เพิ่ม mapping ของ `forgot-password`/`pairing-codes`/`pairing-codes/redeem` เป็น Express route
    จริง, 7 operation ที่เหลือยังเป็น client SDK ตรง), แถว Personalization & Profile remap Cloud Function
    `profileUpdate` เป็น Express route 3 ตัว, แก้ execution note ของ TDEE/Safety Floor (ONB-1/ONB-3) จาก
    React Native client เป็น React+Vite web client
  - Header: อัปเดต "อัปเดตล่าสุด" (รอบ 5)
- `docs/02-design/02-technical/detailed-design/02-daily-youtube-recommendation.md`
  - mechanical re-sync ภาคผนวก Stack Mapping: แถว Content Recommendation/Exertion & Calorie Calculation
    remap Cloud Function `recommendation`/`sessionComplete` เป็น Express route จริง, แก้ execution note
    ของ MET+wearable override (REC-2) เป็น React+Vite web client (ไม่เคยย้าย ไม่กระทบ)
  - Header: อัปเดต "อัปเดตล่าสุด" (รอบ 2)
- `docs/02-design/02-technical/detailed-design/03-planner-logging.md`
  - mechanical re-sync ภาคผนวก Stack Mapping: แถว Planner & Day-Status/Logging & Streak remap Cloud
    Function `cheatRest`/Firestore `onWrite` trigger เดิมเป็น Express route + ฟังก์ชัน `recomputeStreak()`
    ที่ต้องเรียกตรง (เปลี่ยนสถาปัตยกรรมที่มีนัยสำคัญ), แก้ execution note ของ Streak walk-back (PLN-4) เป็น
    React+Vite web client
  - Header: อัปเดต "อัปเดตล่าสุด" (รอบ 2)
- `docs/02-design/02-technical/index.md` — เพิ่มบรรทัดสรุปว่าเชน reconcile ปิดครบแล้ว ไม่มีจุดใดใน
  `detailed-design/` ค้างอ้าง Firebase Cloud Functions เดิม/นับ 8 operation อีกต่อไป

**ไม่แตะ**: เนื้อหาหลักส่วนอื่นของทั้ง 4 ไฟล์ (state diagram, algorithm sections, ความสัมพันธ์กับเอกสารอื่น)
— audit แล้วไม่พบ drift เพิ่มเติมนอกเหนือจาก 2 จุดข้างต้น; `high-level-architecture.md`/`api-spec.md`/
`database-schema.md`/`tech-stack.md` เอง (เป็น read-only upstream ของ skill นี้)

**ผลกระทบต่อเอกสารอื่น**: ไม่มี — เชน `feature-list-journey` → `architecture-writer` →
`api-db-spec-writer` → `detailed-design-writer` → `tech-stack-writer` → `architecture-writer` →
`api-db-spec-writer` → `detailed-design-writer` ที่นำ pipeline กลับมาสอดคล้องกับสถาปัตยกรรม
Express/Cloud-Run/web-first/pairing-code จริงปิดครบสมบูรณ์แล้วในรอบนี้

**คำถามที่ยังค้างอยู่**: ไม่มี — งานนี้เป็น pure factual correction + mechanical re-sync ล้วน ไม่เข้าเกณฑ์
ask-user protocol

## `test-suite-writer` — Self-freshness audit + update `acceptance-criteria.md`/`test-cases/01-*.md`/`test-cases/04-*.md`

**บริบท**: เชน reconcile ทั้งหมดข้างบน (`feature-journey-writer` → `architecture-writer` →
`api-db-spec-writer` → `detailed-design-writer` → `tech-stack-writer` → กลับไป `architecture-writer` →
`api-db-spec-writer` → `detailed-design-writer`) ปิด docs/code drift ของ ONB-0 (web-only) และกลไก
pairing-code identity handoff ครบแล้วในเอกสารต้นน้ำ — `feature-journey-writer` ระบุว่า
`acceptance-criteria.md` และ `test-cases/01-onboarding-personalization.md`/`04-smart-integrations.md`
ล้าหลังตามไปด้วย (ไม่มี AC/test case รองรับ precondition รหัสจับคู่ของ INT-2/INT-3 และไม่มีข้อความยืนยัน
ชัดเจนว่า ONB-0 เป็น web-only) — งานนี้คือการปิด gap นั้น

**Self-freshness audit**: ตรวจ `acceptance-criteria.md` ทั้ง 57 (เดิม 51) scenario และ `test-cases/*.md`
ทั้ง 4 ไฟล์ต่อ Feature ID/REQ ปัจจุบันใน `backlog.md`/`01-spec/` — Feature ID/REQ parity ยังตรงทั้งหมด (ไม่มี
Feature ID ถูกเปลี่ยนเลข) พบ **coverage gap ใหม่ 2 จุด** ตรงกับที่ `feature-journey-writer` แจ้ง: (1) ไม่มี
AC/test case รองรับ ONB-0's web-only UI surface และกลไก pairing-code ของ INT-2/INT-3 เลย (2) scope/priority
ของ `test-plan.md` (MoSCoW) ยังตรงกับ `backlog.md` ปัจจุบัน (ONB-0 = Must, INT-2/INT-3 = Could ไม่เปลี่ยน)
ไม่มี fact drift ของตัวเลข/สูตรใน `01-spec/` ที่กระทบ AC/test data เดิม — **พบ gap เพิ่มเติมนอกเหนือจากที่
ถูกขอ**: `test-plan.md`'s "หมายเหตุสถานะโปรเจกต์" (เขียนไว้ 2026-08-27) ยังระบุว่า "ยังไม่มี application
source code, backend" ซึ่งขัดกับ CLAUDE.md ปัจจุบันที่ยืนยันว่ามี `smartfit_daily_app/` เป็นโค้ดจริงแล้ว —
แก้เฉพาะจุดที่กระทบกลไก pairing-code (R14 ใหม่ + หมายเหตุใน R13) ตามขอบเขตที่ได้รับมอบหมาย ส่วนการ
reconcile เต็มรูปแบบของทั้งเอกสาร (R7/R11/R12/R13 อื่น, Test Environment, Entry/Exit Criteria) บันทึกไว้เป็น
gap แยกต่างหาก ไม่ทำในรอบนี้ (เกินขอบเขตที่ได้รับมอบหมาย)

**ไฟล์ที่แก้ไข**:

- `docs/01-requirements/acceptance-criteria.md`
  - เพิ่ม **AC-ONB-0-07** (พื้นผิว UI ของ Authentication เป็น web-only ทั้งหมด — ยืนยันจากโค้ดจริงว่า
    `apps/mobile/app/` ไม่มีไฟล์ signup/login/forgot-password เลย)
  - เพิ่ม **AC-INT-2-03 ถึง AC-INT-2-06** ภายใต้ INT-2 (ไม่ใช่ ONB-0 แม้เป็น Account & Session Management
    ตาม `api-spec.md` §3.1 ก็ตาม — เลือกจัดกลุ่มตาม Epic ที่ precondition นี้เกิดขึ้นจริง เพื่อให้ AC และ
    test case ใน epic เดียวกันสอดคล้องกัน): mint สำเร็จ (03), redeem สำเร็จ (04), redeem ล้มเหลว → `410`
    กรณีเดียว (05), precondition guard ก่อนเข้าหน้าจับคู่ตาชั่ง (06)
  - เพิ่ม **AC-INT-3-04** ภายใต้ INT-3 (precondition guard เดียวกัน สำหรับหน้าเชื่อมต่อ wearable —
    cross-reference กลับไป AC-INT-2-03/04/05 แทนการเขียนซ้ำ เพราะเป็นกลไกเดียวกัน)
  - อัปเดตตารางสรุปจำนวน scenario ท้ายไฟล์ (ONB-0: 6→7, INT-2: 2→6, INT-3: 3→4, รวม 51→**57**)

- `docs/03-testing/01-test-plan/test-cases/01-onboarding-personalization.md`
  - เพิ่ม **TC-ONB-0-007** (code-inspection-level test case — ตรวจสอบว่า `apps/mobile/app/` ไม่มีไฟล์หน้าจอ
    auth ใด ๆ นอกจาก `pairing-code.tsx`/`device-pairing.tsx` — เลือกใช้รูปแบบนี้แทน runtime UI test เพราะ
    สิ่งที่ต้องพิสูจน์คือ "ไม่มีอยู่จริง" ซึ่งตรวจโค้ดได้แน่นอนกว่า และ execute ได้ทันทีไม่ต้องรอ backend)
  - อัปเดตตารางสรุป AC↔TC และหมายเหตุ coverage ท้ายไฟล์ (17→**18** AC scenario, 22→**23** test case)

- `docs/03-testing/01-test-plan/test-cases/04-smart-integrations.md`
  - เพิ่ม **TC-INT-2-003 ถึง TC-INT-2-007** ภายใต้ INT-2 (mint, redeem สำเร็จ, redeem ล้มเหลวแบบ
    consolidated ครอบคลุมทั้ง 3 สถานการณ์ไม่พบ/หมดอายุ/ถูกใช้แล้วเป็น test case เดียวเพราะโค้ดจริงคืนผลลัพธ์
    ที่แยกไม่ออก, boundary timing ของ TTL 5 นาที, precondition guard) — grounded ด้วย endpoint จริง
    `POST /api/pairing/create-code`/`POST /api/pairing/redeem` จาก `apps/web/server/routes/pairing/index.ts`
    ไม่ใช่ conceptual path ของ `api-spec.md` §3.1 ตรง ๆ
  - เพิ่ม **TC-INT-3-004** (precondition guard เดียวกัน สำหรับหน้าเชื่อมต่อ wearable)
  - เพิ่มหมายเหตุขอบเขตว่า test case กลุ่มนี้ execute ได้จริงในรอบนี้แม้ INT-2/INT-3 เองยังเป็น Could/นอก
    ขอบเขต เพราะ backend จริงของกลไกนี้ implement แล้ว (ต่างจาก test case อื่นของ Epic 4)
  - อัปเดตตารางสรุป AC↔TC ท้ายไฟล์ (9→**14** AC scenario, 10→**16** test case)

- `docs/03-testing/01-test-plan/test-plan.md`
  - §5 ข้อ 2 (Entry Criteria): อัปเดตจำนวน scenario ของ `acceptance-criteria.md` จาก 51 เป็น **57**
  - §2 (ประเภทการทดสอบ): เพิ่มกลไก pairing-code เข้าแถว Functional Testing/Integration Testing
  - §4 (Risk Management): เพิ่ม **R14** ใหม่ (กลไก pairing-code มี backend จริงแล้ว ต่างจาก R7/R11/R12/R13
    — execute ได้จริง แต่ยังมี 2 open point: REQ number formal + rate-limit/concurrent-code) และเพิ่ม
    หมายเหตุใน R13 ว่า AC-ONB-0-07 เป็นข้อยกเว้นที่ execute ได้ทันทีด้วย code inspection ไม่ต้องรอ backend
  - เพิ่มหมายเหตุ (ไม่ผูกกับ R14) ว่า "หมายเหตุสถานะโปรเจกต์" ทั้งฉบับล้าหลังในภาพรวมเทียบ CLAUDE.md ปัจจุบัน
    — บันทึกเป็น gap แยก ไม่แก้เต็มรูปแบบในรอบนี้ (เกินขอบเขตงานที่ได้รับมอบหมาย)
  - §"เอกสารอ้างอิง": เพิ่ม R14 เข้ารายการอ้างอิง `01-spec/`

**ยืนยัน (ไม่ซ้ำ)**: ทั้ง `01-spec/20260823-01-onboarding-personalization.md` และ
`01-spec/20260823-04-smart-integrations.md` มีลิงก์ 1 บรรทัดชี้ไป `acceptance-criteria.md` ใน section
"## Acceptance Criteria" อยู่แล้วจากการ bootstrap ก่อนหน้า — ไม่ต้องเพิ่มซ้ำ ไม่มีการแก้ไฟล์ 01-spec/,
`backlog.md`, หรือ `user-journeys.md` เพิ่มเติมในรอบนี้ (read-only upstream ตามกติกา)

**gap ที่บันทึกไว้ (ไม่ใช่การเงียบ)**:

1. `test-plan.md`'s "หมายเหตุสถานะโปรเจกต์" (และ R7/R11/R12/R13 ที่ตามมา) ยังเขียนโดยสมมติว่าไม่มี
   application code เลย ซึ่งล้าหลังเทียบ CLAUDE.md ปัจจุบัน (มี `smartfit_daily_app/` จริงแล้ว แม้ส่วนใหญ่
   ยัง scaffolded) — แก้เฉพาะส่วนที่กระทบ pairing-code (R14) ตามขอบเขตงานที่ได้รับมอบหมายในรอบนี้เท่านั้น
   แนะนำให้รัน `test-suite-builder` แบบเต็มขอบเขตอีกครั้งเพื่อ audit ทั้ง Test Environment/Entry-Exit
   Criteria ให้ตรงกับสถานะโค้ดจริงทั้งหมด
2. REQ-14 (email verification, password policy), REQ-15 (session timeout ที่แน่นอน) ยังเป็น Open Point
   เดิมที่ไม่มีการเปลี่ยนแปลงในรอบนี้ — gap เดิมที่บันทึกไว้แล้วก่อนหน้า ไม่ใช่ของใหม่

**คำถามที่ยังค้างอยู่**: ไม่มี — งานนี้เป็นการเติม AC/test case ตามกลไกที่ implement จริงแล้ว (mint/redeem/
error-case/expiry/web-only มีคำตอบชัดเจนจากโค้ดและ `api-spec.md` ทั้งหมด) ไม่เข้าเกณฑ์ ask-user protocol

## `test-suite-writer` — Prototype cross-reference fix ปิดท้ายเชน reconcile (pairing-code screens)

**บริบท**: `prototype-writer` เพิ่งสร้างหน้าจอ pairing-code ครบ — เพิ่ม "ขอรหัสจับคู่อุปกรณ์" affordance ใน
`11-device-integrations.html`, สร้างใหม่ `13-companion-pairing-code.html` (หน้ากรอกรหัสบนมือถือ รองรับทั้ง
สถานะถูกต้อง/ไม่ถูกต้อง-หมดอายุ), และเพิ่ม device-select landing state ใน `12-device-pairing.html` — ทำให้
ลิงก์ `Prototype:` ที่เคยเขียนไว้ใน `acceptance-criteria.md`/`test-cases/04-smart-integrations.md` (ตอนยังไม่
มี UI mockup) ล้าหลัง งานนี้เป็นการแก้ mechanical ล้วนตามที่ orchestrator ระบุมาแล้ว ไม่ต้อง audit เพิ่ม

**ไฟล์ที่แก้ไข**:

- `docs/01-requirements/acceptance-criteria.md`
  - AC-INT-2-03: "ไม่มี prototype" → [11-device-integrations.html](../02-design/01-prototypes/v1/11-device-integrations.html)
  - AC-INT-2-05: [12-device-pairing.html](../02-design/01-prototypes/v1/12-device-pairing.html) →
    [13-companion-pairing-code.html](../02-design/01-prototypes/v1/13-companion-pairing-code.html) (สถานะ
    รหัสไม่ถูกต้อง/หมดอายุอยู่ที่หน้านี้จริง ไม่ใช่ 12)
  - AC-INT-2-06, AC-INT-3-04: "ไม่มี prototype" → flow เชิงเส้น 11 → 13 → 12

- `docs/03-testing/01-test-plan/test-cases/04-smart-integrations.md`
  - TC-INT-2-003: เพิ่มลิงก์ 11-device-integrations.html (เดิมไม่มี)
  - TC-INT-2-005: เพิ่มลิงก์ 13-companion-pairing-code.html (boundary test เดิมไม่มีลิงก์ prototype เลย)
  - TC-INT-2-007, TC-INT-3-004: เพิ่มลิงก์ flow เชิงเส้น 11 → 13 → 12

**ยืนยันจาก 3 ไฟล์ HTML จริงก่อนแก้**: `11-device-integrations.html` มีปุ่ม "ขอรหัสจับคู่อุปกรณ์" +
`pairingCodeValue`/`pairingCountdown`; `13-companion-pairing-code.html` มี error state
`codeError`/`btnDemoInvalid` ("รหัสนี้ไม่ถูกต้องหรือหมดอายุแล้ว"); `12-device-pairing.html` มี device-select
landing state (`?device=` ว่าง)

**Self-freshness audit**: ไม่เข้าเกณฑ์ในรอบนี้ — ขอบเขตงานจำกัดเฉพาะแก้ลิงก์ `Prototype:`/`References` ของ AC/
TC 4 คู่ที่ orchestrator ระบุมา ไม่มี Feature ID/REQ ใหม่ ไม่มีตัวเลข/สูตรเปลี่ยน ไม่กระทบ `test-plan.md`

**gap ที่พบ**: ไม่มี

**คำถามที่ยังค้างอยู่**: ไม่มี — เป็นการแก้ reference เชิงกลไกล้วน ปิดเชน reconcile ของวันนี้ทั้งหมด

## `feature-journey-writer` — Formalize pairing-code mechanism เป็น REQ-18 + Feature ID ใหม่ INT-0

**บริบท**: ผู้ใช้งานยืนยัน (นอกรอบ ask-user ของ skill นี้ เพราะเป็น decision ที่ได้คำตอบมาแล้ว) ให้กลไก
pairing-code identity handoff — ซึ่งก่อนหน้านี้ถูกบันทึกเป็น "จุดที่ยังไม่ได้ระบุ" ใน
`01-spec/20260823-04-smart-integrations.md` — ได้ **REQ number ของตัวเอง (REQ-18)** และ **Feature ID
ของตัวเอง (INT-0)** โดยตั้งชื่อ/จัดวางตามแบบแผนเดียวกับที่ ONB-0 เคยได้รับมาก่อน (foundational
precondition ที่ feature อื่นในกลุ่มใช้ร่วมกัน) แทนที่จะผูกเป็น implicit precondition ของ REQ-12/REQ-13
ต่อไป หรือคง duplicate P1-P6 ไว้ในทั้ง INT-2 และ INT-3 เหมือนเดิม

**งานที่ทำ**:

1. `docs/01-requirements/01-spec/20260823-04-smart-integrations.md`
   - เพิ่ม **REQ-18** เข้า "เงื่อนไข/กติกาทางธุรกิจ" (pairing code 6 หลัก, อายุ 5 นาที, single-use, mint
     โดย server ผูกกับบัญชีที่ล็อกอินอยู่, redeem แบบ silent sign-in) พร้อมเพิ่มเข้า Scope (in-scope) และ
     Acceptance Criteria checklist
   - ย้ายเนื้อหาจาก "จุดที่ยังไม่ได้ระบุ" (คำถามว่าควรมี REQ number แยกหรือไม่) เข้า
     "ข้อสมมติฐาน/การตัดสินใจที่ยืนยันแล้ว" — บันทึกผลว่า REQ-18 ผูกกับ Epic Smart Integrations (ไม่ใช่
     Onboarding) พร้อมเหตุผล และ Feature ID คือ INT-0
   - เพิ่ม open point ใหม่ (ปลีกย่อย ไม่กระทบโครงสร้าง): ยังไม่ระบุกติกา rate limiting เมื่อกรอกรหัสผิดซ้ำ
   - อัปเดต "ความสัมพันธ์กับเอกสารอื่น" ให้อ้าง INT-0 แทนการพูดถึง INT-2/INT-3 รวมกันลอย ๆ
   - `docs/01-requirements/01-spec/20260823-01-onboarding-personalization.md`: แก้คำอธิบาย cross-link
     เดิมที่พูดถึง "กลไก pairing-code" ให้ระบุชื่อ INT-0/REQ-18 ชัดเจนแทน (จุดเดียวที่พบว่าอ้างถึง
     mechanism นี้ — ไม่มี open point ของตัวเองเกี่ยวกับ REQ number ในเอกสารนี้ให้ต้องลบ)

2. `docs/01-requirements/backlog.md`
   - เพิ่มแถว **INT-0** เข้าตารางสรุป (Epic Smart Integrations, **Could**, REQ-18) วางเป็นแถวแรกสุดของ
     Epic 4 (เทียบเคียง ONB-0) พร้อมหมายเหตุอธิบายเหตุผลการจัดลำดับ
   - เพิ่มคำอธิบายเต็มของ INT-0 (Priority reasoning: Could เพราะสืบทอด priority จาก INT-2/INT-3 ที่มันรองรับ
     เท่านั้น ไม่มี Must/Should พึ่งพาเหมือน ONB-0 — ต่างจาก ONB-0 ตรงจุดนี้อย่างชัดเจน)
   - ตัดรายละเอียด pairing-code ที่เคย duplicate อยู่ในคำอธิบายของ INT-2 และ INT-3 ออก เปลี่ยนเป็นอ้างอิงกลับ
     มาที่ INT-0 แทน
   - อัปเดต REQ Traceability Matrix (เพิ่มแถว REQ-18 → INT-0, แก้ข้อความสรุปจาก "REQ-01 ถึง REQ-17" เป็น
     "REQ-01 ถึง REQ-18")

3. `docs/02-design/01-prototypes/user-journeys.md`
   - เพิ่ม entry ใหม่ **INT-0** (ก่อน INT-1) พร้อม Mermaid diagram ของตัวเอง (แปลงมาจาก P1-P6 เดิม) →
     คำอธิบาย 7 ขั้นตอนกำกับ REQ-18 ทุกขั้น → Actor/Goal/Trigger/Preconditions/Success State/Alt-Edge Cases
   - แก้ diagram ของ **INT-2** และ **INT-3**: ลบ node P1-P6 ออก เหลือเฉพาะ flow เฉพาะของแต่ละ feature
     เอง เพิ่ม blockquote "Precondition" อ้างกลับไปที่ INT-0 ก่อน diagram แทน และย่อ Alt/Edge Cases ส่วน
     pairing-code ให้เหลือ cross-reference บรรทัดเดียวแทนการ duplicate เนื้อหาเดิม
   - อัปเดต intro ของ Epic 4 ให้กล่าวถึง INT-0 ด้วย
   - Open Questions: ลบข้อเดิม (ควรมี REQ number แยกหรือไม่ — resolve แล้ว) แทนที่ด้วยข้อใหม่ปลีกย่อย
     (rate limiting ของ INT-0) และแก้ข้อความสรุป REQ ให้รวม REQ-18

**Consistency Audit ที่พบ**: REQ-18 เป็นกรณี "spec ล้าหลัง (ไม่ขัดแย้ง)" — เนื้อหาถูกบันทึกไว้แล้วใน spec
ในฐานะ open point ตั้งแต่รอบก่อนหน้า และ decision ที่ยืนยันครั้งนี้คือการ resolve open point นั้น ไม่ใช่การ
ขัดแย้งข้ามชั้น จึงเพิ่มเข้า spec ก่อนแล้วอ้างอิงกลับจาก backlog/journey ตามลำดับที่ SKILL.md กำหนด — ไม่มี
จุดขัดแย้งตรง ๆ ระหว่าง spec กับ downstream ที่ต้องถามผู้ใช้เพิ่มเติมในรอบนี้ (คำถามหลักได้รับคำตอบมาก่อนแล้ว)

**Feature ID/REQ parity หลังแก้**: ONB-0…ONB-3, REC-1…REC-4, PLN-1…PLN-4, **INT-0 (ใหม่)**, INT-1…INT-3 —
ตรงกันครบทั้ง 3 ชั้น REQ-01 ถึง REQ-18 ทุกข้อมี Feature ID ใน backlog.md และ Step mapping ใน
user-journeys.md ครบ ไม่มี REQ ใดตกหล่น

**ผลกระทบต่อเอกสารดาวน์สตรีมที่ต้องรันต่อ (ขั้นตอนที่ 0.5 — ไม่แก้เองตามกติกา)**:

- **`acceptance-criteria.md`** (`test-suite-builder`/`test-suite-writer`): AC-INT-2-03, AC-INT-2-04 (ไม่ได้
  ระบุในโจทย์เดิมแต่ควรตรวจด้วยเพราะเป็นชุดเดียวกัน — mint/redeem สำเร็จ), AC-INT-2-05, AC-INT-2-06, และ
  AC-INT-3-04 อ้าง Feature ID ผิด epic แล้ว (ปัจจุบันอยู่ใต้ INT-2/INT-3 แต่เนื้อหาเป็นของ INT-0/REQ-18)
  ควร renumber เป็น **AC-INT-0-01 ถึง AC-INT-0-0N** (ตามลำดับ mint → redeem สำเร็จ → redeem ล้มเหลว →
  precondition guard) แล้วให้ INT-2/AC-INT-2-06 และ INT-3/AC-INT-3-04 (guard เข้าหน้าอุปกรณ์แต่ละชนิด)
  cross-reference กลับมาแทน
- **`test-cases/04-smart-integrations.md`** (`test-suite-builder`/`test-suite-writer`): TC-INT-2-003,
  TC-INT-2-004 (mint/redeem สำเร็จ — ชุดเดียวกับ TC-INT-2-003 ให้ตรวจรวม), TC-INT-2-005, TC-INT-2-006
  (boundary timing TTL — เนื้อหาเดียวกัน), และ TC-INT-2-007 ควร renumber เป็น **TC-INT-0-001 ถึง
  TC-INT-0-00N**; TC-INT-3-004 (precondition guard ของหน้า wearable) ให้คงเป็น test case ของ INT-3 ที่
  cross-reference กลับไปยัง TC-INT-0 ชุดใหม่แทน (เช่นเดียวกับที่เดิม cross-reference ไปยัง TC-INT-2-007)
- **`high-level-architecture.md`** (`architecture-builder`): §3.1, §3.8, §4.5, §5, §7, §8 อ้าง "map แบบ
  implicit กับ REQ-12/REQ-13" หรือ "ยังไม่มี REQ number formal" สำหรับกลไก pairing-code หลายจุด — ควรแก้ให้
  อ้าง **REQ-18** ตรง ๆ (ไม่ implicit อีกต่อไป) และปิด open point ที่ §8 ข้อ 7 ที่เคยถามว่า "ควรมี REQ number
  หรือไม่"
- **`api-spec.md`** (`api-db-spec-builder`): §4 ข้อ 12 ("ยังไม่มี REQ number formal") ควรปิด/แก้ให้อ้าง
  REQ-18
- **`database-schema.md`** (`api-db-spec-builder`): §6 ข้อที่อ้าง "ยังไม่มี REQ number formal" (ดูเลขข้อ
  ปัจจุบันในไฟล์) ควรปิด/แก้ให้อ้าง REQ-18 เช่นกัน
- **`detailed-design/04-smart-integrations.md`** (`detailed-design-builder`): ความสัมพันธ์กับ REQ-12/
  REQ-13 แบบ implicit ของ sequence diagram "Identity Handoff — Pairing-Code Mechanism" ควรแก้ให้อ้าง
  REQ-18 ตรง ๆ พร้อมเปลี่ยนชื่อ section ให้สะท้อนว่าเป็น feature ของตัวเอง (INT-0) ไม่ใช่แค่ precondition
  ที่ไม่มี REQ
- **`v1/` prototype**: ต่ำ priority — `11-device-integrations.html`/`12-device-pairing.html`/
  `13-companion-pairing-code.html` ไม่จำเป็นต้องแก้ UI (เนื้อหาหน้าจอยังตรงกับ flow เดิมทุกประการ) แต่ถ้ามี
  comment/label อ้าง Feature ID ในโค้ด HTML ควรอัปเดตเป็น INT-0 ให้ตรง — แนะนำให้ `prototype-builder` ตรวจสอบ
  แบบผิวเผินเมื่อสะดวก ไม่เร่งด่วน

**คำถามที่ยังค้างอยู่**: ไม่มี — decision หลัก (REQ-18 + INT-0) ได้รับการยืนยันจากผู้ใช้งานมาก่อนเริ่มงานนี้
แล้ว ส่วน open point ปลีกย่อยที่เหลือ (rate limiting ของ INT-0, ลำดับความสำคัญข้อมูลชนกันของ INT-2/INT-3,
จำนวนวัน log ขั้นต่ำของ INT-1) ถูกบันทึกไว้ใน "จุดที่ยังไม่ได้ระบุ" ของ spec และ "Open Questions" ของ
user-journeys.md ตามเกณฑ์ที่ SKILL.md กำหนด ไม่ใช่คำถามที่ต้องรอผู้ใช้ตอบก่อนดำเนินการต่อ

## `detailed-design-builder` — Citation/grouping fix: pairing-code mechanism → INT-0/REQ-18 (`04-smart-integrations.md`)

**บริบท**: `feature-journey-writer` (บรรทัดด้านบน) เพิ่ง formalize กลไก pairing-code เป็นบทบัญญัติ
**REQ-18** พร้อม Feature ID ของตัวเอง **INT-0** ใน `backlog.md`/`01-spec/20260823-04-smart-integrations.md`
แทนที่การอ้างเป็น implicit precondition ของ REQ-12/REQ-13 เดิม และ flag ให้ `detailed-design-builder` แก้
`detailed-design/04-smart-integrations.md` ต่อ (citation/grouping-only ไม่ใช่การ re-model diagram)

**ไฟล์ที่แก้ไข**:

- `docs/02-design/02-technical/detailed-design/04-smart-integrations.md`
  - เปลี่ยนหัวข้อ `## Identity Handoff — Pairing-Code Mechanism (precondition ของ INT-2, INT-3 — เพิ่ม
    2026-08-30)` เป็น `## INT-0 — ยืนยันตัวตนก่อนจับคู่อุปกรณ์ผ่านรหัสจับคู่ (Pairing Code) (REQ-18)` ให้
    ตรงรูปแบบหัวข้อกลุ่ม Feature ID เดียวกับ INT-1/INT-2/INT-3 อื่นในไฟล์นี้
  - แก้ย่อหน้าคำอธิบายใต้หัวข้อจาก "ยังไม่มี REQ number formal... implicit precondition ของ
    REQ-12/REQ-13" เป็นอ้าง REQ-18/INT-0 ตรงๆ พร้อม cross-reference ไปยัง `backlog.md` และ
    `01-spec/20260823-04-smart-integrations.md`
  - แก้ list "ความสัมพันธ์กับเอกสารอื่น" จาก `INT-1/2/3, REQ-11/12/13` เป็น `INT-0/1/2/3, REQ-18/11/12/13`
  - Header: เพิ่ม "อัปเดตล่าสุด" (รอบ 3) สรุปการเปลี่ยนแปลงรอบนี้
  - **ไม่แตะ**: ตัว sequence diagram (mint/redeem flow, `alt` block `410 Gone`) และเนื้อหาของ section
    INT-1/INT-2/INT-3 เอง — เป็น citation/grouping-only ตามที่ร้องขอ
- `docs/02-design/02-technical/index.md` — เพิ่มบรรทัดสรุปการแก้นี้ต่อท้ายย่อหน้า Stack Mapping/Detailed
  Design เดิม

**ไม่แตะ**: `high-level-architecture.md` §3.1/§3.8/§4.5/§5/§7/§8, `api-spec.md` §4, `database-schema.md` §6
— ยังค้างอ้าง "implicit กับ REQ-12/REQ-13"/"ยังไม่มี REQ number formal" ตามที่ log ด้านบน flag ไว้ว่าเป็น
ผลกระทบต่อเอกสารดาวน์สตรีมที่ต้องรัน `architecture-builder`/`api-db-spec-builder` ต่อ (ไม่ใช่ขอบเขตของงานนี้
ซึ่งจำกัดเฉพาะ `detailed-design/04-smart-integrations.md`)

**ผลกระทบต่อเอกสารอื่น (สำหรับรอบถัดไป)**: `high-level-architecture.md`, `api-spec.md`,
`database-schema.md` ยังต้องแก้การอ้าง REQ-12/REQ-13 แบบ implicit/"ยังไม่มี REQ number formal" ให้เป็น
REQ-18/INT-0 ตรงๆ เช่นเดียวกัน — ตามที่ระบุไว้แล้วในผลกระทบของงาน `feature-journey-writer` ด้านบน
(`architecture-builder` → `api-db-spec-builder` ตามลำดับ) รวมถึง `acceptance-criteria.md`/
`test-cases/04-smart-integrations.md` ที่ควร renumber เป็น AC-INT-0-*/TC-INT-0-* (งานของ
`test-suite-builder`)

**คำถามที่ยังค้างอยู่**: ไม่มี — citation/grouping-only fix ตามที่ผู้ใช้ระบุ ไม่เข้าเกณฑ์ ask-user protocol

## `api-db-spec-builder` — Citation-only fix: pairing-code mechanism → INT-0/REQ-18 (`api-spec.md`/`database-schema.md`)

**บริบท**: `feature-journey-writer` formalize กลไก pairing-code เป็น Feature ID **INT-0** พร้อม business
rule **REQ-18** ใหม่ (ดู `backlog.md` § INT-0 และ `01-spec/20260823-04-smart-integrations.md` § REQ-18)
แทนที่การอ้างเป็น implicit precondition ของ REQ-12/REQ-13 เดิม — `detailed-design-builder` แก้
`detailed-design/04-smart-integrations.md` ไปแล้ว (บรรทัดด้านบน) และ flag ต่อว่า `api-spec.md`/
`database-schema.md` ยังค้างการแก้ citation เดียวกัน — งานนี้คืองานนั้น

**ลักษณะงาน**: citation-only fix ตามที่ผู้ใช้ระบุชัดเจน ไม่ re-model operation/table ใดๆ

**ไฟล์ที่แก้ไข**:

- `docs/02-design/02-technical/api-spec.md`
  - หัวข้อ 3.1: แก้ Feature/REQ column ของ `POST /auth/pairing-codes` และ `.../redeem` จาก
    "INT-2, INT-3 (precondition ทางเทคนิค implicit — ไม่มี REQ number formal ของตัวเอง, ดู HLA §8 ข้อ 7)"
    เป็น **INT-0/REQ-18**
  - หัวข้อ 4 ข้อ 12: resolve จุดที่ยังไม่ได้ระบุเดิม ("ยังไม่มี REQ number formal สำหรับกลไกนี้") ว่า
    resolve แล้วด้วย INT-0/REQ-18 (ขีดฆ่าข้อความเดิม + หมายเหตุ resolved)
  - Header: เพิ่ม "อัปเดตล่าสุด" (รอบ 6) สรุปการแก้ citation นี้
- `docs/02-design/02-technical/database-schema.md`
  - หัวข้อ 3.17 (`pairing_credential`): แก้บรรทัด Feature จาก "INT-2, INT-3 (precondition ทางเทคนิค
    implicit เท่านั้น ยังไม่มี REQ number formal ของตัวเอง — ดู HLA §8 ข้อ 7)" เป็น **INT-0/REQ-18**
  - หัวข้อ 6 ข้อ 9: resolve จุดที่ยังไม่ได้ระบุเดิมด้วย INT-0/REQ-18 เช่นเดียวกับข้างต้น
  - Header: เพิ่ม "อัปเดตล่าสุด" (รอบ 6) สรุปการแก้ citation นี้
  - ตรวจแล้วว่าตาราง `integration_connection` (หัวข้อ 3.16) ที่อ้าง "INT-2, INT-3/REQ-12, REQ-13" ยังคง
    ถูกต้องเดิม **ไม่แก้** — เป็นตารางคนละเรื่อง (Integration Consent/Connection State ของ INT-2/INT-3
    เอง ไม่ใช่ pairing-code mechanism)
- `docs/02-design/02-technical/index.md` — เพิ่มบรรทัดสรุปการแก้นี้ต่อท้ายย่อหน้า Stack Mapping/Detailed
  Design เดิม

**ไม่แตะ**: `high-level-architecture.md` §7/§8 ที่ยังอ้าง "implicit precondition ของ REQ-12/REQ-13"/
"ยังไม่มี REQ number formal" เช่นกัน — เป็นไฟล์ที่ owner คือ `architecture-builder` เท่านั้น (ตาม
CLAUDE.md ห้าม `api-db-spec-writer` แก้ HLA เอง) — สแกน error/edge case, request/response payload, NFR
ของทั้ง 2 operation แล้วไม่พบเนื้อหาอื่นที่ต้องแก้ไปพร้อมกัน (no re-modeling ตามที่ร้องขอ)

**ผลกระทบต่อเอกสารอื่น (สำหรับรอบถัดไป)**: `high-level-architecture.md` §7 (Cross-cutting Concerns) และ
§8 (จุดที่ยังไม่ได้ระบุ ข้อ 7) ยังอ้าง "implicit precondition ของ REQ-12/REQ-13"/"ยังไม่มี REQ number
formal" อยู่ — ควรรัน `architecture-builder` เพื่อแก้ citation เดียวกันให้ครบทั้งเชน รวมถึง
`acceptance-criteria.md`/`test-cases/04-smart-integrations.md` ที่ควร renumber เป็น
AC-INT-0-*/TC-INT-0-* (งานของ `test-suite-builder` — ระบุไว้แล้วในผลกระทบของ `feature-journey-writer`)

**คำถามที่ยังค้างอยู่**: ไม่มี — citation-only fix ตามที่ผู้ใช้ระบุ ไม่เข้าเกณฑ์ ask-user protocol

## `architecture-builder` — Citation-only fix ปิดท้ายเชน (`high-level-architecture.md`)

**บริบท**: `feature-journey-writer` formalize กลไก pairing-code เป็น Feature ID **INT-0** พร้อม business
rule ใหม่ **REQ-18** ใน `backlog.md`/`01-spec/20260823-04-smart-integrations.md` แทนที่การอ้างเป็น
implicit precondition ของ REQ-12/REQ-13 เดิม — `detailed-design-builder` และ `api-db-spec-builder` แก้
citation เดียวกันใน `detailed-design/04-smart-integrations.md`/`api-spec.md`/`database-schema.md` ไปแล้ว
(บรรทัดด้านบน) และ flag ต่อว่า `high-level-architecture.md` §3.1/§3.8/§4.5/§5/§7/§8 ยังค้าง — งานนี้คือ
งานนั้น ปิดเชน citation fix ทั้งหมด

**ลักษณะงาน**: citation-only fix ล้วนๆ ตามที่ผู้ใช้ระบุชัดเจน ไม่ re-model component/data flow/entity ใดๆ

**ไฟล์ที่แก้ไข**: `docs/02-design/02-technical/high-level-architecture.md`

- §3.1 (Account & Session Management — รับผิดชอบ/หน้าที่/คุยกับ): แก้ "implicit precondition ทางเทคนิคของ
  INT-2/INT-3 (ยังไม่มี REQ number formal ของตัวเอง)" เป็น precondition ตาม **REQ-18 (Feature ID INT-0)**
  ในทั้ง 3 ตำแหน่ง (รับผิดชอบ, ประโยค identity handoff ในหน้าที่, ประโยค Integration Gateway ใน "คุยกับ")
- §3.8 (Integration Gateway — หน้าที่/คุยกับ): แก้ "precondition ทางเทคนิคที่ implicit อยู่ใน
  REQ-12/REQ-13" เป็น **REQ-18 (Feature ID INT-0)** ทั้ง 2 ตำแหน่ง
- §4.5 (Flow 5): แก้หัว flow เป็น `(INT-0 → INT-2, INT-3 → INT-1)`, แก้ label ของ `subgraph PAIR` diagram
  จาก "precondition implicit, ยังไม่มี REQ number formal" เป็น `INT-0: ... REQ-18`, แก้คำอธิบาย PAIR
  bullet ให้ระบุ REQ-18/INT-0 แทน "map กับ REQ-12/REQ-13 แบบ implicit"
- §5 (Conceptual Data Entities): แก้ชื่อ entity **Pairing Credential** ให้ระบุ "REQ-18/INT-0" และเพิ่ม
  citation เดียวกันในคำอธิบายความสัมพันธ์
- §7 (Cross-cutting Concerns — Security/Privacy): เพิ่ม citation REQ-18/Feature ID INT-0 ต่อท้าย
  "รหัสจับคู่อุปกรณ์ (Pairing Credential)" และ "กลไก pairing-code" ที่ปรากฏในย่อหน้าเดิม
- §8 ข้อ 7: เขียนใหม่จาก "กลไกนี้ยังไม่มี REQ number formal ของตัวเอง" เป็น "resolved บางส่วน" — ระบุว่า
  ได้ REQ-18/INT-0 formal แล้วเมื่อ 2026-08-30 (อ้างอิง backlog.md § INT-0 แทนลิงก์เดิมที่อ้างแค่ spec) —
  คงเหลือเฉพาะประเด็น NFR-05 coverage ที่ยังไม่ resolved ไว้ตามเดิม
- หัวข้อ 1 (ขอบเขตและหลักการ): แก้จำนวน Feature ID จาก 15 เป็น **16** พร้อมเพิ่มหมายเหตุว่า INT-0/REQ-18
  formalize เมื่อ 2026-08-30
- Header: เพิ่ม "อัปเดตล่าสุด (citation fix)" 2026-08-30 สรุปการเปลี่ยนแปลงทั้งหมดข้างต้น

**ไม่แตะ**: §2 (System context diagram), §6 (External Integration Boundaries), §9 (ความสัมพันธ์กับเอกสาร
อื่น), §10 (ภาคผนวก: Stack Mapping) — ไม่มีการอ้าง REQ-12/REQ-13 ผูกกับกลไก pairing-code ที่จุดเหล่านี้เลย
(สแกนแล้วด้วย grep เต็มไฟล์) — บรรทัดประวัติ (`อัปเดตก่อนหน้า`) ที่เคยบันทึกไว้ว่า pairing-code เป็น
"implicit ต่อ REQ-12/13" ณ เวลานั้นก็**ไม่แก้ย้อนหลัง** เพราะเป็นบันทึกประวัติที่ถูกต้อง ณ ตอนที่เขียน
(ตามธรรมเนียมเดิมของไฟล์นี้ที่ไม่เคยแก้ changelog ย้อนหลัง) — ข้อ 4 ของ §8 ("INT-2/INT-3/REQ-12, REQ-13"
เรื่องลำดับความสำคัญเมื่อข้อมูลจากหลายแหล่งขัดกัน) **ไม่แก้เช่นกัน** เพราะเป็นประเด็นคนละเรื่อง (data-source
conflict priority ของ INT-2/INT-3 เอง ไม่ใช่กลไก pairing-code)

**ผลกระทบต่อเอกสารอื่น**: ไม่มี — เชน citation fix `feature-list-journey` → `detailed-design-writer` →
`api-db-spec-writer` → `architecture-writer` สำหรับ INT-0/REQ-18 ปิดครบทุกไฟล์แล้ว (HLA, API Spec,
Database Schema, Detailed Design) เหลือเพียง `acceptance-criteria.md`/`test-cases/04-smart-integrations.md`
ที่ `feature-journey-writer` เคยระบุว่าควร renumber เป็น AC-INT-0-*/TC-INT-0-* — เป็นงานของ
`test-suite-builder` แยกต่างหาก ไม่ใช่ของ `architecture-builder`

**คำถามที่ยังค้างอยู่**: ไม่มี — citation-only fix ตามที่ผู้ใช้ระบุ ไม่เข้าเกณฑ์ ask-user protocol

## `test-suite-builder` — Renumbering AC-INT-2-*/AC-INT-3-04 และ TC-INT-2-*/cross-ref TC-INT-3-004 เป็น INT-0

**บริบท**: `architecture-builder` (บรรทัดด้านบน) เพิ่ง citation-fix `high-level-architecture.md` ให้อ้าง
**INT-0/REQ-18** แทน "implicit precondition ของ REQ-12/REQ-13" หลัง `feature-journey-writer` formalize
กลไก pairing-code เป็น Feature ID **INT-0** พร้อม **REQ-18** ของตัวเองใน `backlog.md`/`user-journeys.md`
— flag ต่อว่า `acceptance-criteria.md`/`test-cases/04-smart-integrations.md` ยังค้าง renumber เป็น
AC-INT-0-*/TC-INT-0-* งานนี้คืองานนั้น เป็นการ renumbering เชิงกลไกล้วนตามการตัดสินใจที่ทำไว้แล้ว
ไม่เข้าเกณฑ์ ask-user protocol

**ลักษณะงาน**: ย้าย ID ตาม instruction ที่กำหนดไว้ชัดเจน — เนื้อหา Given-When-Then/Test Steps/Expected
Result/Test Data ไม่เปลี่ยนแปลง มีแค่ ID/การจัดกลุ่ม/REQ ที่อ้างอิงที่ปรับให้ตรงกับ REQ-18/INT-0

**ไฟล์ที่แก้ไข**:

- `docs/01-requirements/acceptance-criteria.md`
  - เพิ่ม section ใหม่ `### INT-0 — ยืนยันตัวตนก่อนจับคู่อุปกรณ์ผ่านรหัสจับคู่ (Pairing Code)` ก่อน INT-1
    พร้อม **AC-INT-0-01 ถึง AC-INT-0-04** (mint → redeem success → redeem failure → precondition guard)
    ย้ายมาจาก AC-INT-2-03/04/05/06 และ AC-INT-3-04 เดิม — AC-INT-2-06 กับ AC-INT-3-04 (precondition guard
    คนละปลายทาง) รวมเป็น AC-INT-0-04 เดียวเพราะกลไกเดียวกันทุกประการ
  - INT-2 section: ลบเนื้อหา AC-INT-2-03 ถึง 06 ออก แทนที่ด้วยหมายเหตุ cross-reference ไปยัง AC-INT-0-01
    ถึง 04 (เหลือ AC-INT-2-01/02 เดิม)
  - INT-3 section: ลบเนื้อหา AC-INT-3-04 ออก แทนที่ด้วยหมายเหตุ cross-reference ไปยัง AC-INT-0-04
  - แก้ cross-reference ใน AC-ONB-0-07 ที่เคยอ้าง "AC-INT-2-03 ถึง AC-INT-2-06" เป็น "AC-INT-0-01 ถึง
    AC-INT-0-04"
  - อัปเดต TOC, สรุปจำนวน scenario ต่อ Feature (เพิ่มแถว INT-0 = 4, INT-2 เหลือ 2, INT-3 เหลือ 3, รวม
    Feature ID 15→16, รวม scenario 57→56), และ header count (15→16 Feature ID)
- `docs/03-testing/01-test-plan/test-cases/04-smart-integrations.md`
  - เพิ่ม section ใหม่ `## INT-0 — ยืนยันตัวตนก่อนจับคู่อุปกรณ์ผ่านรหัสจับคู่ (REQ-18)` ก่อน INT-1 พร้อม
    **TC-INT-0-001 ถึง TC-INT-0-005** ย้ายมาจาก TC-INT-2-003/004/005/006/007 เดิมตามลำดับ (mint → redeem
    success → redeem failure consolidated 410 → TTL boundary → precondition guard ครอบคลุมทั้ง 2 ปลายทาง)
  - INT-2 section: ลบเนื้อหา TC-INT-2-003 ถึง 007 ออก แทนที่ด้วยหมายเหตุ cross-reference ไปยัง TC-INT-0-001
    ถึง 005 (เหลือ TC-INT-2-001/002 เดิม)
  - INT-3 section: **TC-INT-3-004 คงอยู่ตามเดิม** (ไม่ย้าย) แต่แก้หมายเหตุ/References ให้ cross-reference
    TC-INT-0-005/AC-INT-0-04 แทนการอ้าง TC-INT-2-007 แบบยืนลำพัง
  - อัปเดต TOC, header intro, และตารางสรุปจำนวน Test Case ต่อ Feature/map กับ AC ทั้งหมดให้สะท้อน mapping
    ใหม่ (AC ไม่ซ้ำรวม 14→13 เพราะ AC-INT-2-06/AC-INT-3-04 รวมเป็น AC-INT-0-04 เดียว, test case รวมคงที่
    ที่ 16 เพราะเป็นการย้าย ไม่ใช่เพิ่ม/ลด)
- `docs/03-testing/01-test-plan/test-plan.md`
  - §4 Risk R14: แก้ ID citation จาก TC-INT-2-003–007/AC-INT-2-03–06 เป็น TC-INT-0-001–005/AC-INT-0-01–04
    พร้อมระบุว่ากลไกนี้ตอนนี้เป็น Feature ID INT-0/REQ-18 ของตัวเองแล้ว ไม่ใช่ precondition ที่ไม่มี REQ
    number
  - §5 Entry Criteria ข้อ 2: แก้จำนวน scenario/Feature ID (57→56, 15→16) และ citation
    AC-INT-2-03–06/AC-INT-3-04 → AC-INT-0-01–04

**ไม่แตะ**: `backlog.md`, `user-journeys.md`, `01-spec/20260823-04-smart-integrations.md` (ยืนยันอ่านตรงจาก
ทั้งสองไฟล์แรกเพื่อ ground การ renumbering แต่ไม่แก้ไขใดๆ — เป็น read-only upstream ตาม methodology ของ
skill นี้เสมอ)

**ผลกระทบต่อเอกสารอื่น**: ไม่มี — เชน citation/renumbering fix สำหรับ INT-0/REQ-18 ที่เริ่มจาก
`feature-journey-writer` → `detailed-design-writer` → `api-db-spec-writer` → `architecture-writer` →
`test-suite-writer` ปิดครบทุกไฟล์ปลายทางแล้วในรอบนี้ (HLA, API Spec, Database Schema, Detailed Design,
Acceptance Criteria, Test Case, Test Plan)

**คำถามที่ยังค้างอยู่**: ไม่มี — pure mechanical renumbering ตาม instruction ที่กำหนดไว้ชัดเจน ไม่เข้าเกณฑ์
ask-user protocol
