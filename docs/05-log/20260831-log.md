# Log — 2026-08-31

## `api-db-spec-builder` — เพิ่ม operation `GET /insights/weight-records`

**บริบท**: หน้า Progress (INT-1) ต้องแสดงกราฟแนวโน้มน้ำหนัก แต่ Weight Record entity (HLA §5) เขียนได้
ผ่าน `POST /integrations/smart-scale/sync` (`api-spec.md` §3.8) เพียงทางเดียว ไม่มี operation ให้อ่าน
ประวัติกลับมาเลย ทำให้กราฟต้องพึ่ง mock data ต่อไป แม้ `GET /insights/forecast` จะคำนวณค่าพยากรณ์จริง
แล้วก็ตาม (แก้ในโค้ดจริงไปก่อนหน้านี้ในวันเดียวกัน)

**Scope**: จำกัดเฉพาะ operation เดียว (narrow, single-operation addition) ตามที่ผู้ใช้ระบุ — ไม่ใช่การ
audit เต็มรูปแบบ

**แผนที่ยืนยันจากผู้ใช้**: เพิ่ม `GET /insights/weight-records` ใน `api-spec.md` §3.7 (Insights &
Forecast) — ไม่ใช่ §3.8 (Integration Gateway) — เพราะ HLA §3.7/§5 ระบุชัดว่า Insights & Forecast เป็น
component ที่**อ่าน** Weight Record history เพื่อพยากรณ์/แสดงแนวโน้ม ส่วน Integration Gateway เป็นผู้
**เขียน**จากการซิงค์อุปกรณ์เท่านั้น ไม่มีข้อขัดแย้ง/ข้อมูลใหม่ที่ต้องส่งกลับ `architecture-builder` — HLA
มี entity/component รองรับอยู่แล้วครบ

**ไฟล์ที่แก้ไข**:

- `docs/02-design/02-technical/api-spec.md` §3.7 — เพิ่มแถว `GET /insights/weight-records` (รับช่วงวันที่
  optional, คืนรายการ Weight Record เรียงตามเวลาที่บันทึก) มิเรอร์รูปแบบเดียวกับ `GET /logs` ที่มีอยู่แล้ว
- `docs/02-design/02-technical/database-schema.md` §5 (Query/Access Pattern Considerations) — เพิ่ม
  bullet ยืนยัน `weight_record` ค้นหาตาม `user_profile_id` + ช่วง `recorded_at` เป็น pattern ที่ operation
  ใหม่นี้ใช้จริง — **ไม่แตะตาราง `weight_record` เอง** (§3.14) เพราะ column ที่มีอยู่ครบพอแล้ว ไม่ใช่การ
  re-model

**ภาคผนวก Stack Mapping**: ไม่แตะ — operation/table ที่แก้ไม่ใช่ Component ใหม่ ไม่กระทบ mapping ที่มีอยู่

**ขั้นตอนถัดไป (นอกเหนือ scope ของ skill นี้)**: implement server route (`GET /insights/weight-records`
ใน `apps/web/server/routes/insights-forecast/index.ts`) และผูก client (`ProgressScreen.tsx` เลิกใช้
`MOCK_WEIGHT_HISTORY`)

## `api-db-spec-builder` — เพิ่มตาราง `today_recommendation_snapshot`/`today_recommendation_rejected_video` (แคชการแนะนำวิดีโอรายวัน)

**บริบท**: audit ตามโค้ดที่เพิ่ง shipped ใน commit `b463436`
(`apps/web/server/routes/content-recommendation/index.ts`) พบ caching pattern ใหม่ที่ไม่มีตารางรองรับใน
`database-schema.md` เลย — ระบบแคชผลการเลือกวิดีโอแนะนำของวันนั้นไว้บน `users/{userId}.todaysRecommendation`
(`computedFor: string`/`video: PickedVideo`/`rejectedVideoIds: string[]`) recompute เฉพาะเมื่อ
`computedFor` ไม่ตรงกับวันนี้ ใช้ทั้งใน `GET /workouts/today/recommendation` และ `.../swap`

**ตรวจสอบ HLA ก่อนตาม hard prerequisite**: อ่าน `high-level-architecture.md` §3.3 (Content Recommendation
— รับผิดชอบ REC-1/REC-3/REC-4) และ §5 (entity **Video/Workout Content**) แล้วยืนยันว่านี่เป็น byproduct
เชิง implementation ของ REC-1/REC-3 ที่ HLA โมเดลไว้แล้ว (recompute-and-cache ผลลัพธ์ของ entity เดิม) ไม่ใช่
แนวคิดธุรกิจใหม่ที่ HLA ไม่ครอบคลุม — จึงดำเนินการต่อได้เองโดยไม่ต้องส่งกลับ `architecture-builder`

**แผน**: เพิ่ม 2 ตารางใหม่ตาม pattern เดียวกับ `weight_forecast_snapshot`/`streak_snapshot` (cache แบบ
recompute-and-overwrite รายวัน 1:1 กับผู้ใช้ ไม่ persist ประวัติ):

- **`today_recommendation_snapshot`** (§3.18) — `user_profile_id` (FK, 1:1), `computed_for_date`,
  และ column วิดีโอที่ derive โครงสร้างจาก `session_video` (§3.6): `external_video_id`, `title` (เพิ่มใหม่ —
  ต้องแสดงก่อนเริ่มเซสชันจริง), `duration_minutes`, `activity_type`, `intensity`, `estimated_kcal`,
  `includes_warmup_cooldown`
- **`today_recommendation_rejected_video`** (§3.19) — FK → `today_recommendation_snapshot.id`,
  `external_video_id` — **แยกจาก `session_rejected_video` (§3.7) โดยตั้งใจ** เพราะ scope ต่างกัน: ตัวใหม่นี้
  ครอบคลุมทุก recommendation call ก่อนเริ่มเซสชันจริงในวันนั้นทั้งวัน ส่วน `session_rejected_video` ผูกกับ
  1 เซสชันที่เริ่มไปแล้วเท่านั้น — ไม่ merge เป็นตารางเดียว

**ไฟล์ที่แก้ไข**:

- `docs/02-design/02-technical/database-schema.md` — เพิ่ม header changelog (รอบ 8), เพิ่ม 2 entity/2
  ความสัมพันธ์ใน ER Diagram (§2), เพิ่ม Table Details §3.18/§3.19, เพิ่ม bullet ใหม่ 2 จุดใน §4
  (Relationships & Constraints — cache 1:1 ใหม่, และคำอธิบายว่าตาราง rejected-video 2 ตัวเป็นคนละ entity),
  เพิ่ม bullet ใหม่ใน §5 (Query/Access Pattern — ไม่มี pattern query อิสระ เหมือน
  `streak_snapshot`/`weight_forecast_snapshot`)
- `docs/02-design/02-technical/index.md` — ตรวจแล้วไม่ต้องแก้ (มีการกล่าวถึง `api-spec.md`/
  `database-schema.md` อยู่แล้วในระดับที่ไม่ผูกกับจำนวนตาราง)

**ภาคผนวก Stack Mapping**: **ไม่แตะ §8** ของ `database-schema.md` รอบนี้ — `tech-stack.md` ยังไม่ได้
reconcile จาก Firebase Cloud Functions/React Native-Expo เดิมมาเป็น Express + web-first ตามโค้ดจริง
(pre-existing drift ตาม CLAUDE.md § "Docs/code drift") ต้องรอ `tech-stack-builder` ก่อน — เตรียม field
name จริงจากโค้ดไว้ให้ใช้ต่อ: `todaysRecommendation.computedFor`/`.video`/`.rejectedVideoIds` แบบ embedded
map field บน `users/{userId}` เดียวกับ `weightForecastSnapshot` (ไม่ใช่ subcollection แยก — เข้าเกณฑ์ embed
ตาม §8.2 เดิม: bounded, 1:1 กับผู้ใช้, ไม่มี pattern query อิสระ)

**ขั้นตอนถัดไป (นอกเหนือ scope ของ skill นี้)**: รัน `tech-stack-builder` เพื่อ reconcile ภาคผนวก Stack
Mapping ทั้ง 4 จุด (HLA §10, `api-spec.md` §6, `database-schema.md` §8, `detailed-design/*.md`) ให้ตรงกับ
Express + web-first จริง ซึ่งรวมถึงแถวใหม่ของ `today_recommendation_snapshot`/
`today_recommendation_rejected_video` ที่เพิ่งเพิ่มนี้ด้วย

## `detailed-design-builder` — แก้ REC-1 (ไม่มี tolerance/widen-retry จริง) และ INT-1 (สูตร forecast ผิด + execution ผิด) ให้ตรงกับโค้ดที่ ship แล้ว

**บริบท**: audit เทียบ `detailed-design/02-daily-youtube-recommendation.md` และ
`detailed-design/04-smart-integrations.md` กับโค้ดจริงที่เพิ่ง ship ใน commit `b463436`
(`apps/web/server/services/youtube.ts`, `videoRecommender.ts`,
`apps/web/server/routes/insights-forecast/index.ts`) พบว่าเนื้อหาหลัก (sequence diagram + algorithm) ของ
ทั้งสอง Feature ขัดกับ implementation จริงตรงๆ ไม่ใช่แค่ล้าหลัง

**REC-1 (`02-daily-youtube-recommendation.md`)**: เอกสารเดิมอธิบายเป็น loop ค้นหา → เทียบ tolerance ตัวเลข
→ ถ้าไม่ตรงพอขยายเกณฑ์ → ค้นหาซ้ำ จนถึงขีดจำกัด — โค้ดจริงเป็น **single-pass ล้วนๆ**: ค้นหา YouTube ครั้ง
เดียว (สูงสุด 15 candidate, ระยะเวลาปานกลาง, safe search, ไม่รวมวิดีโอที่เคยแสดงแล้ว) → กรอง candidate ที่
embed ไม่ได้ออก → ส่งเข้าขั้นตอนจับคู่/ประเมินด้วย AI (Gemini) ครั้งเดียวที่เลือก candidate ที่ดีที่สุด 1
รายการแบบ best-effort (ไม่มี tolerance ตัวเลข ไม่มีการขยายเกณฑ์ ไม่มีการค้นหาซ้ำเลย) — คืน error เฉพาะเมื่อ
ไม่มี candidate เหลือเลย หรือขั้นตอนจับคู่ไม่ได้ผลลัพธ์ที่ใช้งานได้ — **แก้ sequence diagram + algorithm
ของ REC-1 ทั้งหมดให้ตรงกับพฤติกรรมจริง** (ปรับ REC-3 ให้อ้างชื่ออัลกอริทึมใหม่ตามด้วย) และ**resolve จุดที่
ยังไม่ได้ระบุเดิมข้อ 1** (ตัวเลข tolerance/ขีดจำกัดขยายเกณฑ์) เพราะคำตอบจริงคือ "ไม่มีตัวเลข tolerance —
ใช้ AI ranking แบบ best-effort แทน" — แก้ภาคผนวก Stack Mapping แถว Content Recommendation จาก "ปัจจุบัน
เป็น stub คืน `501`" (ล้าหลัง) เป็น implementation จริง พร้อมระบุชื่อ **Gemini** (ข้อยกเว้นเดียวที่อนุญาต
ชื่อ stack จริง)

**INT-1 (`04-smart-integrations.md`)**: 2 จุด — (1) **สูตร step 4 ของอัลกอริทึม forecast เขียนผิด** เดิม
เขียนว่า "อัตราขาดดุลเฉลี่ยต่อวัน = ผลรวมส่วนต่าง (เป้าหมาย − แคลอรี่จริง) หารด้วยจำนวนวัน" ซึ่งกลับทิศความ
สัมพันธ์ (ออกกำลังกายน้อยลงจะพยากรณ์ว่าน้ำหนักเปลี่ยนเร็วขึ้น) — สูตรที่ถูกต้องและ ship จริง (ยืนยันจาก code
comment ในไฟล์ที่ระบุว่าผู้ใช้ยืนยันการแก้นี้แล้วเมื่อ 2026-08-31 ในอีก session หนึ่ง) คือ**ค่าเฉลี่ยของ
แคลอรี่ที่เผาผลาญจริงต่อวันตรงๆ** ไม่ใช่ผลต่างจากเป้าหมาย เพราะแอปนี้ติดตามเฉพาะแคลอรี่ที่เผาผลาญ ไม่มีการ
บันทึกแคลอรี่จากอาหาร — แก้ step 4 + label ที่เกี่ยวข้องใน sequence diagram (2) **"Execution ของ algorithm"
+ แถว "Insights & Forecast" ในภาคผนวก Stack Mapping อ้างผิดว่า INT-1 คำนวณฝั่ง client** (generalize มาจาก
REC-2's MET calculation ที่มีเหตุผลด้าน latency ระหว่างออกกำลังกายจริง) — จริงๆ `GET /api/insights/forecast`
เป็น bodyless GET ที่ **server คำนวณทั้งหมดเอง** — แก้ให้ตรงกับพฤติกรรมจริง (ใช้ดุลพินิจแก้เองไม่ถาม
AskUserQuestion เพราะเป็นการแก้ข้อความบรรยาย execution ให้ตรงกับโค้ดจริง ไม่ใช่การเปลี่ยน business rule)
พร้อมแก้แถว "Insights & Forecast" ที่เคยระบุว่า "ปัจจุบันคืนค่า snapshot ที่มีอยู่ตรงๆ รอ implement การ
คำนวณจริง" ซึ่งล้าหลังไปแล้วเช่นกัน — ตรวจ `MIN_LOG_DAYS_FOR_FORECAST = 3` ในโค้ดแล้วยืนยันว่า**ยังไม่ resolve
เป็นทางการ** (code comment ระบุเป็นค่า placeholder เชิงปฏิบัติ) จึง**คงจุดที่ยังไม่ได้ระบุ #1 (จำนวนวัน log
ขั้นต่ำ) ไว้เหมือนเดิม** เพิ่มหมายเหตุอ้างอิงค่า placeholder นี้ประกอบเท่านั้น ไม่ถือว่า resolve แล้ว

**AskUserQuestion**: ไม่ได้ใช้ — ทั้งหมดเป็นการแก้ให้ตรงกับพฤติกรรมโค้ดจริงที่ระบุหลักฐานชัดเจน (comment ใน
โค้ด/พฤติกรรม endpoint จริง) ไม่ใช่การตัดสินใจ business rule ใหม่หรือขัดแย้งที่ต้องเลือกฝั่ง

**ไฟล์ที่แก้ไข**:

- `docs/02-design/02-technical/detailed-design/02-daily-youtube-recommendation.md` — header changelog
  ใหม่, sequence diagram + algorithm ของ REC-1 เขียนใหม่ทั้งหมด (single-pass + AI ranking), แก้ REC-3 ให้
  อ้างชื่ออัลกอริทึมใหม่, resolve จุดที่ยังไม่ได้ระบุเดิมข้อ 1, แก้ภาคผนวก Stack Mapping แถว Content
  Recommendation
- `docs/02-design/02-technical/detailed-design/04-smart-integrations.md` — header changelog ใหม่, แก้
  step 4 ของอัลกอริทึม INT-1 + label sequence diagram, เพิ่มหมายเหตุ placeholder ในจุดที่ยังไม่ได้ระบุ #1,
  แก้แถว Insights & Forecast + ย่อหน้า "Execution ของ algorithm" ในภาคผนวก Stack Mapping
- `docs/02-design/02-technical/index.md` — เพิ่ม changelog bullet สรุปรอบนี้
- `docs/05-log/20260831-log.md` — เพิ่ม section นี้

**สิ่งที่ยัง stale ใน `tech-stack.md` (นอกขอบเขตการแก้ของ skill นี้ — flag ให้ `tech-stack-builder` ต่อ)**:

1. §6.1 แถว **Content Recommendation** (บรรทัดที่ขึ้นต้น `| Content Recommendation |`) ยังเขียนว่า
   "ปัจจุบันเป็น stub คืน `501` รอเรียก YouTube Data API v3 จริง" และ "สร้าง ... `sessionVideos: []` ...
   ยังไม่ implement ในโค้ดปัจจุบัน" — ทั้งคู่ล้าหลังไปแล้ว (implement จริงและ ship แล้ว รวมถึง Gemini ranking
   ที่ยังไม่ถูกกล่าวถึงในแถวนี้เลย)
2. §6.1 แถว **Insights & Forecast** ยังเขียนว่า "ปัจจุบันคืนค่า snapshot ที่มีอยู่ตรงๆ รอ implement การ
   คำนวณจริงจากประวัติ `dailyLogs`/`weightRecords`" — ล้าหลังไปแล้ว (คำนวณจริงและ ship แล้วเต็มรูปแบบ)
3. §4 (เหตุผลการเลือก, "Client-side calculation ตาม NFR-01/NFR-03") ระบุว่า "[Detailed Design] ระบุชัดว่า
   TDEE (ONB-1), safety floor (ONB-3), MET+wearable override (REC-2), streak walk-back (PLN-4), และ
   **forecast (INT-1)** ต้องเป็นการคำนวณที่ไม่มี network latency — จึง implement ... ที่ client โดยตรง" —
   ส่วน "forecast (INT-1)" ในประโยคนี้**ผิด** หลัง audit รอบนี้: `detailed-design/04-smart-integrations.md`
   แก้ไปแล้วว่า INT-1 คำนวณฝั่ง server ทั้งหมด ไม่ใช่ client-side — ควรตัด "forecast (INT-1)" ออกจากรายการนี้
   ใน §4 (หรือแก้คำอธิบายให้ตรงว่า INT-1 ไม่เข้าเกณฑ์ NFR-01/03 แบบเดียวกับ feature อื่นในรายการ)

## `tech-stack-builder` — formalize Google Gemini + reconcile 3 จุดที่ล้าหลังตามที่ `detailed-design-builder` flag ไว้

**บริบท**: รับช่วงต่อจาก `detailed-design-builder`/`api-db-spec-builder` ข้างบนในวันเดียวกัน — ตรวจ
`smartfit_daily_app/apps/web/server/services/videoRecommender.ts`/`youtube.ts` และ
`routes/insights-forecast/index.ts` ที่ commit `b463436` ship จริงแล้วโดยตรงตามที่ผู้ใช้ระบุ ไม่ใช่การเปิด
Discovery Questionnaire ใหม่

**การตัดสินใจที่ยืนยันไว้ล่วงหน้าแล้ว (ไม่ถาม AskUserQuestion ซ้ำ)**: **Google Gemini** (`@google/genai`
SDK, model `gemini-3.6-flash`) เป็นเทคโนโลยีจริงที่เลือกแล้วสำหรับขั้นตอน ranking/ประเมินความเข้มข้นของวิดีโอ
ใน Content Recommendation (REC-1/REC-4) — เป็นขั้นตอนเสริมต่อจาก YouTube Data API v3 search (ยังคงเป็น
แหล่งค้นหาผู้สมัครเหมือนเดิม) ไม่ใช่แทนที่ — เลือก tier Flash เพราะเป็น tier เดียวที่มี free-tier quota ให้
ใช้งานจริงโดยไม่ต้องเปิด billing (Pro-tier ไม่มี free-tier ตาม comment ในโค้ดเอง)

**ไฟล์ที่แก้ไข**:

- `docs/02-design/02-technical/tech-stack.md`:
  - Header — เพิ่ม "อัปเดตล่าสุด: 2026-08-31" (cascade ป้ายชื่อ "อัปเดตก่อนหน้า" เดิมทั้งหมดลงมา 1 ชั้น)
  - §3 (Recommended Tech Stack) — แถว Third-party Integration เพิ่ม Gemini พร้อม env var
    (`GEMINI_API_KEY`/`GOOGLE_API_KEY`)
  - §4 (เหตุผลการเลือก) — เพิ่มย่อหน้า rationale ของ Gemini (free-tier Flash vs. billing-only Pro) และแก้
    รายการ "Client-side calculation ตาม NFR-01/NFR-03" ตัด forecast (INT-1) ออกพร้อมอธิบายเหตุผล
  - §5 (ทางเลือกอื่นที่พิจารณาแล้ว) — เพิ่มหัวข้อย่อยใหม่ "Video Ranking Model สำหรับ Content Recommendation
    (Gemini)" บันทึกทางเลือกโดยสังเขป (OpenAI GPT-4o-mini, Anthropic Claude Haiku, rule-based keyword
    filtering) — ไม่ใช่ Weighted Scoring Model เต็มรูปแบบเพราะเป็นการบันทึกย้อนหลังของสิ่งที่ ship แล้ว
  - §6.1 — แก้แถว **Content Recommendation** (implement จริงแล้ว: YouTube search → Gemini ranking
    single-pass → cache `users/{userId}.todaysRecommendation`) และ **Insights & Forecast** (implement
    จริงแล้ว: server คำนวณทั้งหมด + เพิ่ม `GET /api/insights/weight-records`)
  - §8 — แก้ให้ตรงกับสถานะจริง (ความ stale เดิมจากรอบ 2026-08-30 resolve ไปแล้วจริงตาม index.md) พร้อม flag
    ความ stale ใหม่เฉพาะจุดที่รอบนี้สร้างขึ้น: HLA §10 (แถว Content Recommendation) และ
    `database-schema.md` §8.2/§8.3 (ยังไม่มีแถว `today_recommendation_snapshot`/
    `today_recommendation_rejected_video`)
- `docs/02-design/02-technical/index.md` — เพิ่ม changelog bullet สรุปรอบนี้ และแก้ย่อหน้าสรุป stack
  ปัจจุบันด้านบนให้กล่าวถึง Gemini ด้วย
- `docs/05-log/20260831-log.md` — เพิ่ม section นี้

**ตรวจสอบ `.env.example`**: `smartfit_daily_app/apps/web/.env.example` มีการ document
`GEMINI_API_KEY` ไว้แล้วจริง (บรรทัด "REC-1/REC-4 ... GEMINI_API_KEY — a Gemini API key ...") — **ไม่ใช่
gap ที่ต้องแก้เพิ่ม** ต่างจากสมมติฐานเริ่มต้นของ task นี้ที่คาดว่ายังไม่ได้ documented

**AskUserQuestion**: ไม่ได้ใช้ — การเลือก Gemini เป็นการบันทึกข้อเท็จจริงที่ยืนยันไว้ล่วงหน้าแล้ว ส่วนที่เหลือ
เป็นการแก้ไขให้ตรงกับพฤติกรรมโค้ดจริงที่มีหลักฐานชัดเจน ไม่ใช่การตัดสินใจ business/stack ใหม่

**ผลกระทบต่อเอกสารอื่น (นอกขอบเขตของ skill นี้ — ห้ามแก้เอง)**:

1. **`high-level-architecture.md` §10** — แถว "Content Recommendation" ยังอ้าง "ตรรกะ matching/
   widen-retry" ที่ไม่มีจริง (โค้ดเป็น single-pass) และไม่มี mapping ของ Gemini เลย — ควรรัน
   `architecture-builder`
2. **`database-schema.md` §8.2/§8.3** — ยังไม่มีแถว mapping ของ `today_recommendation_snapshot`/
   `today_recommendation_rejected_video` (§3.18/§3.19) — ควรรัน `api-db-spec-builder`
3. `detailed-design/02-daily-youtube-recommendation.md` และ `04-smart-integrations.md` **ไม่ stale** —
   sync ภาคผนวกของตัวเองไปแล้วล่วงหน้าในรอบก่อนหน้าของวันเดียวกัน (ดู section ด้านบน)

## `architecture-builder` — mechanical re-sync แถว "Content Recommendation" ใน HLA §10 ตาม `tech-stack.md` §6.1

**บริบท**: รับช่วงต่อจาก `tech-stack-builder` ข้างบนโดยตรง — §10 (ภาคผนวก: Stack Mapping) ของ
`high-level-architecture.md` แถว "Content Recommendation" stale ตามที่ `tech-stack-writer` flag ไว้: ยังอ้าง
"ตรรกะ matching/widen-retry" ที่ไม่มีจริงในโค้ด (single-pass ล้วน) และไม่มี mapping ของ Google Gemini เลย

**การตัดสินใจ**: เป็น mechanical re-sync ล้วนๆ ตามกติกา Stack Mapping Appendix freshness — ไม่ต้องถาม
AskUserQuestion (การเลือก Gemini + พฤติกรรม single-pass เป็นข้อเท็จจริงที่ตัดสินใจ/ship ไปแล้วที่อื่น ไม่ใช่
การตัดสินใจเนื้อหาใหม่) เนื้อหาหลักหัวข้อ 1-9 ของ HLA ไม่ต้องแก้ (REC-1 ถูกบรรยายเชิงแนวคิดในหัวข้อ 3.3/§4
โดยไม่อ้างรายละเอียดระดับ widen-retry นี้อยู่แล้ว จึงไม่ขัดแย้งกับ implementation จริง)

**ไฟล์ที่แก้ไข**:

- `docs/02-design/02-technical/high-level-architecture.md` §10 — แก้แถว "Content Recommendation" ให้ตรงกับ
  `tech-stack.md` §6.1 ฉบับล่าสุดทุกประการ: ลบข้อความ widen-retry/tolerance ที่ไม่มีจริง เพิ่ม 2-ขั้นตอน
  (YouTube Data API v3 search → Google Gemini `gemini-3.6-flash` ranking แบบ single-pass) พร้อม cache field
  `users/{userId}.todaysRecommendation` (`computedFor`/`video`/`rejectedVideoIds`) ที่ map กับ
  `database-schema.md` §3.18/§3.19 — เพิ่ม freshness note ใหม่ลงวันที่ 2026-08-31 เหนือ table
- `docs/02-design/02-technical/index.md` — เพิ่ม changelog bullet สรุปรอบนี้
- `docs/05-log/20260831-log.md` — เพิ่ม section นี้

**AskUserQuestion**: ไม่ได้ใช้ — pure mechanical re-sync ตามกติกา Stack Mapping Appendix freshness

**ผลกระทบต่อเอกสารอื่น (นอกขอบเขตของ skill นี้ — ห้ามแก้เอง)**:

1. **`database-schema.md` §8.2/§8.3** — ยังไม่มีแถว mapping ของ `today_recommendation_snapshot`/
   `today_recommendation_rejected_video` (§3.18/§3.19) — ควรรัน `api-db-spec-builder` ต่อ

## `api-db-spec-builder` — mechanical re-sync `database-schema.md` §8.2/§8.3 ตาม `tech-stack.md` §6.1 (ปิดท้ายเชน Gemini/`todaysRecommendation`)

**บริบท**: รับช่วงต่อจาก `architecture-builder` ข้างบนโดยตรง — `database-schema.md` §8.2/§8.3
(ภาคผนวก: Stack Mapping) ยังไม่มีแถว mapping ของ 2 ตารางที่เพิ่มไปแล้วในรอบ 8 ของวันเดียวกัน
(`today_recommendation_snapshot`/`today_recommendation_rejected_video`, §3.18/§3.19) ทั้งที่
`tech-stack.md` §6.1 แถว Content Recommendation เพิ่ง reconcile รายละเอียด embed ครบแล้ว

**การตัดสินใจ**: mechanical re-sync ล้วนๆ ตามกติกา Stack Mapping Appendix freshness — ไม่ใช้
AskUserQuestion (ข้อเท็จจริงเรื่อง embed field/route ตัดสินใจไว้แล้วใน `tech-stack.md`) ไม่แตะเนื้อหาหลัก
หัวข้อ 1-7 (ถูกต้องอยู่แล้วจากรอบ 8)

**แผน**:

- §8.2 — เพิ่ม 2 แถวใหม่ต่อจากแถว `pairing_credential`: `today_recommendation_snapshot` (embed เป็น
  sub-field `computedFor`/`video` ของ `users/{userId}.todaysRecommendation`) และ
  `today_recommendation_rejected_video` (embed เป็น sub-field array `rejectedVideoIds` ของ field
  เดียวกัน — ไม่มี field เทียบเท่า `rejected_at` ตรงกับที่ §3.19 ระบุไว้แล้ว)
- §8.3 — เพิ่ม 1 แถวใหม่: recompute-on-cache-miss ของ `todaysRecommendation` enforce ที่ Express route
  `GET /api/workouts/today/recommendation` (เทียบ `computedFor` กับวันนี้เอง) และ
  `POST .../swap` (บังคับ recompute เสมอ) — ไม่มี TTL/trigger อัตโนมัติให้ใช้ฟรี

**ไฟล์ที่แก้ไข**:

- `docs/02-design/02-technical/database-schema.md` — เพิ่ม header changelog (รอบ 9), เพิ่ม note block
  ใหม่เหนือ §8.1 (ชี้แจงว่าไม่แตะ 8.1 เพราะ logical type เดิมครอบคลุมครบแล้ว), เพิ่ม 2 แถวใน §8.2, เพิ่ม
  note block + 1 แถวใหม่ใน §8.3
- `docs/02-design/02-technical/index.md` — เพิ่ม changelog bullet สรุปรอบนี้ (ปิดท้ายเชน Gemini/
  `todaysRecommendation` — ยืนยันว่าภาคผนวก Stack Mapping ทั้ง 4 จุดของ epic นี้ sync ครบแล้ว)
- `docs/05-log/20260831-log.md` — เพิ่ม section นี้

**AskUserQuestion**: ไม่ได้ใช้ — pure mechanical re-sync ตามกติกา Stack Mapping Appendix freshness
ตามที่ระบุไว้ในคำสั่งงานนี้โดยตรง

**ผลกระทบต่อเอกสารอื่น**: ไม่มี — ปิดท้ายเชนการ reconcile Gemini/`todaysRecommendation` ที่เริ่มจาก
`detailed-design-builder` → `tech-stack-builder` → `architecture-builder` → `api-db-spec-builder` ครบทุก
ไฟล์ในวันเดียวกันแล้ว
