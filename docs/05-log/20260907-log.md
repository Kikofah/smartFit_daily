# Log — 2026-09-07

## `api-db-spec-builder` — sync ฟีเจอร์ลบบัญชี (`DELETE /api/account`) เข้า `api-spec.md`/`database-schema.md`

**บริบท**: ฟีเจอร์ลบบัญชีถูก implement จริงแล้วในโค้ด ก่อนที่เอกสารจะตามทัน — server route ใหม่
`smartfit_daily_app/apps/web/server/routes/account-session/deleteAccount.ts` (`DELETE /api/account`,
mount หลัง `authenticate` middleware ใน `apps/web/server/index.ts`) ลบทุก document ในแต่ละ 5
subcollection ใต้ `users/{userId}` (`dailyLogs`, `dayStatus`, `weeklyPlanEntries`, `weightRecords`,
`workoutSessions`) แล้วลบ document `users/{userId}` เอง (รวม embedded map ทั้งหมด: `goalSelection`,
`integrationConnections`, `streakSnapshot`, `weightForecastSnapshot`, `todaysRecommendation`) แล้วลบ
Firebase Authentication account (`auth.deleteUser(userId)`) เป็นลำดับสุดท้าย — เป็น immediate hard-delete
ถาวร ไม่มี retention window/soft-delete ฝั่ง client มีปุ่ม "ลบบัญชี" (danger zone) พร้อม inline confirm
card ใน `ProfileScreen.tsx` — grounded ใน **NFR-06** ที่มีอยู่แล้ว (`01-spec/20260827-05-non-functional-
requirements.md`) ไม่ใช่ business rule ใหม่ ไม่ต้องมี Feature ID/REQ ใหม่

**Audit**: เทียบกับ HLA (component **Account & Session Management**, §3.1/§7 — ระบุไว้แล้วว่าเป็นเจ้าของ
การบังคับใช้ NFR-06), NFR-06/NFR-11 (`01-spec/20260827-05-non-functional-requirements.md`), `backlog.md`
(NFR-06 traceability row ครอบคลุม "ทุก Feature ที่เก็บข้อมูลผู้ใช้" อยู่แล้ว ไม่ต้องแก้), และ
`user-journeys.md` (ไม่มี journey step เฉพาะสำหรับการลบบัญชี — ไม่ใช่ gap เพราะ NFR ไม่บังคับต้องมี journey
ของตัวเอง) — ไม่พบข้อขัดแย้งกับ upstream ใดๆ เป็นเอกสารล้าหลังล้วนๆ (upstream มี NFR-06 อยู่แล้ว แค่ยังไม่มี
operation/schema resolution รองรับ) แก้ผ่าน flow ปกติได้เลย ไม่ต้องใช้ ask-user protocol สำหรับการตัดสินใจ
หลัก (ผู้ใช้ยืนยันแล้วว่า immediate hard-delete เป็นทางเลือกที่ตั้งใจ)

**สิ่งที่ตรวจพบเพิ่มเติมระหว่าง audit (นอกเหนือ scope หลัก แต่เกี่ยวเนื่องโดยตรง)**:

1. `pairing_credential` (top-level collection `pairingCodes/{code}`) ที่ยังผูกกับ `user_account_id` ของ
   บัญชีที่ถูกลบไม่ถูกลบตามไปด้วยในโค้ด — orphan risk ที่บรรเทาด้วยอายุการใช้งานสั้นอยู่แล้ว (TTL 5 นาที) แต่
   ยังไม่มีการตัดสินใจอย่างเป็นทางการ — บันทึกเป็นจุดที่ยังไม่ได้ระบุใหม่ในทั้งสองไฟล์ ไม่ใช่ข้อขัดแย้งที่ต้อง
   บล็อกงาน
2. ไม่มี idempotency guard สำหรับการเรียก `DELETE /account` ซ้ำ/พร้อมกัน — implementation จริงจะ error
   แบบ generic `500` ถ้าเรียกซ้ำหลังลบสำเร็จแล้ว — บันทึกเป็นจุดที่ยังไม่ได้ระบุเช่นกัน (พฤติกรรมโดยบังเอิญ
   ของ implementation ไม่ใช่การตัดสินใจ business rule)

**แผน**: เพิ่ม operation ใหม่ `DELETE /account` ท้ายหัวข้อ 3.1 (Account & Session Management) ของ
`api-spec.md`, resolve `database-schema.md` §6 ข้อ 6 (เดิมถามว่า immediate delete หรือ retain) ด้วยรูปแบบ
`~~...~~` **resolved** ตาม convention ของไฟล์, เพิ่มลำดับการลบ cascade เต็มรูปแบบในหัวข้อ 4 ข้อ 9 ของ
`database-schema.md` และแถวใหม่ในหัวข้อ 8.3, เพิ่มจุดที่ยังไม่ได้ระบุใหม่ทั้งสองไฟล์ (idempotency,
`pairing_credential` orphan)

**ไฟล์ที่แก้ไข**:

- `docs/02-design/02-technical/api-spec.md` — changelog header (รอบ 10), แถวใหม่ `DELETE /account` ใน
  §3.1, จุดที่ยังไม่ได้ระบุใหม่ 2 ข้อ (14, 15) ในหัวข้อ 4, หมายเหตุ drift ในหัวข้อ 6 (ไม่แตะตาราง Stack
  Mapping จริงเพราะเป็น strict mirror ของ `tech-stack.md` ที่ยังไม่ reconcile operation นี้)
- `docs/02-design/02-technical/database-schema.md` — changelog header (รอบ 11), resolve หัวข้อ 6 ข้อ 6,
  เพิ่มข้อ 9 ในหัวข้อ 4 (cascade order), เพิ่มจุดที่ยังไม่ได้ระบุใหม่ 2 ข้อ (12, 13) ในหัวข้อ 6, เพิ่มแถวใหม่
  ในหัวข้อ 8.3 (ได้รับอนุญาตให้ออกแบบเพิ่มเติมเองก่อน `tech-stack.md` ตาม pattern ที่ยืนยันไว้ตั้งแต่
  2026-08-29)
- `docs/02-design/02-technical/index.md` — เพิ่มรายการสรุปการเปลี่ยนแปลงนี้

**ภาคผนวก Stack Mapping**: `api-spec.md` §6 (strict mirror ของ `tech-stack.md` §6.3.1) **ไม่แตะ** — ยังไม่
มีอะไรใน `tech-stack.md` ให้มิเรอร์มา (ยังนับ 10 operation เดิม ไม่รวม `DELETE /account`) เพิ่มหมายเหตุ drift
ไว้แทน รอ `tech-stack-builder` ขยายในรอบถัดไป — `database-schema.md` §8.3 เพิ่มแถวใหม่ได้เองตาม pattern ที่
อนุญาตให้ `api-db-spec-builder` ออกแบบเพิ่มเติมล่วงหน้าได้ (§8.2/8.3 ไม่ใช่ strict mirror)

**ขั้นตอนถัดไป (นอกเหนือ scope ของ skill นี้)**: แนะนำให้รัน `tech-stack-builder` เพื่อเพิ่ม `DELETE
/account` เข้า `tech-stack.md` §6.1/§6.3.1 (จะทำให้ `api-spec.md` §6 กลับมา sync ครบอีกครั้ง) — ไม่มีการ
เรียก skill อื่นในรอบนี้เพราะไม่พบข้อขัดแย้ง/ข้อมูลใหม่ที่ต้องส่งกลับ HLA/Requirement/Backlog/User Journey

**คำถามที่ยังค้างอยู่**: ไม่มี — ประเด็นหลัก (immediate hard-delete vs. retention) ผู้ใช้ยืนยันไว้แล้วใน
คำสั่งงาน ส่วนจุดรองที่พบเพิ่ม (idempotency, pairing_credential orphan) บันทึกเป็นจุดที่ยังไม่ได้ระบุ ไม่ใช่
ข้อขัดแย้งที่ต้องบล็อกงาน

## `api-db-spec-builder` — follow-up: resolve 2 open point ย่อย (pairing-code cleanup, idempotency)

**บริบท**: coordinator แจ้งว่าทั้ง 2 ช่องโหว่ที่พบระหว่าง audit ข้างต้น (pairing-code cleanup gap,
idempotency gap) ถูก patch ในโค้ดแล้ว — อ่าน `smartfit_daily_app/apps/web/server/routes/account-session/
deleteAccount.ts` ตรงเป็น source of truth ยืนยันทั้งสองจุด: (1) เพิ่ม query
`db.collection('pairingCodes').where('uid', '==', userId)` ลบทุกแถวที่พบก่อนลบ document `users/{userId}`
(2) ห่อ `auth.deleteUser(userId)` ด้วย try/catch ที่ treat error code `auth/user-not-found` เป็นความสำเร็จ
แทนที่จะปล่อยหลุดไปเป็น `500`

**ไฟล์ที่แก้ไข (ปิด open point เท่านั้น ไม่ใช่การ audit ใหม่)**:

- `docs/02-design/02-technical/api-spec.md` — resolve open point ข้อ 14 (idempotency) และข้อ 15
  (pairing-code cleanup) ในหัวข้อ 4 ด้วยรูปแบบ `~~...~~` **resolved**; แก้ Response/Error column ของแถว
  `DELETE /account` ในหัวข้อ 3.1 ให้ตรงกับพฤติกรรมใหม่ (รวม Pairing Credential เข้าไปในสิ่งที่ถูกลบ,
  ระบุว่า idempotent); เพิ่ม changelog entry รอบ 11
- `docs/02-design/02-technical/database-schema.md` — resolve open point ข้อ 12/13 ในหัวข้อ 6 ด้วยรูปแบบ
  เดียวกัน; แก้ข้อยกเว้นใน §4 ข้อ 9 (เดิมบอกว่า `pairing_credential` ไม่ถูกลบตามไปด้วย — แก้เป็นถูกลบด้วย
  แล้ว); แก้แถว account deletion cascade ใน §8.3 ให้ตรงกับพฤติกรรมใหม่ (เพิ่มขั้นตอน query
  `pairingCodes`, เพิ่ม try/catch สำหรับ idempotency); เพิ่ม changelog entry รอบ 12

**ไม่มีการเปลี่ยนแปลงอื่น** — ไม่แตะ HLA/Requirement/Backlog/User Journey/Stack Mapping appendix ของ
`api-spec.md` §6 (ยังรอ `tech-stack-builder` เหมือนเดิม) ไม่มีคำถามค้างอยู่

## `api-db-spec-builder` — follow-up: split open point #10 (account enumeration policy)

**บริบท**: coordinator แจ้งว่า `apps/web/server/routes/account-session/forgotPassword.ts` มี comment ใหม่
ที่ระบุชัดว่า control flow เดิม (ตกไปที่ `202 { status: 'sent' }` เดียวกันเมื่ออีเมลไม่มีอยู่จริง เพราะ
เงื่อนไข `422` ถูกข้ามไปเฉยๆ เมื่อ `auth.getUserByEmail` หาไม่เจอ) เป็น**การตัดสินใจ anti-enumeration ที่
ตั้งใจ**ตามแนวทาง OWASP สำหรับ password-reset flow ไม่ใช่ byproduct ที่ไม่ได้ตั้งใจอีกต่อไป — อ่านไฟล์ตรง
ยืนยันแล้ว ส่วน `POST /auth/login/email` (client-side Firebase Auth SDK call ตรง ผ่าน
`signInWithEmailAndPassword`) ยังคง leak account existence จริง (Firebase คืน `auth/user-not-found` แยก
จาก `auth/wrong-password`) เพราะการปิดช่องนี้ต้องเปลี่ยนให้ login ผ่าน server ของทีมเองแทน ซึ่งเป็นการ
เปลี่ยนสถาปัตยกรรมที่ใหญ่กว่าที่มีใครร้องขอไว้ — ยังไม่ resolve

**ไฟล์ที่แก้ไข (ปิดเฉพาะครึ่ง forgot-password ของ open point #10 เท่านั้น ไม่ใช่การ audit ใหม่)**:

- `docs/02-design/02-technical/api-spec.md` §4 — split open point ข้อ 10 เป็น 2 ครึ่งด้วยรูปแบบ `~~...~~`
  **resolved บางส่วน**: ครึ่ง `POST /auth/forgot-password` resolved (anti-enumeration ที่ตั้งใจ), ครึ่ง
  `POST /auth/login/email` ยังคง open (architecture ปัจจุบันไม่รองรับการปิดช่องนี้โดยไม่เปลี่ยน login ให้
  ผ่าน server เอง) — แก้แถว `POST /auth/forgot-password`/`POST /auth/login/email` ในหัวข้อ 3.1 (Error/Edge
  Case column) ให้ตรงกับสถานะใหม่ — เพิ่ม changelog entry รอบ 12

**ไม่แตะ**: `database-schema.md` (ไม่มีตาราง/column ที่เกี่ยวข้องกับประเด็นนี้), HLA, Requirement, Backlog,
User Journey — ไม่มีคำถามค้างอยู่
