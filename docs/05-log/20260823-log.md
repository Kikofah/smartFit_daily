# 2026-08-23 - Log

## สรุปสิ่งที่ทำ

- Reorganize โครงสร้าง `docs/` ทั้งหมดให้ตรงกับ convention เดียวกับโปรเจกต์ `my-coffee-store` ของผู้ใช้งาน
  (numbered pipeline: `00-archived`, `01-requirements/{01-spec,02-plan,03-task,backlog.md}`,
  `02-design/{01-prototypes,02-technical}`, `03-testing/{01-test-plan,02-test-result}`,
  `04-retrospectives`, `05-log`) พร้อม `index.md` อธิบายจุดประสงค์ในทุกโฟลเดอร์
- ย้าย/แยกเนื้อหาเดิม:
  - `docs/requirements/product-backlog.md` (ไฟล์รวม Feature List/Backlog/Requirement Spec/Decisions)
    ถูกแยกเป็น 4 เอกสาร requirement ต่อ epic ใน `docs/01-requirements/01-spec/`:
    [Onboarding](../01-requirements/01-spec/20260823-01-onboarding-personalization.md),
    [Daily YouTube Recommendation](../01-requirements/01-spec/20260823-02-daily-youtube-recommendation.md),
    [Planner & Logging](../01-requirements/01-spec/20260823-03-planner-logging.md),
    [Smart Integrations](../01-requirements/01-spec/20260823-04-smart-integrations.md)
    — โดยกระจาย Decisions (Resolved Ambiguities) ที่เคยรวมไว้ที่เดียว ไปไว้ใน section
    "ข้อสมมติฐาน/การตัดสินใจที่ยืนยันแล้ว" ของเอกสาร spec ที่ REQ นั้นสังกัดโดยตรง และกระจาย Open Questions
    เดิมไปยัง "จุดที่ยังไม่ได้ระบุ" ของ epic ที่เกี่ยวข้อง
  - `docs/features/feature-list.md` → ย้าย (git mv) เป็น `docs/01-requirements/backlog.md`
  - `docs/features/user-journeys.md` → ย้าย (git mv) เป็น `docs/02-design/01-prototypes/user-journeys.md`
    (ตรงกับความหมายของ `01-prototypes` ใน convention ใหม่ คือ "user flow และ navigation flow")
  - อัปเดต cross-link ทั้งหมดในสองไฟล์นี้ให้ชี้ไปยัง path ใหม่
- อัปเดต `.claude/skills/feature-list-journey/SKILL.md` และ `.claude/agents/feature-journey-writer.md`
  ให้อ้างอิง path ใหม่ (`01-spec/*.md` แทน `product-backlog.md` เดี่ยว, `backlog.md`/`user-journeys.md`
  ตำแหน่งใหม่) และเพิ่มกติกาว่า decision ที่ resolve จาก AskUserQuestion ต้องถูกบันทึกที่เอกสาร spec ก่อน
  แล้วค่อยอ้างอิงกลับมาจาก backlog/user-journeys แทนที่จะเขียนไว้ที่ backlog/user-journeys เป็นที่แรก
- เขียน `CLAUDE.md` ใหม่ทั้งไฟล์ให้ตรงกับ style/โครงสร้างของ `my-coffee-store` (Project status,
  Current focus, Requirement workflow, Product Backlog, User Journeys, Language, Automation,
  Documentation structure) แต่ปรับเนื้อหาให้ตรงกับสถานะจริงของ smartFit_daily ซึ่งไปไกลกว่า requirements
  stage แล้ว (มี backlog.md และ user-journeys.md ที่สมบูรณ์แล้ว)
- เพิ่ม `README.md` (`# smartFit_daily`) และเพิ่มบรรทัด `.claude/settings.local.json` ใน `.gitignore`
  ให้ตรงกับ convention เดียวกัน

## Open items ที่ยังค้างอยู่

- `docs/01-requirements/02-plan/`, `03-task/`, `docs/02-design/02-technical/`, `docs/03-testing/`,
  `docs/04-retrospectives/` เป็น scaffold ว่างตาม convention — ยังไม่ต้องเติมเนื้อหาจนกว่าจะถึงขั้นตอนนั้นจริง
- Open Questions/จุดที่ยังไม่ได้ระบุเดิมทั้ง 5 ข้อ (REC-1 tolerance, REC-4 warmup counting, PLN-1 past-day
  edit, INT-1 min log history, INT-2/INT-3 conflict resolution) ยังคงค้างอยู่เหมือนเดิม เพียงแต่ย้ายที่เก็บ
  ไปอยู่ใน section "จุดที่ยังไม่ได้ระบุ" ของเอกสาร spec แต่ละ epic แทน — ยังไม่ได้ resolve เพิ่มในรอบนี้
